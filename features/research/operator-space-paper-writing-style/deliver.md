# 交付说明 — 算子空间论文表述规范（范本蒸馏 + Skill）

关联：`spec.md`

## 交付范围

- 一篇 `knowledge/references/` 下的算子空间论文英文表述规范。
- 一个 `skills/operator-space-paper-prose/` 薄层 Skill。
- （可选）适配层生成的 `.cursor/rules/skill-*.mdc` 等同步更新。

## 版本与发布物

- 无版本号；以 Git commit 为准。

## 上线与验证

- 本地打开 Skill 说明，按 `test_report.md` 做最小抽检。

## 回滚

- Git revert 相关提交；收回 adapt 生成文件若已推送。

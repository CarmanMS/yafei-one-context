---
description: "Skill: remotion-pipelines — 场景组件驱动的 Remotion 视频流水线。audioConfig.ts 为单一数据源，SRT→音画同步， 中视频须 scene-boundaries.md 话题分组；Remotion render + burn-subtitles 出交付 MP4。 提供共享组件库 + init 脚手架，每条视频在独立 feature 目录开发。 触发词：Remotion pipeline、视频生成、scene"
globs:
alwaysApply: true
---

<!-- GENERATED by onecxt adapt — DO NOT EDIT. Changes will be overwritten. Edit the source in meta/ or knowledge/ instead. -->

# Skill: remotion-pipelines

# Remotion Pipelines — Scene-Based Audio-Driven Video Engine

## 核心架构

```
SRT + WAV → scene-boundaries.md（话题分组）→ audioConfig.ts → Scene 组件 + <Audio>
  → Remotion render → final_remotion.mp4 → burn-subtitles → final_remotion_sub.mp4
```

关键设计：
1. **audioConfig.ts 是单一数据源** — 场景时长、音频文件、帧率全部在此
2. **中视频按话题分组** — 一条 SRT ≠ 一场景；须 `scene-boundaries.md` 锚定口播再写帧数
3. **音频驱动时长** — 策略 B 用 ffprobe 实测时长；策略 A 用 SRT 时间戳换算
4. **Remotion 不烧字幕** — 渲染后调用 `burn-subtitles.mjs`（内置 `lib/subtitle_burn.js`，ffmpeg + ASS）
5. **共享层 + 项目层分离** — 积木越用越多，每条视频独立存在
6. **四轨动画（可选）** — Remotion 原生为默认；复杂动效用 Anime.js（SVG/Text）或 GSAP（Timeline/插件）；预制 AE 动画用 Lottie（`@remotion/lottie`）
7. **SVG 视觉隐喻优先** — 场景以图形/图表/视觉隐喻为主，文字精简为关键词；纯文字卡片缺乏表现力
8. **画布空间充分利用** — 1920×1080 主要内容区域应占 ≥70% 画布面积，避免大面积空白

## 共享层 vs 项目层

```
skills/remotion-pipelines/              ← 共享层（越用越丰富）
├── scripts/
│   ├── generate-audioconfig.mjs       # SRT 逐条 → config（短视频/教程类）
│   └── burn-subtitles.mjs             # 烧录 sub.srt → final_remotion_sub.mp4
├── references/
│   └── scene-boundaries.template.md   # 中视频话题边界表模板
├── src/shared/                         # 可复用积木
│   ├── colors.ts                       # 色彩系统（含辩论光谱色、图表辅助色）
│   ├── typography.ts                   # 字体 tokens
│   ├── layouts/                        # 布局模式
│   │   ├── FullBleedTitle.tsx
│   │   └── SplitLayout.tsx
│   ├── SceneBackground.tsx             # 通用动态背景（粒子/电路/轨道/六边形/数据流）
│   ├── svg/                            # SVG 动画工具 hooks（基础三件套 + 质感三件套）
│   │   ├── useStrokeReveal.ts          # 描线动画（strokeDashoffset + interpolate）
│   │   ├── useGrowIn.ts                # 缩放弹入（scale(0)→1 + spring）
│   │   ├── usePulse.ts                 # 呼吸脉冲（节点/图标高亮）
│   │   ├── useCountUp.ts               # 数字滚动揭示（替代静态 KPI）
│   │   ├── useBarGrow.ts               # 条形 overshoot 回弹（替代线性增长）
│   │   ├── useGlowPulse.ts             # 赢家/重点项到位后辉光收尾
│   │   └── index.ts
│   └── animations/                     # A 轨 Remotion + B 轨 Anime.js + C 轨 GSAP + D 轨 Lottie（可选）
│       ├── index.ts
│       ├── useGSAPTimeline.ts
│       ├── gsap/
│       ├── anime/
│       └── lottie/
├── src/scenes/cover/                   # 封面共享组件（init 会拷贝到项目）
│   ├── SceneCover.tsx                  # 变体路由器（按话题关键词自动选变体）
│   ├── CoverLayered.tsx                # 液态极光背景 + 装饰 SVG 层
│   ├── CoverParticle.tsx               # 粒子聚散轨道 + 脉冲环
│   ├── CoverCinematic.tsx              # 星场 + 扫描线 + 透视网格 + 电影黑边
│   └── shared/
│       ├── CoverText.tsx               # 逐字弹入标题 + 副标题 + tagline + shimmer
│       ├── CoverOverlay.tsx            # 暗角 + 噪点 + 可选扫描线
│       ├── ShimmerSweep.tsx            # 光泽扫过效果（可复用于任何组件）
│       ├── CornerMeta.tsx              # 四角等宽字体元数据标签
│       ├── coverThemes.ts             # 4 色彩主题：tech/danger/blueprint/deck
│       └── index.ts
├── templates/default/                  # 项目脚手架模板
└── cli.mjs                             # init 命令

features/.../my-video/                  ← 项目层（每次新建，推荐 remotion/ 子目录）
├── remotion/                           # init 目标目录
│   ├── src/audioConfig.ts
│   ├── src/scenes/
│   └── scripts/burn-subtitles.mjs
└── production/                         # content-pipeline 标准布局
    ├── media/voiceover.wav
    ├── subtitles/sub.srt
    ├── timing/
    │   ├── scene-boundaries.md         # 话题边界（人审）
    │   └── wav-durations.json          # 与 audioConfig 同步 + 字幕样式
    └── videos/
        ├── final_remotion.mp4          # 无字幕
        └── final_remotion_sub.mp4      # 交付物（有字幕）
```

**积累在 shared，变化在 scenes**：
- AI 写 SceneCover 时 `import { FullBleedTitle } from "../shared/layouts"`
- 下次做视频，shared 里多了 `ParallaxReveal`、`DataPulse` 等积木可选
- 每条视频的场景代码是新鲜的，但不会从零开始——越做积木越多

## 创建新视频项目

```bash
# 在 skill 目录执行
node cli.mjs init features/.../my-video/

# 或用 npm script
npm run init -- features/.../my-video/

# 然后进入项目
cd features/.../my-video/
npm install
```

init 会：
1. 从 `templates/default/` 复制完整脚手架
2. 复制 `scripts/*.mjs`（含 `generate-audioconfig.mjs`、`burn-subtitles.mjs`、`ensure-shared-browser.mjs`、`render-verify-stills.mjs`、`concat-intro-outro.mjs`）
3. 若存在 `../production/timing/`，复制 `references/scene-boundaries.template.md` → `scene-boundaries.md`（不覆盖已有）
4. 改写 `package.json` 项目名，并**按当前平台注入 `@remotion/compositor-xxx` + `@remotion/renderer`**，把 `remotion` 与所有 `@remotion/*` 钉到同一精确版本（避免 Studio "Version mismatch"），注入 `postinstall: ensure-shared-browser`
5. 复制 `src/shared/` 共享积木库到项目
6. 复制 `src/scenes/cover/` 封面组件库到项目

## Pipeline 步骤

| Step | 名称 | 产物 |
|------|------|------|
| 1 | Download | 原始文章 Markdown |
| 2 | Structure | `content-structure.md` |
| 3 | Script + TTS | `sub.srt` + `voiceover.wav` |
| 4 | Scene boundaries | `production/timing/scene-boundaries.md` |
| 5 | AudioConfig | `src/audioConfig.ts` + `wav-durations.json` |
| 6 | Scenes | 场景 React 组件 |
| 7 | Render | `final_remotion.mp4` |
| 8 | Burn subtitles | `final_remotion_sub.mp4` |
| 9a | Cover | `production/cover.html`（**走 `skills/cover-design`**，CONFIG 驱动模板） |
| 9b | Publish kit | `production/content/05-publish-kit.md` |

### Step 4: Scene boundaries（中视频 / 播客 **必做**）

Remotion 按 **话题** 切场景，不是按 SRT 条目数。模板：`references/scene-boundaries.template.md`。

1. 通读 `production/subtitles/sub.srt`，按口播话题划 8–15 段（与 `01-script.md` 块大致对应）。
2. 每段记录：**进入秒**、**时长**、**锚点 SRT#**、**摘录**。
3. 翻页落在话题切换句起点；过渡语（「接下来」「下面」）仍算 **上一段**。
4. 写入 `production/timing/scene-boundaries.md`，成片前试听 3 个锚点（首专名、第一次大转场、总结）。

### Step 5: AudioConfig

**中视频（推荐）**：从 `scene-boundaries.md` 手填 `audioConfig.ts`：

```typescript
// durationInFrames = Math.round(时长秒 × FPS)
{ id: "s2", title: "…", durationInFrames: 1890, audioFile: "audio/voiceover.wav" }
```

同步 `production/timing/wav-durations.json`：

```json
{
  "slideDurationsSec": [20.1, 78.72, …],
  "outputFile": "videos/final_remotion_sub.mp4",
  "burnSubtitles": true,
  "srtFile": "subtitles/sub.srt",
  "subtitle": { "fontSize": 60, "charsPerLine": 22, "barHeight": 0 }
}
```

**短视频 / 一条字幕一场景**（慎用中视频）：

```bash
node scripts/generate-audioconfig.mjs --srt script.srt --audio voiceover.wav
```

策略选择：
- 默认（策略 A）：SRT 时间戳映射帧数，播放完整 WAV
- `--mode split`（策略 B）：ffmpeg 切分 WAV + ffprobe 测时长

### Step 6: Scenes

AI 读取 SRT 内容 + audioConfig.ts，为每个场景生成 React 组件。

新建文件 `src/scenes/SceneXxx.tsx`，然后在 `scenes/index.tsx` 的 `SceneRouter` 中添加路由。

场景组件遵循 remotion-video-skill 的 3B1B 风格指南：逐步构建、颜色语义化、探索式叙事。

**字号硬约束（1920×1080）**：使用 `shared/typography.ts` 的 `FONT_SIZE`，禁止正文 12–14px。

| 元素 | 最小字号 | 说明 |
|------|---------|------|
| 正文 / 路径 / 卡片标签 | **≥42px**（`FONT_SIZE.body`） | div 内文字 |
| 卡片标题 | **≥52px** | |
| SVG `<text>` 文字 | **≥28px，推荐 ≥32px** | SVG 内字号在 1080p 下显示偏小，28px 仅限极短标签（2字内）；3字以上 ≥32px |
| SVG 节点序号 | **≥36px** | 流程图/步骤的 01/02 序号 |
| 关键词标签/气泡 | **≥32px** | 胶囊标签、光谱节点气泡等 |
| 辅助标注 | **≥32px** | 年份、阵营名、图标标签等 |
| 封面主标题 | **≥120px**，推荐 140–160px | 1920×1080 画布上 84px 偏小，标题应占画布宽度 40%+ |

**SVG 字号放大经验**：SVG 的 `fontSize` 在 1080p 视频中视觉偏小约 20-30%，原因是 SVG 坐标系与 CSS 像素映射。因此 SVG 内所有文字一律比直觉多 1-2 档。

可复用 shared 组件：
```tsx
import { FullBleedTitle, SplitLayout, COLORS, FONT_SIZE, SceneBackground } from "../shared";
import { useStrokeReveal, useGrowIn, usePulse } from "../shared/svg";
```

### 素材来源 vs 画面署名（**必守**）

微信稿、博客、HN 等是 **内容来源**，不是成片 **画面署名对象**。细则见 `references/source-attribution-boundaries.md`。

| 层 | 允许 | 禁止 |
|----|------|------|
| **Scene 可见 UI** | 本期论点、专名、数字、口播金句；本频道固定关注句（`video-voiceover-script-conventions.md`） | 素材公众号名、`@昵称`、nick_name、转载者/二道贩子署名、素材号引流 |
| **文档** | `spec.md`、`00-podcast-source.md`、`05-publish-kit.md`、`content/source/*` 写原文 URL 与出处 | 把上述 meta 抄进 `SceneCover` 底部 pill、`tagline`、致谢页 |

**写 Scene 前**：读 `production/content/source/*` 只取 **论点与结构**；**不得**把 `_weixin_raw.html` 的 `nick_name` / 作者列入 `enumeratedItems` 或底部装饰字。

**封面/致谢底部**：默认 **仅**「欢迎关注大厂吾师兄」一类本频道引导——**不要**与素材作者双署名并排（钉钉 One Inside 类事故：`@倪叔的思考暗时间`）。

### SVG 视觉隐喻场景模式

**核心理念**：场景应以图形/图表/视觉隐喻为主，文字精简为关键词。纯文字卡片在 1080p 视频中缺乏表现力，无法帮助观众直觉理解内容。

**目录结构**：
```
src/scenes/svg/              # 场景专用 SVG 组件
├── BalanceScale.tsx         # 天平隐喻（速度 vs 质量）
├── ReviewFunnel.tsx         # 漏斗图（审查过滤）
├── SpectrumAxis.tsx         # 光谱轴（快←→慢）
├── FlowNode.tsx             # 流程节点（步骤卡片）
├── TimelineAxis.tsx         # 时间轴（年份→年份）
├── …
src/shared/svg/              # 通用 SVG 动画 hooks
├── useStrokeReveal.ts       # 描线动画
├── useGrowIn.ts             # 缩放弹入
├── usePulse.ts              # 呼吸脉冲
├── useCountUp.ts            # 数字滚动揭示
├── useBarGrow.ts            # 条形 overshoot 回弹
├── useGlowPulse.ts          # 辉光收尾
└── index.ts
```

**视觉隐喻库**（按场景类型选择）：

| 视觉隐喻 | 适用场景 | SVG 组件 |
|---------|---------|---------|
| 天平 | 二元对立/悖论 | `BalanceScale` |
| 漏斗 | 过滤/审查流程 | `ReviewFunnel` |
| 光谱轴 | 多立场/渐变 | `SpectrumAxis` + `CampNode` |
| 流程图 | 步骤/方法 | `FlowNode` + `FlowArrow` |
| 时间轴 | 演进/转型 | `TimelineAxis` |
| 三图标公式 | 公式/组合 | `FormulaIcons` |
| 愿景图 | 协作/伙伴 | `AgentVision` |

**SVG 动画三件套**（纯 Remotion 原生，零额外依赖）：

| Hook | 效果 | 用法 |
|------|------|------|
| `useStrokeReveal(pathLen, start, dur)` | 描线绘制 | 返回 `strokeDasharray` + `strokeDashoffset` |
| `useGrowIn(delay, config?)` | 缩放弹入 | 返回 `opacity` + `scale`，直接挂 `style` |
| `usePulse(start, cycles, dur)` | 呼吸脉冲 | 返回 `opacity` + `scale`，用于节点/图标高亮 |

**场景布局模式**（按需组合，不限于以下）：
- **左图右文**：SVG + 关键词区，适合图形和文字同等重要且图形偏纵向
- **全幅图形**：SVG 铺满内容区，文字要点以底部/顶部条呈现，适合图形需要横向展开
- **上下分区**：上半图形 + 下半文字/标签，适合图形需要纵深叙事

### 封面场景设计模式

**核心原则：封面必有前景主视觉，不是多层微弱背景 + 纯文字。**

封面是观众第一帧看到的画面，需要在 0.5 秒内传递"这是什么 + 为什么看"。多层低透明度背景（粒子、电路、六边形、轨道环……）叠加再多也只是氛围，不能替代内容层的主视觉。

#### 封面组件库（init 已预置）

`src/scenes/cover/` 提供 3 个封面变体，**每个变体有独立的整页构图**，不只是换背景。agent 应基于这些变体组装封面，而非从零手写：

**三种构图变体**（各自包含完整的背景 + 文字排版 + 效果层）：

| 变体 | 构图 | 文字位置 | 视觉风格 |
|------|------|---------|---------|
| `CoverLayered` | 非对称编辑式 | 右侧 40% 竖向堆叠 | 极光色斑 + 几何装饰 + 竖向分割线 |
| `CoverParticle` | 上下分区 | 下方 35% 左对齐 | 粒子聚散 + 脉冲环 + 水平渐变分割线 |
| `CoverCinematic` | 全幅电影海报 | 居中超大 140px | 星场 + 扫光 + 透视网格 + 电影黑边 + credit 条 |

每个变体接受 `heroContent?: React.ReactNode` prop，agent 传入自定义的前景主视觉（SVG 图形、数据可视化等），变体会将其放在合适的区域。

**共享组件**（变体内部已集成，也可单独使用）：
- `CoverText` — 逐字弹入标题 + shimmer，支持 `layout` prop（`"center"` / `"right-stack"` / `"bottom-left"`）
- `CoverOverlay` — 暗角遮罩（4 方向：to-bottom / to-top / center-out / to-left） + 噪点 + 可选扫描线
- `ShimmerSweep` — 光泽扫过效果，可用于任何需要 polish 的元素
- `CornerMeta` — 四角等宽字体元数据标签
- `coverThemes.ts` — 4 套色彩主题（tech / danger / blueprint / deck）

**封面组装模式**：

```tsx
// ✅ 直接用变体（最简方式），传入 heroContent 自定义前景主视觉
import { CoverLayered } from "./cover/CoverLayered";

export const SceneCover: React.FC = () => (
  <CoverLayered
    headline="视频标题"
    subtitle="副标题"
    tagline="钩子文案"
    theme="tech"
    heroContent={<MyHeroSVG />}  // 占左侧 60% 区域
  />
);

// ✅ 上下分区布局
import { CoverParticle } from "./cover/CoverParticle";

export const SceneCover: React.FC = () => (
  <CoverParticle
    headline="视频标题"
    theme="danger"
    heroContent={<DataChart />}  // 占上方 65% 区域
  />
);

// ❌ 错误：从零手写文字动画和布局
```

#### 前景主视觉的类型

| 类型 | 适用场景 | 示例 |
|------|---------|------|
| **结构可视化** | 架构/分层/选型类视频 | 三层同心轨道环（短期→会话→长期） |
| **核心隐喻图** | 概念/方法类视频 | 天平、漏斗、光谱轴（已在 svg/ 库中） |
| **动态数据图** | 数据/对比类视频 | 柱状对比图、雷达图、流程图 |

**无论哪种类型，主视觉元素应占画布 50%+ 面积，且位于内容层（z-index 高于背景）。**

#### 封面视觉层级（从后往前）

```
z-0  氛围背景（粒子/扫描光柱等，opacity < 0.3）
z-1  中心光晕（radial-gradient，呼应主色调）
z-2  ★ 前景主视觉（SVG 动画图形，占画布 50%+）
z-3  标题文字（叠在主视觉中心或上方，≥120px）
z-4  副标题 + 钩子徽章（底部或标题下方）
z-5  四角装饰
```

#### 封面 vs 内页的区别

| 维度 | 封面 | 内页 |
|------|------|------|
| 主视觉 | **必须有大面积动态图形** | 标题 + 布局组件 |
| 背景 | 可用独立特效（不依赖 SceneBackground） | 用 SceneBackground |
| 文字 | 极简：标题 + 副标题 + 1 条钩子 | 详尽：要点/卡片/公式 |
| 空间 | 图形占 50%+，文字叠在图形上 | 内容区占 70%+ |
| 动画 | 入场 spring + 主视觉持续动态 | 逐步构建 |

#### 常见错误与修正

| 错误 | 修正 |
|------|------|
| 8 层背景叠 + 纯文字前景 → "全是字" | 砍掉冗余背景层，加 1 个大面积前景主视觉 |
| 主视觉只占画布 20-30%（如 600px 轨道在 1920 画布上） | 放大到占 50%+（900px+），SVG viewBox 接近 1000 |
| 标题 84-100px 在大画布上像正文 | 标题 ≥120px，推荐 140-160px |
| 加了小 icon/徽章图但反馈"太细微" | 问题不是缺 icon，是缺大视觉；一个占画布一半的动态元素 > 十个小装饰 |
| 背景层很丰富但前景空洞 | 背景是对氛围的加分（10%），前景主视觉才是内容（90%） |

#### 自检清单

- [ ] 封面有 1 个前景主视觉元素，面积 ≥ 画布 50%
- [ ] 标题叠在主视觉上或紧邻主视觉，不是孤零零的文字块
- [ ] 主视觉可视化视频核心概念（不是纯装饰）
- [ ] 标题字号 ≥120px
- [ ] 遮住所有文字，图形本身能让观众猜到主题方向
- [ ] 使用了 `CoverText`（自带 shimmer）和 `CoverOverlay`（暗角/噪点），不是从零手写

### 尾页场景设计模式

skill 源码 `src/scenes/SceneEnding.tsx` 提供完整的尾页参考实现，采用上下分区构图：

```
上半区（55%）：结语文字 + "起点" 爆炸粒子
──── 全宽渐变分割线 ────
下半区（45%）：[logo 徽章]（左）  [CTA 按钮]（右）水平排列
底部：© footer 小字
```

**agent 生成尾页时应**：
1. 采用上下分区 + 水平 logo/CTA 布局，避免纯纵向堆叠
2. 复用 `ShimmerSweep`（从 `./cover/shared/ShimmerSweep` 导入）为 logo/CTA 增加光泽扫过
3. CTA 按钮使用磨砂玻璃风格（`backdropFilter: 'blur(12px)'` + 渐变半透明背景 + `borderRadius: 999`）
4. logo 徽章使用旋转 `conic-gradient` 边框
5. 用全宽渐变分割线视觉分隔文字区和行动区

### 色彩系统扩展

当场景涉及**多立场对立、辩论、光谱**时，仅有 `accent` 一色不够用。应在 `colors.ts` 中扩展辩论光谱色和图表辅助色：

```typescript
export const COLORS = {
  // 基础色（已有）
  bg: "#0a0a0b",
  text: "#f1efea",
  muted: "#a8a8a8",
  accent: "#e8a090",
  accentDim: "rgba(232, 160, 144, 0.20)",
  accentLit: "rgba(232, 160, 144, 0.05)",
  // 辩论光谱色（按需添加）
  debateFast: "#6ec87a",       // 快派绿
  debateMiddle: "#e8a090",     // 中间派（可复用 accent）
  debateSlow: "#7c8cf8",       // 慢派紫
  // 图表辅助色（按需添加）
  graphGrid: "rgba(241,239,234,0.06)",
  graphLine: "rgba(232,160,144,0.4)",
} as const;
```

**规则**：任何 ≥3 立场的场景必须使用对比色，禁止全用 accent 单色区分。光谱色应从绿→橙→紫渐变，对应 快→中→慢 的直觉映射。

### SRT 内容结构对齐

**场景步骤/分点必须与 SRT 口播内容分组对齐**，而非自行脑补逻辑分组。

常见错误：将 SRT 中属于"公式总结"的一条内容提拔为等权独立步骤。

正确做法：
1. 通读该场景对应的 SRT 条目范围
2. 识别哪些是**等权并列项**（可独立为步骤/卡片），哪些是**总结/公式**（应作为底部公式区或引用条）
3. 步骤数应与口播中独立动作/方法的数量一致
4. 如果口播说"两步 + 一个公式"，场景不应渲染为"三步"

示例：Nolan 公式 "读三遍 + 写测试 + Code Review" 在 SRT 中是总结公式的一部分，不应成为独立第三步。正确拆分为 2 步 + 1 公式区。

### 逐句心象法（设计入口）

**核心理念**：写每一页场景之前，先在脑中把对应 SRT 段落"放一遍"，逐句问自己：观众**听到这句话**的瞬间，**眼睛**希望在画面上看到什么动画、可视化或图形，才能帮助他直觉理解这句话？

口播提到的每一个**具体名词、动作、数字、对比、因果**，画面上都应有可被识别的视觉对应物。不必字字对应，但语义必须呈现——否则就是"耳朵在前，眼睛掉队"，观众潜意识觉得"画面跟讲的不是一回事"。这是观众流失最隐蔽的源头之一，比"画面太丑"更致命。

**实操步骤**（4 步可操作流程）：
1. **摘主词**：从该段 SRT 提取**具体名词、动作、数字、对比、因果**——不要复述大意，要逐条列字眼
2. **一句话造影像**：针对最关键的那几个字眼，写出"观众听到这一刻**最想看到**的具体画面"（如"AI 从云端落到客户的办公桌"），用具体物品/动作描述，不要抽象词
3. **3-5 候选 + 不同动作/物品**：围绕该影像产出 3-5 个候选，每个候选必须对应**不同动作或物品**（否则就是同一隐喻换皮）
4. **三条筛选标准**：① 遮住所有文字图形，能否猜到主题方向 ② 是否对应口播最关键的字眼（而非次要细节） ③ 是否有动作叙事（不是静态图标说明书）

**进入实施前**：候选数 ≥3 才开始动笔；候选只有 1 个 = 你套了模板，重新做第 1-2 步。

**与已有原则的递进关系**：
- 本节 = **设计入口**（动笔前的心智：观众想看到什么）
- 「SRT 叙事→视觉选型」 = **形态选择**（用哪种图形模式承载叙事）
- 「口播信息→视觉审计（SRT 密度对齐）」 = **交付审计**（写完核对画面是否覆盖了口播信息）

三者递进：**先想看什么 → 再选用什么 → 最后审有没有漏**。

### SRT 叙事→视觉选型

场景缺少图形时，**先读 SRT 提取叙事结构，再选图形模式**，不要凭空设计。

1. 读该场景对应 SRT 条目，识别口播的**叙事弧**（递进、对比、分层、循环、枚举等）
2. 叙事弧决定图形选型：有方向/因果关系的必须用带箭头的图形（流程图、漏斗等），并列无序的可以用卡片
3. 有递进关系的场景，**禁止用纯卡片堆叠**——递进必须用箭头/连线体现，否则观众无法直觉感知因果

### 架构图场景：具体内容 > 抽象形状

解释系统分层/架构时，**展示每层的具体内容物，而非仅用抽象几何形状 + 层名标签**。

抽象形状（倒梯形金字塔、同心圆、嵌套方块等）适合封面——传递"有层次"的直觉即可。但内页场景需要帮助观众理解**每层到底是什么**，仅标"短期/长期"不够。

**判断标准**：场景时长 >60s 且口播在解释各层细节 → 内页；该场景图形只用几何形状 + 层名 → 信号不够，需要填充内容物。

**内容物填充方式**（按场景选择）：
- **条目 pills/标签**：每层 3-5 个关键词条目（如"对话上下文"、"MCP Servers"）
- **容量/持久性 badge**：右侧标注量级（如"~200K tokens"、"文件级"）
- **职责注释**：每层一行短句说明干什么（如"易失 · 会话结束即清空"）

抽象形状 → 内容填充后，信息密度从 3 个层名提升到 3×(层名 + 副标题 + 4 条目 + badge + 注释)，观众能直觉理解每层"装了什么"。

### 分层架构必须展示层间数据流

任何分层/架构场景，**层与层之间不只画分隔线，还要画数据流向**。只有分隔线的分层图无法回答"层之间怎么协作"这个观众必然产生的疑问。

**数据流最小表达**：双向箭头 + 方向标签（如 ↑ retrieve / ↓ store）。方向标签用短英文（2-8 字符），保持视觉轻量。

**数据流动画**：箭头线出现后，沿箭头方向添加循环流动粒子（小圆点 + sin 衰减 opacity），观众直觉感知"数据在层间流动"。

**编排时序**：先逐层出现（结构建立），再出现数据流箭头（关系建立），最后粒子开始流动（运行态）。三阶段递进，不要同时出现。

### 二级渐进揭示

结构元素出现后，**其内部条目不应与结构同时出现**，而应延迟几帧逐一揭示。这创造了两级视觉节奏：

1. **一级**：结构框架出现（卡片/容器滑入）→ 观众建立空间认知
2. **二级**：框架内条目逐一出现 → 观众逐个吸收细节

如果结构和内容同时出现，观众需要在同一瞬间处理空间布局 + 内容含义，认知过载。二级揭示让每一步有 0.2-0.5 秒的认知缓冲。

**实现方式**：结构层 `interpolate(frame - i*offset, [...])`，内容层在结构动画完成后 10-15 帧开始，每个条目再错开 5-8 帧。

### 口播信息→视觉审计（SRT 密度对齐）

场景画面必须承载口播中提到的**所有结构化信息**，不能只放关键词而让观众"听见但看不见"。

**审计方法**：读该场景对应的 SRT / 01-script.md，逐条标注口播中提到的结构化信息（分类、层级、流程步骤、数据指标），然后对照场景组件——每一条都应有对应的视觉元素。如果口播说"七大类"但画面只写"7 大类"三个字，就是视觉密度不足。

**常见缺口信号**：
- 口播枚举了 N 个类别/步骤/指标，画面只放了"关键词卡片"而不展开具体内容
- 口播描述了 A→B→C 的流程或因果关系，画面只放并列文字
- 口播给出具体数字（百分比、价格、性能指标），画面只放概念名称

**修复手法**（按叙事结构选择）：
- 枚举/分类 → 放射图/网格/分层图，每项带 icon + 名称 + 子标签
- 流程/因果 → 带箭头的管线/流程图，节点间有方向连线
- 对比/选型 → 左右/上下分区图 + vs 标注或对比表 + 差异化标签
- 指标/数据 → 大字号数值 + 单位标签 + 趋势色

### 多 Phase 场景的视觉一致性

当一个场景内部按 Phase 路由（如 s7 有 5 个 Phase），**升级其中任意一个 Phase 的视觉表达后，必须审计其余 Phase 是否出现视觉落差**。

**原则**：同一场景内的多个 Phase 应在视觉密度上保持同一水平。一个 Phase 用了全幅 SVG + 动画，相邻 Phase 还在用纯文字卡片，会产生明显的粗糙/精细割裂感。

**审计清单**（升级某 Phase 后执行）：
1. 遍历其余 Phase，逐个判断：当前是纯文字卡片还是图形化？
2. 如果有 Phase 仍是纯文字卡片：读对应 SRT，判断口播中是否有结构化信息可图形化
3. 已有图形但不充分（如表格缺差异化行、流程缺数值指标）：补齐信息密度
4. 对比表本身是合适的呈现形式（不强制 SVG），但需配差异化标签/总结行

### SVG 边界校验：viewBox 必须容纳所有元素

SVG 放射图/流程图中，节点和标签的位置可能因角度和偏移量超出 viewBox 边界，导致截断。**写完 SVG 后必须静态校验坐标范围**。

**校验步骤**：
1. 列出所有节点的 (x, y) 坐标 + 元素尺寸（圆半径、矩形宽高、文字偏移）
2. 计算每个元素的 bbox 上下左右极值
3. 确认所有极值在 viewBox (0, 0, width, height) 范围内
4. 特别注意：放射图顶/底部节点（角度 ±90°）的子标签容易向上/向下溢出

**常见截断场景**：
- 放射图顶部节点：`y - offset` < 0
- 放射图底部节点：`y + offset + textHeight` > viewBox height
- 右侧节点子标签 pill：`x + pillWidth` > viewBox width

**修复方式**：调整中心点 y 偏移、缩小 radius、或将远距离子标签改为紧贴节点（消除 labelDist 远距离偏移）。

### 布局由图形决定，不由习惯决定

选择布局时，核心判断是**图形的空间需求**：

- 图形需要横向展开（流程、光谱、时间轴等）→ 给图形全幅空间，文字要点放底部/顶部条
- 图形需要纵向分层（金字塔、漏斗等）→ 左图右文
- 其他空间需求 → 按图形形态自定布局

**不要把"左图右文"当默认**。横向展开的图形被塞进分栏窄侧，是画布空白的头号原因。

### "元素太小"反馈的排查优先级

收到"元素太小/画布空白多"反馈时，按优先级排查：

1. **布局是否匹配图形形态** — 横向图形是否被挤进了分栏窄侧？优先改布局
2. **SVG viewBox 是否接近可用内容区** — 1920×1080 去 padding 后内容区约 1760×700-880，viewBox 不应远小于此
3. **容器 width% × viewBox × 内部几何参数三因子联动** — 这是"太小"最常见且最隐蔽的成因：容器 CSS width 设小了（如 70%）、viewBox 也偏小、内部几何参数（R、nodeR、fontSize）全部按小坐标系写的，三者叠加导致内容被缩了两遍。修复时三者必须联动放大：容器改到 95%/100% → viewBox 放大到接近内容区 → 内部参数按相同比例放大。只调字号不调几何参数，本质是坐标系没变只在里面写大字，仍然填不满画布。**特别注意**：SVG 组件通过 `width/height` prop 接收外部尺寸但内部 `viewBox` 写死较小值时，放大外部容器只是把小画布拉伸显示——pill 间距、层间 gap、层高等几何常量不会跟着变，视觉上元素仍然小。必须同时改 viewBox 和内部所有几何常量（y 坐标、wTop/wBot、h、pillSpacing、pill 尺寸、fontSize）。
4. **节点/图标尺寸** — 只调字号不调几何通常治不了本

### 画布空间利用率

**1920×1080 画布主要内容区域应占 ≥70% 面积**。常见空白过多的问题及解决方案：

| 问题 | 解决方案 |
|------|---------|
| SVG viewBox 过小（如 700×860），实际渲染只占画布 40% | 放大 viewBox 至接近画布尺寸（960+），或让 SVG `width="100%"` 自适应 |
| 漏斗/光谱等水平元素居中窄带 | 让图形撑满可用宽度，左图右文布局中图表占 55-60% |
| 三栏卡片内容只有标题+关键词，大量空间空白 | 每栏增加 1-2 行核心观点，字号用 `FONT_SIZE.subtitle`/`FONT_SIZE.body` |
| SVG 内文字过小（12-22px） | 参照字号硬约束表，SVG `<text>` ≥28px，3字以上 ≥32px |
| 节点图/图标尺寸过小 | 图标 radius 36→44+，节点圆 r 18→22+，连线 strokeWidth 2→3+ |

**自检方法**：在 Remotion Studio 预览中，如果场景中央有超过 1/4 画布面积的空白区域（无文字、无图形、无装饰），需要重新布局。

### SVG viewBox 默认策略

占画布 ≥50% 的主视觉 SVG 图形，推荐直接使用画布坐标系：

```tsx
<svg viewBox="0 0 1920 1080"
  style={{ position: "absolute", inset: 0, width: "100%", height: "100%" }}>
```

小 viewBox（如 `1000×700`）配合 CSS 缩放（`width: 1600px; top: 100px`）是「太小」和「底部溢出」同时出现的头号成因——容器 × viewBox × 几何参数三因子耦合，改一个另外两个不跟着动。全画布 viewBox 把坐标系和渲染区域统一，1px SVG = 1px 画布，所见即所得，彻底绕开三因子问题。

何时用小 viewBox：局部装饰性 SVG（占画布 <30%）、需要独立缩放的可复用组件（如 shared/svg/ 里的工具）。

### 圆形/放射布局的中心点预算

环形图、hub-spoke 图的中心 Y 不要放在画布正中 (540)——底部节点 + 标签 + 描述文会溢出画布。

**预算方法**：`CY = 540 - bottomLabelBudget / 2`。底部标签通常占 nodeR + 间距 + 2 行中文（≈120-160px），所以 CY 通常落在 **400-480** 范围。最终校验：所有元素最低点 ≤ y=1020（留 60px 安全边距）。

### 底栏信息密度 = 画布效率

底栏标签如果只放关键词（keyword pill/tag），在画布高度允许的情况下是空间浪费。应升级为 **icon + 颜色标识 + 一行描述** 的 card 格式——不增加画布高度占用，但信息密度从"5 个词"提升到"5 句话"。

**判断标准**：底栏高度 < 画布 15% 且只有 pill 标签 → 升级为卡片；底栏已经占 >25% 或行数 ≥3 → 保持简洁标签。

### Step 7: Render

```bash
npm run render        # → production/videos/final_remotion.mp4（无字幕）
npm run burn-sub      # → production/videos/final_remotion_sub.mp4
npm run render:full   # 上述两步串联
```

Remotion 一步输出含音频的 H.264；**不含硬字幕**，须 Step 8。

## 跨平台支持

### 平台依赖注入

`cli.mjs init` 自动检测当前 `os.platform() + os.arch()` 并注入对应的 `@remotion/compositor-xxx` 依赖：

| 平台 | 架构 | compositor 包 |
|------|------|--------------|
| macOS | arm64 | `@remotion/compositor-darwin-arm64` |
| macOS | x64 | `@remotion/compositor-darwin-x64` |
| Linux | x64 | `@remotion/compositor-linux-x64-gnu` |
| Windows | x64 | `@remotion/compositor-win32-x64-msvc` |

若跨平台部署，需手动安装目标平台的 compositor 包。

### OpenGL 渲染器

`remotion.config.ts` 按平台自动选择 OpenGL 渲染器：

```typescript
const glRenderer = process.platform === "linux" ? "swiftshader" : "angle";
Config.setChromiumOpenGlRenderer(glRenderer);
```

- **macOS / Windows**：`angle`（Apple Silicon 和主流 GPU 推荐）
- **Linux（CI / 无 GPU）**：`swiftshader`（软件渲染，兼容性好）
- 遇到渲染黑帧或崩溃时，可手动改为 `"swangle"` 或删除此行让 Remotion 自动决定

### 已知环境陷阱

参见 `knowledge/standards/dev-env-traps.md`，常见问题：

- **SOCKS 代理导致 AbortError**：系统配置了 SOCKS 代理（如 `127.0.0.1:13659`）时，Remotion Studio 对 `bugs.remotion.dev` 的版本检查请求会失败，控制台报 `mediabunny.cjs AbortError` + `inspector.js Failed to fetch`。这不影响预览/渲染/导出，忽略即可，或把 `bugs.remotion.dev` 加入代理例外

### Step 8: Burn subtitles

字幕烧录由本 skill 内置的 `lib/subtitle_burn.js`（SRT→ASS + ffmpeg）完成，依赖 npm 包 `ffmpeg-static`，无外部 skill 依赖。`burn-subtitles.mjs` 先找同包 `../lib/`，feature 部署场景下向上查找仓库根的 `skills/remotion-pipelines/lib/`。

要求：
- `production/subtitles/sub.srt`
- `production/timing/wav-durations.json`（`burnSubtitles: true`、`subtitle` 样式）

`subtitle` 字段说明（默认值同 `ass_colours.js` 的 `DEFAULT_SUBTITLE_STYLE`）：

| 字段 | 默认 | 说明 |
|------|------|------|
| `fontSize` | 60 | 1080p 下 ≥60 可读；42 太小不可用 |
| `charsPerLine` | 22 | 字号越大每行字数越少，`fontSize=60` 时 22 字/行为宜 |
| `barHeight` | 0 | 底部半透明暗栏高度；0=不绘制（推荐，画面更干净） |
| `barAlpha` | 0.55 | 仅 `barHeight>0` 时生效 |
| `primaryColour` | `#FFD700` | 金色字幕，深色画面对比度好 |
| `outline` | 0 | 不用描边，避免脏黑边；靠 barHeight 或字号保证可读 |

## audioConfig.ts 格式

中视频由 **scene-boundaries.md** 驱动，手填或与 `slideDurationsSec` 同步。  
短视频可由 `generate-audioconfig.mjs` 自动生成。

```typescript
export interface SceneConfig {
  id: string;
  title: string;
  durationInFrames: number;
  audioFile: string;
}

export const SCENES: SceneConfig[] = [...];
export function getSceneStart(sceneIndex: number): number { ... }
export const TOTAL_FRAMES = ...;
export const FPS = 30;
```

格式与 `repos/reference/remotion-video-skill` 的 audioConfig.ts 完全一致。

## Audio 适配层

`scenes/index.tsx` 自动判断模式：

```typescript
const isSingleAudio = new Set(SCENES.map(s => s.audioFile)).size === 1;
```

- **single（策略 A）**：一个 `<SafeAudio src={SCENES[0].audioFile} />` 播放完整 WAV
- **split（策略 B）**：每个 `<Sequence>` 内嵌 `<SafeAudio src={scene.audioFile} />`

### SafeAudio 容错组件

**问题**：Remotion `<Audio src={staticFile("audio/voiceover.wav")} />` 在文件不存在时抛 404 错误，Studio 覆盖层堵死整个 UI，无法查看画面。这在开发早期（音频尚未生成）反复发生。

**方案**：`shared/components/SafeAudio.tsx` — HEAD 探活 + Error Boundary 双保险：
- Studio 模式下 `fetch(url, {method: "HEAD"})` 检测文件是否存在，不存在则渲染 `null`
- Error Boundary 兜底，即使 HEAD 通过但实际加载失败也不崩溃
- render 模式下跳过探活（假定文件存在），不拖慢渲染速度

```tsx
import { SafeAudio } from "../shared/components/SafeAudio";

// 替代 <Audio src={staticFile("audio/voiceover.wav")} />
<SafeAudio src="audio/voiceover.wav" />
```

切换策略只需重新运行 `generate-audioconfig.mjs`，Scene 代码不用动。

## 场景组件开发

参考 `repos/reference/remotion-video-skill/SKILL.md` 中的完整指南，包括：
- 3B1B 风格：Why→What、逐步构建、颜色语义化
- 过程动画：StepByStep、ValueFlyIn、SlidingWindow
- Scene 组件原则：单一职责、独立动画、延迟出现

### 添加新场景

1. 创建 `src/scenes/SceneXxx.tsx`
2. 在 `src/scenes/index.tsx` 的 `SceneRouter` 中添加 case：

```tsx
function SceneRouter({ id, index }: { id: string; index: number }) {
  switch (id) {
    case "01": return <SceneCover />;  // 使用 cover/ 共享组件组装
    case "02": return <SceneConcept />;
    // 添加新场景...
    default: return <PlaceholderScene index={index} />;
  }
}
```

> 封面场景使用 `src/scenes/cover/shared/` 的 CoverText、CoverOverlay、ShimmerSweep 等共享组件组装，
> 不要从零手写文字动画和视觉效果。详见「封面场景设计模式 → 封面组件库」。

### 向 shared 贡献积木

做完一条视频后，把可复用的组件提炼到 `src/shared/`：
- 布局模式 → `shared/layouts/`
- SVG 动画工具 → `shared/svg/`（useStrokeReveal, useGrowIn, usePulse, useCountUp, useBarGrow, useGlowPulse）
- 视觉组件 → `shared/components/`（SceneTitle, GradientBar, StatCard, KpiNumber — 均调色板无关，传色入参）
- 动画工具 → `shared/animations/`（含 anime/、gsap/、lottie/ 子目录）
- 背景系统 → `shared/SceneBackground.tsx`
- 色彩/字体 → `shared/colors.ts`, `shared/typography.ts`

- 封面组件 → `scenes/cover/shared/`（CoverText, CoverOverlay, ShimmerSweep, CornerMeta, coverThemes）

场景专用 SVG 图形组件放在 `src/scenes/svg/`，**不进 shared**（太特定，如图标、漏斗等随主题变化）。

下次 `init` 新项目时，这些积木会自动复制过去。

### 场景背景系统 (SceneBackground)

**优先级**：每场景的背景**优先按当页主隐喻构想专属 aura**（与前景内容同主题的氛围层）；构想不出主题、或时间紧时，再用通用 `SceneBackground` 兜底。**内页不得使用纯黑静态背景**。

通用 `SceneBackground` 提供的 6 件套（粒子/电路/轨道/六边形/数据流/扫描光柱）是**通用素材**——所有 AI/科技主题视频套上去看起来都一样。当一整条视频每场都用同一套通用背景，整条视频会失去辨识度。专属 aura 不必复杂——一组与主隐喻同主题的漂浮元素或渐变即可（如"云雾/拼图碎片/锁链/星空/图纸格栅"），各自只在本视频内部使用，无须沉淀到 shared/。

```tsx
import { SceneBackground } from "../shared";

// 内页兜底（未构想专属 aura 时）
<SceneBackground intensity={0.5} />

// 强调页（总结/过渡）
<SceneBackground intensity={0.6} scanBeam={true} />

// 含大量 SVG 内容的页面，关闭六边形避免视觉冲突
<SceneBackground intensity={0.5} hexGrid={false} />
```

**组件包含的动态元素**（全部 `zIndex: 0`，不遮挡内容层）：

| 元素 | 参数 | 说明 |
|------|------|------|
| 粒子连线 | `particles` (默认 true) | 32 个浮动圆点 + 连线 |
| 电路痕迹 | `circuits` (默认 true) | 四角折线 + 端点呼吸光环 |
| 轨道环 | `orbits` (默认 true) | 3 圈虚线环 + 光弧 + 亮点 |
| 扫描光柱 | `scanBeam` (默认 false) | 从左到右的倾斜光柱 |
| 数据流 | `dataStreams` (默认 true) | 8 条水平飘动短线 |
| 六边形网格 | `hexGrid` (默认 true) | 仅铺边缘区域，中心留空 |

#### 暗色背景透明度校准（关键陷阱）

在 `#0a0a0b` 暗色背景上，SVG 元素最终显示 opacity = **baseOpacity × intensity × fadeFactor**。其中 fadeFactor 来自 `interpolate` 动画（通常 0~1）。

**核心教训**：`intensity` 默认 0.5，乘以一个"直觉上合理"的 baseOpacity 0.12，结果只有 0.06——在暗色背景上完全不可见。

**校准规则**：设 `intensity=0.5` 时，各元素应**清晰可辨**。baseOpacity 应设定为：

| 元素 | 错误基数 (×0.5 后不可见) | 正确基数 (×0.5 后可辨) |
|------|--------------------------|------------------------|
| 粒子连线 | 0.12 → 0.06 | **0.25** → 0.125 |
| 粒子圆点 | 0.15~0.45 | **0.30~0.80** |
| 电路痕迹 | 0.35 | **0.50** |
| 轨道主线 | 0.08 | **0.15** |
| 轨道光弧 | 0.20 | **0.40** |
| 数据流 | 0.18 | **0.30** |

**自检方法**：在 Remotion Studio 中跳到任意内页场景的第 50 帧左右（动画完成进场后），如果看不到至少 3 种动态元素（粒子/连线/轨道/电路/数据流），则 baseOpacity 校准不足。

#### svg/ 目录审计规则

写场景组件前，**必须**先检查 `src/scenes/svg/` 下是否已有可复用的 SVG 图形组件。常见遗漏：

```bash
# 开发前检查
ls src/scenes/svg/
```

| 规则 | 原因 |
|------|------|
| 优先用 `svg/` 中已有组件替代纯文字卡片 | SVG 图形在 1080p 视频中更有表现力 |
| 图片 `objectFit: "contain"` 导致两侧空白 | 替换为 SVG 组件可自适应充满区域 |
| 10+ 秒的纯文字页面至少配一个 SVG 装饰 | 避免静态画面导致观众流失 |
| `SceneBackground` 不能替代场景图形 | 背景是氛围层，图形是内容层，二者互补 |

## CLI 速查

```bash
# 创建新视频项目
node cli.mjs init <project-dir>

# 在项目内生成 audioConfig（策略 A）
node scripts/generate-audioconfig.mjs --srt script.srt --audio voiceover.wav

# 在项目内生成 audioConfig（策略 B）
node scripts/generate-audioconfig.mjs --srt script.srt --audio voiceover.wav --mode split

# 预览
npm run dev

# 渲染（在 feature/remotion/ 目录）
npm run render
npm run burn-sub
npm run render:full

# 按场出代表帧（每场内进度 80%，用于 layout 自检）
npm run verify-stills                       # 全部场 → verify/s00-<id>.png ...
npm run verify-stills -- --scene 0,3,6      # 只渲指定场
npm run verify-stills -- --ratio 0.5        # 改场内进度比
npm run verify-stills -- --frames 400,7500  # 直接给绝对帧
npm run verify-stills -- --parallel 4       # 并发数
```

## 反模式

| 不要 | 原因 |
|------|------|
| 中视频直接 `generate-audioconfig --srt` | 一条字幕一场景，页数爆炸，与口播话题不符 |
| 只跑 `render` 就交付 | Remotion 不烧字幕，观众看不到 sub.srt |
| 沿用旧 html-deck 的 `slideDurationsSec` 不重算 | 画面与播客口播错位 |
| Scene 里用 12–14px 小字 | 手机/压缩后不可读 |
| `Config.setChromiumOptions({ gl })` | 已废弃；用 `Config.setChromiumOpenGlRenderer()`，见 remotion.config.ts 跨平台逻辑 |
| 在 `<svg>` 里画 `Q midX midY` 弧线时 `midY = anchorY + curve` 而 `curve` 大于"锚点到下方卡片"的间距 | 弧线最低点会扎进底部金句卡内部（s3 实测 curve=200/120 都越界） |
| `<svg>` 汇聚线 `y1` 起点落在 Applet/Card 的 bbox 内 | dashed 线从卡片内部冒出（s8 实测 `y1=620` 落在 Applet `[460,696]` 内） |
| 底部金句卡按"单行"算高，但实际 fontSize 38 + 中文 ~42 字会折两行 | 金句卡顶上抬 ~50px，与 FlowStep 底重叠（s5 实测） |
| 1920 画布上 `fontSize: 200` 的英文标语放进 `width: 48%` 左栏 | 字宽 ≈10×0.55em×200 ≈1100px > 845px，向右溢出压到右栏（s0 实测 `/workflows`） |
| 单卡内 monospace 命令行字号 26px，命令字符 ≥30 | 480px > 卡内宽，折行或裁切（s4 实测 `export CLAUDE_CODE_WORKFLOWS=1`） |
| 字幕 `fontSize=42` + `barHeight=80` | 字太小不可读，黑色半透明横条遮挡画面且不美观；改为 `fontSize=60, barHeight=0`（实测验证） |
| 中心徽章 `top` 与底部说明卡 `bottom` 计算后只差 ~2px | 视觉贴边，等同重叠（s8 实测汇聚徽章 vs 复用条仅 2px 间隙） |
| 场景只放纯文字卡片，无图形/视觉隐喻 | 1080p 视频缺乏表现力，观众无法直觉理解；必须配 SVG 图形主视觉 |
| 设计场景时不先读 SRT 默念、不思考"观众听到此句希望看到什么" | 观众"耳朵在前、眼睛掉队"——听到了具体名词/动作/数字/对比，画面却只有泛泛关键词，潜意识觉得"画面跟讲的不是一回事"；参见"逐句心象法" |
| 口播枚举了具体类别/步骤/指标但场景只放关键词 | 视觉密度不足；展开口播中的结构化信息（类别→放射图、步骤→流程图、指标→数值卡），参见"口播信息→视觉审计" |
| 多 Phase 场景升级了某 Phase 的视觉但忽略其余 Phase | 同场景内 Phase 间视觉密度割裂；升级任一 Phase 后必须审计其余 Phase，参见"多 Phase 场景的视觉一致性" |
| SVG 放射图顶部/底部节点的子标签超出 viewBox | 写完 SVG 后静态校验坐标范围，调整中心点偏移或消除远距离 labelDist，参见"SVG 边界校验" |
| SVG `<text>` fontSize < 28px | 1080p 下不可读；3字以上标签必须 ≥32px |
| 光谱/辩论场景全部用 accent 一色 | ≥3 立场必须用对比色（debateFast/debateMiddle/debateSlow） |
| 场景步骤数与 SRT 口播不符（如公式项被提拔为等权步骤） | 步骤结构必须与 SRT 内容分组对齐，公式/总结不能膨胀为独立步骤 |
| 画布中心大面积空白（>25% 无内容区域） | 增大 SVG viewBox/节点尺寸/文字字号，或增加内容填充 |
| SVG viewBox 尺寸远小于画布可用宽度 | viewBox 应接近可用区域宽度（960+），或设 `width="100%"` 自适应 |
| 收到"太小"但只调 SVG fontSize 不调 viewBox/容器/几何参数 | 三因子联动放大：容器 width% → viewBox 尺寸 → 内部参数（R、nodeR、fontSize、strokeWidth）同比例放大，缺一则缩放不足 |
| SVG 组件接收外部 width/height prop 但内部 viewBox 未同步放大 | 放大容器只是把小 viewBox 拉伸显示——结构间距和元素尺寸不会变大，等于放大了空白。**修 SVG 组件"太小"必须改 viewBox + 内部所有几何常量**（层 y/wTop/wBot/h、pill 宽高、pillSpacing、fontSize），而非只改外部容器 |
| 底栏只放 keyword pill 标签，画布高度有富余 | 升级为 icon + 颜色标识 + 描述文的 card，信息密度从"5 个词"到"5 句话"，高度占用几乎不变 |
| SceneBackground baseOpacity × intensity 结果 < 0.08 | 暗色背景上不可见；baseOpacity 应校准到 intensity=0.5 时清晰可辨，参考上方透明度校准表 |
| 内页场景未使用 SceneBackground（纯黑静态背景） | 每页必须有动态背景氛围，专属 aura 优先，`<SceneBackground intensity={0.5} />` 兜底 |
| 直接用 `<Audio src={staticFile(...)}>` 而非 `<SafeAudio>` | 音频文件缺失时 404 错误覆盖层堵死 Studio 预览；用 `<SafeAudio>` 容错，缺文件时静默跳过 |
| 一整条视频每场都套同一个通用 SceneBackground（粒子/电路/轨道/六边形 6 件套） | 通用素材让所有 AI 视频长得一模一样，失去辨识度；应优先按当页主隐喻构想专属 aura，通用 SceneBackground 仅作兜底，参见"场景背景系统" |
| `svg/` 目录有已实现的 SVG 图形组件但场景未引用 | 写场景前必须审计 `ls src/scenes/svg/`，优先用 SVG 替代纯文字卡片和 `objectFit:contain` 图片 |
| 封面只有文字 + 多层低透明度背景（粒子/电路/六边形等），无前景大视觉 | 封面必须有占画布 50%+ 的前景主视觉图形（结构可视化/隐喻图/数据图），不能靠背景层凑画面 |
| 封面前景图形仅占画布 20-30%（如 600px 元素在 1920 画布上） | SVG viewBox 接近 1000，渲染 width ≥900px；标题 ≥120px；参考"封面场景设计模式" |
| 封面标题 <120px | 1920×1080 画布上标题应 ≥120px，推荐 140-160px |
| 反馈"加图形/动画"后只加了几处小 icon/微装饰 | "加图形"意味着需要大面积前景视觉，不是小点缀；一个大的动态主视觉 > 十个微装饰 |
| 有递进/因果关系的场景用纯卡片堆叠 | 递进关系必须用箭头/连线体现，不能靠并列卡片暗示因果 |
| 横向展开的图形（流程图/时间轴等）被塞进左图右文的窄侧 | 布局由图形空间需求决定，横向图形给全幅，文字要点挪到顶部/底部 |
| 架构分层场景只用抽象几何形状（金字塔/同心圆）+ 层名标签 | 封面可用抽象形状；内页必须填充每层的具体内容物（条目 pills、badge、职责注释），参考"架构图场景：具体内容 > 抽象形状" |
| 分层架构图只画层分隔线、不画层间数据流 | 分层必须展示数据流向（双向箭头 + 方向标签 + 流动粒子），只有分隔线无法回答"层之间怎么协作" |
| 结构框架和内部条目同时出现 | 用二级渐进揭示：框架先出现（一级），条目延迟 10-15 帧逐一出现（二级），避免认知过载 |
| 全屏 SVG 用小 viewBox（如 `1000×700`）+ CSS 缩放定位 | 三因子耦合的根源；全屏图形直接用 `viewBox="0 0 1920 1080"` + `inset: 0`，参见「SVG viewBox 默认策略」 |
| 圆形/放射布局的中心 Y 放在画布正中 (540) | 底部节点 + 标签 + 描述文溢出画布；CY 应上移至 400-480，参见「圆形/放射布局的中心点预算」 |
| Phase 2 用 `scale(N)` 放大某列，但覆盖层坐标按未缩放位置写 | `scale(N)` 使子元素的画布 Y = groupY + localY × N；localY=200 在 scale(1.5) 后到达 Y=600，但"验证闸门"等固定覆盖层写在 Y=640 → 只差 40px 直接重叠。**写完 scale 场景后必须算展开坐标**：每个子元素的画布 bbox = (groupTranslateX + localX × scale, groupTranslateY + localY × scale)，覆盖层 Y 起点须 ≥ 展开后最低元素底部 + 40px |
| 多 Phase 场景 Phase N 元素在 Phase N+1 开始后仍可见，无淡出 | Phase 2 的覆盖层（如"验证闸门""每次循环带上失败信息"）与 Phase 3 底部条在画布上冲突。每个 Phase 的专属覆盖层必须在下一 Phase 开始时 `interpolate(frame, [PHASE_N+1_START, PHASE_N+1_START+60], [1, 0])` 显式淡出 |
| `interpolate(strokeDashoffset, [pathLen*0.1, 0], [0, 1])` 输入范围递减 | Remotion `interpolate` 要求 inputRange **严格递增**，`strokeDashoffset` 从 pathLen 递减到 0，直觉写 `[大, 0]` 会运行时崩溃。正确写法：`interpolate(offset, [0, pathLen*0.1], [1, 0])`（翻转输入和输出） |
| 把素材公众号/作者 nick、`@…` 写进封面底部或内页 pill | 观众会以为你在帮二道贩子引流；出处只进 `spec` / `05-publish-kit` / `content/source`，画面仅本频道固定句 + 本期内容 |
| 从 `_weixin_raw.html` 的 `nick_name` 自动生成「致谢素材号」 | meta 是采集痕迹，不是画面文案；见 `references/source-attribution-boundaries.md` |

## 四轨动画系统

### L1 Scene 内动画 — 四轨并存

```
┌─────────────────┐ ┌──────────────────┐ ┌──────────────┐ ┌──────────────┐
│ A. Remotion 原生 │ │ B. Anime.js      │ │ C. GSAP      │ │ D. Lottie    │
│ interpolate /   │ │ useAnimeSeek /   │ │ useGSAPTime- │ │ <Lottie> /   │
│ spring（默认）   │ │ useAnimeTimeline │ │ line         │ │ useLottieSeek│
└─────────────────┘ └──────────────────┘ └──────────────┘ └──────────────┘
```

**Anime 侧重**: SVG 路径动画（`createDrawable`、`morphTo`、`createMotionPath`）和文本动画（`splitText`、`scrambleText`），API 比 GSAP 更简洁。

**GSAP 侧重**: Timeline 生态和插件广度（`MorphSVGPlugin`、`MotionPathPlugin`、`SplitText`），复杂编排。

**Lottie 侧重**: AE 设计师交付的 .json 预制动画、品牌 logo、角色动画、任何"手画 SVG 成本远高于 AE 导出"的动效。**仅在真正有 AE 资源时引入**，不替代手画 SVG 视觉隐喻。

**单 Scene 择一引擎**，禁止同根 DOM 节点挂双引擎 hook（seek 冲突）。Lottie 与其他三轨可并存（不同根节点），但同一 Lottie 容器内不再叠加 Anime/GSAP。

### 四轨决策表

| 场景 | 用 Remotion 原生 | 用 Anime（seek） | 用 GSAP（seek） | 用 Lottie |
|------|------------------|------------------|-----------------|-----------|
| 标题 spring 入场、单值 opacity | ✅ | | | |
| 单参数 `frame → number` | ✅ | | | |
| SVG 描线 `createDrawable` | | ✅ | GSAP DrawSVG 也可 | |
| SVG 路径变形 `morphTo` | | ✅（唯一选择） | GSAP MorphSVG 也需插件注册 | |
| SVG 沿路径运动 `createMotionPath` | | ✅ | GSAP MotionPath 也需插件 | |
| grid/from-center stagger | | ✅（API 最简洁） | GSAP 也可但配置多 | |
| 逐字/分词 `splitText` | | ✅（API 轻量） | GSAP SplitText 也可 | |
| 打字乱序 `scrambleText` | | ✅（唯一选择） | GSAP 无等价 | |
| 多步骤 Timeline 编排 | | ✅（简单编排） | ✅（复杂编排，插件生态广） | |
| 仅借 easing 曲线 | ✅（`animejs/easings` + Remotion `Easing`） | 或 Anime | 或 GSAP | |
| AE 设计师交付 .json 动画 | | | | ✅（唯一选择） |
| 品牌 logo / 角色动画 / 复杂手绘动画 | | | | ✅ |
| 简单 SVG 描线/弹入/脉冲 | ✅（用 shared/svg 三件套） | | | ❌（杀鸡用牛刀） |

### Anime.js 可选层

**集成模式**：`autoplay: false` + 每帧 `seek(sceneLocalMs(frame, fps))`，遵循 [Remotion 第三方集成文档](https://www.remotion.dev/docs/third-party)。

**npm 包**：`animejs@^4.4.1`（MIT），**非** `@animejs/...`。

**参考源码**：`repos/reference/anime`（v4.4.1，只读对照）。

#### 可用模块

| 模块 | 状态 | 说明 |
|------|------|------|
| `animate` | ✅ 可用 | 核心单次动画 |
| `createTimeline` | ✅ 可用 | 多步骤编排 |
| `stagger` | ✅ 可用 | 与 animate/timeline 联用 |
| `svg.createDrawable` | ✅ 可用 | SVG 描线动画 |
| `svg.morphTo` | ✅ 可用 | SVG 路径变形 |
| `svg.createMotionPath` | ✅ 可用 | SVG 沿路径运动 |
| `easings` | ✅ 可用 | 可单独 import |
| `text.splitText` | ⚠️ P2 | 逐字分割，需验证 Remotion 无头渲染兼容性 |
| `text.scrambleText` | ⚠️ P2 | 文字打乱，**须传 `seed` 参数**保证确定性 |
| `scroll` / `draggable` / `waapi` | ❌ 禁止 | 无头 Chromium 无用户交互 / WAAPI 不可靠 |

#### Hook API

**`useAnimeSeek`** — 单次 `animate()` seek 模式：

```tsx
import { useCallback } from "react";
import { animate, stagger } from "animejs";
import { AbsoluteFill } from "remotion";
import { useAnimeSeek } from "../shared/animations/anime";

export const SceneStagger = () => {
  const build = useCallback((root) => {
    return animate(root.querySelectorAll(".card"), {
      translateY: [40, 0],
      opacity: [0, 1],
      delay: stagger(80),
      duration: 600,
      autoplay: false,
      easing: "outExpo",
    });
  }, []);

  const ref = useAnimeSeek(build);

  return <AbsoluteFill ref={ref}>{/* .card 元素 */}</AbsoluteFill>;
};
```

**`useAnimeTimeline`** — `createTimeline()` seek 模式：

```tsx
import { useCallback } from "react";
import { stagger, svg } from "animejs";
import { AbsoluteFill } from "remotion";
import { useAnimeTimeline } from "../shared/animations/anime";

export const SceneSVGDemo = () => {
  const build = useCallback((tl, root) => {
    tl.add(svg.createDrawable(root.querySelectorAll(".line")), {
      draw: ["0 0", "0 1"],
      duration: 700,
      delay: stagger(60),
    })
    .add(root.querySelectorAll(".node"), {
      opacity: [0, 1],
      duration: 500,
      delay: stagger(80, { from: "center" }),
    }, "-=300");
  }, []);

  const ref = useAnimeTimeline(build);

  return <AbsoluteFill ref={ref}>{/* SVG 内容 */}</AbsoluteFill>;
};
```

**`sceneLocalMs(frame, fps)`** — 帧号转毫秒工具：

```ts
import { sceneLocalMs } from "../shared/animations/anime";
const ms = sceneLocalMs(frame, fps); // = (frame / fps) * 1000
```

### Lottie 可选层

**何时用 Lottie**：
- AE 设计师交付的 `.json` 动画文件（lottiefiles.com / Bodymovin 导出）
- 品牌 logo、角色动画、复杂手绘动画
- 用 SVG hooks（useStrokeReveal/useGrowIn/usePulse）成本远高于直接复用预制动画

**何时不用**：
- 简单 SVG 描线/弹入/脉冲 → `shared/svg` 三件套
- 文字动画 → Anime `splitText`/`scrambleText`
- 多步骤编排 → Anime/GSAP Timeline
- 一时找不到 `.json` 资源时凭空"加 Lottie 让画面丰富"——这是典型反模式

**npm 包**：
- `@remotion/lottie@^4.0.0`（与 `remotion` 主版本必须一致）
- `lottie-web@^5.12.0`（peer 依赖，必须显式安装）

**推荐 API：`<Lottie />` 组件**

`@remotion/lottie` 已经是 frame-driven 组件，多数场景直接用即可，无需 hook：

```tsx
import { Lottie } from "@remotion/lottie";
import brandLogoJson from "./public/brand-logo.json";

export const SceneIntro = () => (
  <Lottie
    animationData={brandLogoJson}
    style={{ width: 500, height: 300 }}
    loop={false}
    playbackRate={1}
  />
);
```

**进阶 API：`useLottieSeek` hook**

需要直接操作 `lottie-web` AnimationItem 实例（`playSegments`、子图层引用、动态修改属性等）时使用：

```tsx
import lottie from "lottie-web";
import { AbsoluteFill } from "remotion";
import { useLottieSeek } from "../shared/animations/lottie";
import characterJson from "./public/character.json";

export const SceneCharacter = () => {
  const ref = useLottieSeek((root) => {
    return lottie.loadAnimation({
      container: root,
      renderer: "svg",
      loop: false,
      autoplay: false,
      animationData: characterJson,
    });
  });
  return <AbsoluteFill ref={ref} />;
};
```

**资源放置**：
- 小文件（<200KB）直接 `import json from "./xxx.json"`，Remotion 打包时静态引入
- 大文件用 `staticFile("animations/xxx.json")` + `fetch`，需配合 `delayRender()` 避免渲染空帧
- 禁止 `fetch` 远程 URL：无头 Chromium 离线渲染场景不可靠

### 动画反模式

| 禁止 | 原因 |
|------|------|
| Anime/GSAP `autoplay: true` | Remotion 逐帧截图，autoplay 只在 rAF 驱动下工作 |
| Anime/GSAP 定总时长 | 时长唯一真源是 `audioConfig.ts`，动画 duration 仅作编排参考 |
| Anime `scroll` / `draggable` / `waapi` | 无头 Chromium 无用户交互 |
| Anime `from: 'random'` 或裸 `Math.random()` | 破坏确定性渲染（同一帧两次渲染应像素一致） |
| 同一根 DOM 节点同时挂 Anime hook 和 GSAP hook | seek 冲突，两引擎争抢同一 DOM 属性 |
| Anime `scrambleText` 不传 `seed` | 非确定性，每帧渲染结果不同 |
| Lottie `autoplay: true` 或 `loop: true` | 与 Remotion 逐帧 seek 冲突；时长由 audioConfig 决定 |
| `lottie.loadAnimation({ path: "https://..." })` | 无头 Chromium 离线渲染场景不可靠；用 `animationData` 或 `staticFile` |
| 用 Lottie 替代可手画的简单 SVG 动画 | 杀鸡用牛刀；优先 `shared/svg` 三件套 |
| `@remotion/lottie` 版本与 `remotion` 主版本不一致 | 必须同步升级所有 `@remotion/*` 包 |

## 动效质感与去同构（避免「土 + 死板」）

「内容都对、信息都全，但成片观感土、死板、不够酷炫」是 Content Pipeline 最常见的返工源。
根因几乎总是这四条，**首版就按本节做，可一次性拿到想要的质感**，免去推倒重来：

### 根因 1：线性 fade / 线性增长 = 廉价感

`interpolate(opacity, [0,1])` 和 `interpolate(width, ...)` 是默认动作，但它们没有物理感、没有节奏。
**默认换成带物理的揭示**：

| 旧（廉价/死板） | 新（有质感） | 用 shared 积木 |
|------|------|------|
| 条形 `interpolate(width)` 线性长出 | overshoot 冲过头回弹定格 | `useBarGrow(start)` |
| KPI/分数静态标签直接显示 | 从 0 滚动到终值 | `useCountUp(target, start, dur, decimals)` |
| 元素到位即静止 | 赢家/重点项到位后辉光呼吸收尾 | `useGlowPulse(settleFrame)` |
| 卡片纯 opacity 淡入 | spring 缩放+上浮入场 | `<StatCard>` / `useGrowIn` |

封面已用 spring（`useGrowIn`）但内页却全用线性 `interpolate`，会造成**封面惊艳、内页骤降**的体感断层——内页也要用同级动效。

### 根因 2：多页同构 = 死板的头号来源

整条视频每页都「顶部居中大标题 → 中部内容 → 底部 takeaway」，10 页长得一样，再好的单页也显得呆板。
**用 `<SceneTitle>` 的 3 种 variant 交替打破同构**：

```tsx
import { SceneTitle } from "../shared";
// 内页按奇偶交替，封面/收尾用 kicker
<SceneTitle title="净改进排名 Top 8" variant="top-left" />   // 奇数内页：左上角 + 描线下划线
<SceneTitle title="五项体检报告" variant="top-center" />      // 偶数内页：居中 + 描线下划线
<SceneTitle title="谁是干活王者" kicker="AGENT ARENA" variant="kicker" /> // 封面/收尾
```

规则：**同一条视频内页标题 variant 至少出现 2 种**，不允许 10 页全 `top-center`。

### 根因 3：配色脏 + 颜色 hardcode 散落

- 主色饱和度不足（偏暖灰/偏闷）→ 整片"土"。换主色板时改 `colors.ts` 一处即可，**前提是各场景从不 hardcode 颜色**。
- 同一个实体（如某个模型/品类）在不同场景各写各的色值 → 全片不统一。
- **做法**：把"实体 → 颜色"的映射收进 `colors.ts` 的一个函数（如 `modelColor(name)` / `categoryColor(name)`），各场景一律调函数，不写字面色值。
- 数据条/KPI 用**渐变填充**（`GradientBar` 的 `colorFrom`/`colorTo`）比纯色更有质感；重点数字加辉光描边。
- 换色板时，旧 key 保留为新色的**别名**（如 `alibabaBlue` 指向新蓝），`SceneBackground` 等引用零破坏迁移。

### 根因 4：背景能力闲置

`SceneBackground` 内页常被压到 `intensity={0.3}` 而几乎不可见，等于浪费。内页推荐 **0.45**（仍不抢内容，但能看出氛围层）。配合「[暗色背景透明度校准](#暗色背景透明度校准关键陷阱)」一节核对。

### shared 积木速查（去同构 / 加质感）

这些已沉淀到 `src/shared/`，`init` 新项目自动带走，**调色板无关**（传色入参，不绑定固定主色）：

| 积木 | 来源 | 解决 |
|------|------|------|
| `useBarGrow(start)` | `shared/svg` | 条形 overshoot 回弹，替代线性增长 |
| `useCountUp(target,start,dur,dec)` | `shared/svg` | 数字滚动揭示，替代静态标签 |
| `useGlowPulse(settleFrame)` | `shared/svg` | 赢家/重点项到位后辉光收尾 |
| `<SceneTitle variant=...>` | `shared/components` | 3 种标题形态，交替打破多页同构 |
| `<GradientBar colorFrom colorTo>` | `shared/components` | 渐变条 = 增长+滚动+辉光一体 |
| `<StatCard accentColor>` | `shared/components` | 统一卡片 + spring 入场 |
| `<KpiNumber value suffix>` | `shared/components` | 大号 KPI 滚动 + 辉光 |

### 改造既有视频的安全路径

要把一条"已能跑、但土"的视频升级，**严格只换视觉表达层，不动时间轴**：
1. 先建/更新 `colors.ts`（新色板 + 实体色函数 + 旧 key 别名）与 shared 积木。
2. **只改 1 个场景做旗舰样板** → `npm run dev` 让用户验收观感。
3. 满意后按同一套规则推平其余场景（可派多 agent 并行，每 agent 2-3 个场景，共享同一套积木）。
4. 全程**不改** `audioConfig.ts` 和任何 `enterFrame`/`delay`/`interpolate` 帧区间数值——否则字幕/口播对齐会漂移，需重新 burn 字幕。

## 成片自检（门控）

- [ ] `scene-boundaries.md` 与 `audioConfig.ts` / `slideDurationsSec` 一致，总和 ≈ WAV 时长
- [ ] 试听 3 个锚点时刻：画面话题与口播一致
- [ ] 存在 `final_remotion_sub.mp4` 且字幕可读（42px、底栏）
- [ ] 各 Scene 可见正文 ≥42px
- [ ] 已跑 `npm run verify-stills`（按场出代表帧到 `verify/`），并对每张 `s*-*.png` 肉眼扫一遍：标题/卡片/SVG 线/底部说明四区无相互压盖
- [ ] SVG `<text>` 字号 ≥28px（3字以上 ≥32px），节点圆/图标尺寸足够大
- [ ] 主要内容区域占画布 ≥70%，无明显 >25% 空白区
- [ ] 多立场/辩论场景使用对比色（debateFast/Middle/Slow），不全用 accent
- [ ] 场景步骤数与 SRT 口播内容对齐，公式/总结未膨胀为独立步骤
- [ ] 长中文文本（fontSize≥30、≥30 字）按 1.4 行高估算后再核对底部留白，不能按"单行"假设排版
- [ ] `<svg>` 里所有 `line/path` 的端点坐标 (x1,y1,x2,y2) 与 mid 控制点都落在卡片 bbox 之外（除非刻意穿越）
- [ ] 每个内页场景使用专属 aura 或兜底 `<SceneBackground intensity={0.5}>`；跳到第 50 帧可见 ≥3 种动态元素
- [ ] `src/scenes/svg/` 下已有组件均被引用，无纯文字场景遗漏可用 SVG
- [ ] 无 `objectFit: "contain"` 导致两侧空白的图片场景（应替换为 SVG 组件）
- [ ] 封面有前景主视觉元素且面积 ≥ 画布 50%；标题 ≥120px；不是"多层背景+纯文字"
- [ ] 封面主视觉可视化视频核心概念，遮住文字也能猜到主题方向
- [ ] 每个 Phase 场景的视觉密度在同一水平，无"一个 Phase 全幅 SVG + 相邻 Phase 纯文字卡片"割裂
- [ ] 口播枚举的所有类别/步骤/指标在画面中有对应视觉元素，不仅是关键词文字
- [ ] 逐句默念该场景 SRT，每句的具体名词/动作/数字/对比/因果在画面上都能找到对应视觉元素（不必字字对应，语义须呈现）
- [ ] SVG 放射图/流程图中所有节点和标签坐标在 viewBox 范围内，无截断
- [ ] 使用 `scale(N)` 的多 Phase 场景：展开后的子元素画布坐标（groupY + localY × N）与固定覆盖层至少 40px 间距，无重叠
- [ ] 多 Phase 场景的每个 Phase 专属覆盖层在下一 Phase 开始时有显式淡出，不与后续 Phase 元素冲突
- [ ] 所有 `interpolate(strokeDashoffset, ...)` 的 inputRange 为递增序列（`[0, pathLen*X]`），非递减
- [ ] 内页动效非纯线性 fade：条形用 `useBarGrow`、KPI/分数用 `useCountUp` 滚动、赢家/重点项有 `useGlowPulse` 收尾（内页动效不弱于封面）
- [ ] 标题 variant 至少 2 种交替（`<SceneTitle>` 的 top-left/top-center/kicker），不是 N 页全居中同构
- [ ] 各场景无 hardcode 色值：同一实体（模型/品类）的颜色走 `colors.ts` 的映射函数，全片统一
- [ ] 内页 `SceneBackground intensity` ≥0.45（不再压到 0.3 让氛围层不可见）
- [ ] **画面署名**：`src/scenes/**/*.tsx` 无素材公众号 nick、`@` 第三方账号、转载者署名；`content/source/*` 中的 `nick_name` 未出现在 JSX；封面/底部仅本频道固定引导（见 `references/source-attribution-boundaries.md`）

## Scene 布局重叠核查（bbox audit）

中视频常见 14 个 Scene、每帧手算 bbox 不现实，按下列顺序排查最有效（用 14 帧 verify-s*.png + 源码 bbox 静推交叉）：

1. **顶/中/底三带分层** — 标准布局：标题区 (top 0~220)、主体 (240~820)、底部说明卡 (bottom 50~70)。三带任何越界都先看。
2. **算 bbox 时按"实际行数"** — fontSize×1.4×行数 + padding×2，长中文一律预设 ≥2 行。
3. **SVG 与 DOM 卡的关系** — 弧线 `Q` 的 `midY = anchor + curve`、汇聚线 `y1/y2`，必须确认控制点不进卡片 bbox。
4. **flex 等宽容器 + 长 monospace** — 命令字符数 × 字号×0.6 与单卡内宽对齐，溢出会折行抬高整列。
5. **中心徽章 vs 底部条** — `徽章.top + 徽章.h` 与 `1080 - bottom - 底部条.h` 至少留 24px。
6. **绝对定位 `width:N%` 的英文超大标题** — fontSize≥120 时按 `字数 × fontSize × 0.55` 估算英文字宽。

落地动作：在交付前对每个 Scene 列一张 4 列表（标题底/主体顶/主体底/底栏顶）逐项核对；与 `verify-s*.png` 双重确认。

## 与其他 skill 的关系

| Skill | 分工 |
|-------|------|
| **remotion-pipelines** | Scene 组件 + Remotion 渲染 + `scene-boundaries` 话题切场 |
| **html-video-from-slides** | HTML 幻灯成片管线（字幕烧录逻辑已内联至本 skill `lib/`，不再依赖） |
| **srt-proofread** | 口播 WAV → 校对后 `sub.srt`（本 skill 上游） |
| **html-deck-layout** | HTML 幻灯版式；Remotion 路径用 Scene + shared typography 替代 |
| **cover-design** | Step 9a 封面设计（`production/cover.html`，CONFIG 驱动；标题在上 + 画图区 SVG 在中） |

## 文件结构

```
skills/remotion-pipelines/              ← 共享层
├── SKILL.md
├── cli.mjs                             # init 命令
├── package.json
├── remotion.config.ts
├── tsconfig.json
├── scripts/
│   ├── generate-audioconfig.mjs
│   └── burn-subtitles.mjs
├── references/
│   └── scene-boundaries.template.md
├── src/
│   ├── index.ts                        # Remotion 入口（skill 自身也可预览）
│   ├── Root.tsx
│   ├── audioConfig.ts                  # 当前项目的配置
│   ├── scenes/                         # 当前项目的场景
│   │   ├── SceneXxx.tsx               # 场景组件（文字+布局+动画编排）
│   │   └── svg/                        # 场景专用 SVG 视觉组件
│   │       ├── BalanceScale.tsx        # 天平隐喻
│   │       ├── ReviewFunnel.tsx        # 漏斗图
│   │       └── …                       # 按场景需要添加
│   ├── shared/                         # 可复用积木
│   │   ├── colors.ts
│   │   ├── typography.ts
│   │   ├── layouts/
│   │   │   ├── FullBleedTitle.tsx
│   │   │   └── SplitLayout.tsx
│   │   ├── svg/                            # SVG 动画工具 hooks
│   │   │   ├── useStrokeReveal.ts          # 描线动画
│   │   │   ├── useGrowIn.ts               # 缩放弹入
│   │   │   ├── usePulse.ts                # 呼吸脉冲
│   │   │   ├── useCountUp.ts              # 数字滚动揭示
│   │   │   ├── useBarGrow.ts              # 条形 overshoot 回弹
│   │   │   ├── useGlowPulse.ts            # 辉光收尾
│   │   │   └── index.ts
│   │   ├── animations/
│   │   │   ├── index.ts
│   │   │   ├── useGSAPTimeline.ts
│   │   │   ├── gsap/
│   │   │   ├── anime/
│   │   │   │   ├── index.ts
│   │   │   │   ├── useAnimeSeek.ts
│   │   │   │   ├── useAnimeTimeline.ts
│   │   │   │   └── sceneLocalMs.ts
│   │   │   └── lottie/
│   │   │       ├── index.ts
│   │   │       └── useLottieSeek.ts
│   │   └── index.ts
│   └── hooks/
│       └── useCurrentSceneIndex.ts
├── templates/default/                  # 新项目脚手架
│   ├── package.json
│   ├── tsconfig.json
│   ├── remotion.config.ts
│   ├── src/
│   │   ├── index.ts
│   │   ├── Root.tsx
│   │   ├── audioConfig.ts
│   │   ├── scenes/index.tsx
│   │   └── hooks/useCurrentSceneIndex.ts
│   └── public/audio/.gitkeep
├── workflows/                          # 可选：多 agent 并行 workflow（用户显式触发）
│   ├── gen-scenes.workflow.mjs         # 并行生成 14 个 SceneXxx.tsx + 自带 audit
│   └── post-render-audit.workflow.mjs  # 成片自检（6 维度并行，取代手工 checklist）
└── public/audio/                       # 当前项目的音频
```

## 可选：workflow 加速

当一条中视频要批量写 8-15 个场景，或一版渲染后要过 SKILL.md「成片自检」清单时，可调用
`workflows/` 下的脚本，让多个 agent 并行干活、context 不爆主线。

**触发条件**（不会自动跑）：
- 用户在请求里出现 `workflow` / `workflows` 关键字
- 或 ultracode 开启
- 或用户明确说"用 workflow"、"多 agent 并行"

**两个 workflow**：

| 脚本 | 用途 | 何时跑 |
|------|------|--------|
| `gen-scenes.workflow.mjs` | 并行生成所有 SceneXxx.tsx，每个 scene 独立 agent + 自带字号/SVG viewBox/SceneBackground/SRT 对齐 audit | Step 5 audioConfig 就绪后、Step 6 之前 |
| `post-render-audit.workflow.mjs` | 7 维度并行扫成片：font-size / svg-viewbox / scene-bg / phase-consistency / srt-density / config-sync / source-attribution | Step 7/8 渲染后、Step 9 发布前 |

两个 workflow 都**不渲染、不改 audioConfig、不自动 patch tsx**。决策（接受 / 重写 / 重渲）留给主线 agent。
调用契约见各脚本顶部注释；args 必填项打通后直接 `Workflow({ scriptPath, args })`。

## html-video 互补层（可选）

> [html-video](https://github.com/nexu-io/html-video)（Apache-2.0）是 nexu-io/open-design 团队的另一开源项目，
> 提供 HTML→Video 元层能力：多引擎可插拔、21 精选模板、Agent 自动选模板+填内容、本地渲染出 MP4。
> 本章节定义 html-video 作为 remotion-pipelines 的**轻量互补外挂**，覆盖 remotion-pipelines 不擅长的风格化片头/片尾场景。

### 使用范围（硬约束）

| 场景 | 引擎 | 原因 |
|------|------|------|
| 封面 / 片头 | html-video（**可选**） | 独立视觉语言，允许风格不同；无口播需对齐 |
| 片尾 Logo 卡 | html-video（**可选**） | 独立视觉语言 |
| 转场卡（场景间短卡） | html-video（**可选**） | 1-3s 短卡，无需音画同步 |
| **内页场景** | **仅 remotion-pipelines** | 需帧精确音画同步、spring 动画、SVG 视觉隐喻 |
| **数据可视化** | **仅 remotion-pipelines** | 需 spring/interpolate/数据驱动动画 |
| **字幕烧录** | **仅 remotion-pipelines** | html-video 无字幕管线 |
| **TTS 口播对齐** | **仅 remotion-pipelines** | html-video 无 audioConfig / scene-boundaries |

**判断标准**：该段落是否有 TTS 口播需要精确对齐？有 → remotion-pipelines；无（纯视觉/品牌展示）→ 可选 html-video。

### 集成流程（3 步）

**前置**：`repos/reference/html-video/` 已 clone（上游 nexu-io/html-video，只读参考）。

```bash
# 第 1 步：html-video CLI 生成片头/片尾 MP4
cd repos/reference/html-video
node packages/cli/dist/bin.js project-create --name "my-intro"
node packages/cli/dist/bin.js project-set-template <id> --template frame-liquid-bg-hero
node packages/cli/dist/bin.js project-set-var <id> --key headline --value '"..."'
node packages/cli/dist/bin.js project-render <id>
# → .html-video/projects/<id>/output-*.mp4

# 第 2 步：复制到中视频项目
cp repos/reference/html-video/.html-video/projects/<id>/output-*.mp4 \
   features/.../production/videos/intro.mp4

# 第 3 步：concat 拼接
node skills/remotion-pipelines/scripts/concat-intro-outro.mjs \
  --intro features/.../production/videos/intro.mp4 \
  --body  features/.../production/videos/final_remotion_sub.mp4 \
  --outro features/.../production/videos/outro.mp4 \
  --output features/.../production/videos/final_with_intro_outro.mp4 \
  --transition black
```

### 推荐模板映射

| remotion-pipelines 场景类型 | html-video 推荐模板 | 风格 |
|---------------------------|-------------------|------|
| 产品发布视频封面 | `frame-liquid-bg-hero` | 液态渐变 + 大标题 + CTA |
| 技术揭秘视频封面 | `frame-glitch-title` | RGB 故障 + 扫描线 |
| 品牌片头 | `frame-light-leak-cinema` | 胶片颗粒 + 漏光 + 2.39:1 |
| 极简风格封面 | `frame-build-minimal` | 白底 + 大字 + 微动 |
| Logo 片尾 | `frame-logo-outro` | 干净 Logo 动画 |
| 数据报告封面 | `frame-data-chart-nyt` | NYT 风格折线图 |

### 视觉过渡方式

| 方式 | 命令参数 | 特点 |
|------|---------|------|
| 直切 | `--transition cut` | 最快（-c copy），要求编码参数一致，不一致时自动重编码 |
| 黑场过渡 | `--transition black` | 段间 1s 黑场，buffer 自然，适合封面→内页 |
| 交叉淡入淡出 | `--transition crossfade` | 最柔和，需重编码较慢，适合风格接近的段落 |

### html-video CLI 速查

```bash
# 环境诊断
node packages/cli/dist/bin.js doctor

# 查看可用模板
node packages/cli/dist/bin.js search-templates --intent "product launch" --top 5

# 查看模板详情
node packages/cli/dist/bin.js inspect-template frame-liquid-bg-hero

# 完整流程
node packages/cli/dist/bin.js project-create --name "intro"
node packages/cli/dist/bin.js project-set-template <id> --template <template-id>
node packages/cli/dist/bin.js project-set-var <id> --key <key> --value '<value>'
node packages/cli/dist/bin.js project-preview <id>    # HTML 预览
node packages/cli/dist/bin.js project-render <id>     # → MP4
```

### concat-intro-outro.mjs 完整参数

```bash
node scripts/concat-intro-outro.mjs \
  --intro path/to/intro.mp4       # 可选，html-video 片头
  --body  path/to/body.mp4        # 必填，remotion-pipelines 主体
  --outro path/to/outro.mp4       # 可选，html-video 片尾
  --output path/to/final.mp4      # 可选，默认 body 同目录 + _with_io 后缀
  --transition cut                # cut（默认）| black | crossfade
  --black-duration 1              # black 过渡秒数
  --crossfade-duration 0.5        # crossfade 秒数
```

### 回滚

html-video 片段 = 独立 MP4 文件，**零侵入** remotion-pipelines 代码。回滚方式：
- 不用 html-video → remotion-pipelines 完整流程照常（`final_remotion_sub.mp4` 即最终交付物）
- 去掉已加入的片段 → 删一行 concat 命令
- 无需 git revert，无需代码变更

### 反模式

| 不要 | 原因 |
|------|------|
| 用 html-video 做有口播的内页场景 | 无 audioConfig / scene-boundaries，音画必然错位 |
| 用 html-video 做数据图表场景 | 只有 CSS stroke-dashoffset，无 spring/interpolate 物理动画 |
| 在 html-video 片段中烧录字幕 | html-video 无 SRT→ASS→ffmpeg 字幕管线 |
| 修改 remotion-pipelines 代码来适配 html-video | 零侵入原则，html-video 是可选外挂 |
| html-video 片头和内页风格强行统一 | 片头片尾允许独立视觉语言，强行统一反而降低两个引擎各自的优势 |
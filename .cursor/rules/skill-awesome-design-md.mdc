---
description: "Skill: awesome-design-md — Apply brand DESIGN.md design systems from VoltAgent awesome-design-md when building UI, landing pages, or components. Use when the user mentions DESIGN.md, getdesign.md, brand UI (Vercel/Stripe/Linear"
globs:
alwaysApply: true
---

<!-- GENERATED by onecxt adapt — DO NOT EDIT. Changes will be overwritten. Edit the source in meta/ or knowledge/ instead. -->

# Skill: awesome-design-md

# awesome-design-md — 品牌 DESIGN.md 设计系统

基于 [VoltAgent/awesome-design-md](https://github.com/VoltAgent/awesome-design-md)（本仓克隆：`repos/reference/awesome-design-md/`）。每个品牌一份 **DESIGN.md**（色板、字体、组件、布局、Do/Don't），供 Agent 生成一致 UI。

与 `AGENTS.md`（怎么建项目）并列：**DESIGN.md = 项目应该长什么样**。

## 前置：同步参考库

克隆/更新（在 one-context 仓库根）：

```powershell
python -m one_context sync awesome-design-md
```

本地路径：`repos/reference/awesome-design-md/design-md/<brand>/DESIGN.md`

完整目录索引见：`repos/reference/awesome-design-md/README.md`

## 何时使用

- 用户指定品牌气质（如「Vercel 风」「Stripe 紫渐变」「Linear 极简」）
- 用户要求复制某产品官网视觉
- 项目根已有或需要新增 `DESIGN.md`
- 需要统一 landing / dashboard / 营销页的 design tokens

## 工作流

### Step 1：选品牌

从 `design-md/` 子目录选最贴近需求的 `<brand>`（kebab-case，如 `vercel`、`linear.app`、`stripe`、`notion`、`cursor`）。

不确定时：读 `repos/reference/awesome-design-md/README.md` 的分类表，向用户确认 1–2 个候选。

### Step 2：加载 DESIGN.md

**必须 Read**：

`repos/reference/awesome-design-md/design-md/<brand>/DESIGN.md`

可选预览（若存在）：同目录 `preview.html` / `preview-dark.html`（浏览器打开核对色板）。

### Step 3：落盘到目标项目

| 场景 | 做法 |
|------|------|
| 新项目 / 单页 | 复制 `DESIGN.md` 到**目标项目根**（或用户指定路径） |
| 已有 design 文件 | 只引用 token，不覆盖用户自定义 `DESIGN.md`，除非用户明确要求替换 |
| one-context 视频幻灯 | 将 token 转写进 `presentation.html` / CSS，与 `html-deck-layout` 契约兼容 |

复制示例（PowerShell，在 one-context 根）：

```powershell
Copy-Item "repos/reference/awesome-design-md/design-md/vercel/DESIGN.md" "path/to/your-project/DESIGN.md"
```

### Step 4：实现 UI

生成 HTML/CSS/React 等时 **严格遵循** DESIGN.md 中：

1. Visual Theme & Atmosphere  
2. Color Palette（语义名 + hex，勿臆造主色）  
3. Typography 层级表  
4. Component Stylings（含 hover/focus/disabled）  
5. Layout / spacing scale  
6. Do's and Don'ts  

**禁止**：忽略 DESIGN.md 自造一套相近但不同的色板；禁止把 token 写进注释却不体现在 CSS 变量/类名中。

### Step 5：自检

- [ ] 主色、背景、正文色与 DESIGN.md hex 一致  
- [ ] 字体栈与 hierarchy 字重/字号档位匹配  
- [ ] 按钮/卡片圆角、阴影层级符合文档  
- [ ] 响应式断点与 touch target 满足文档 §Responsive  

## 常用品牌速查

| 气质 | brand 目录 |
|------|------------|
| 黑白开发者平台 | `vercel`, `cursor`, `supabase` |
| 紫渐变 fintech | `stripe`, `linear.app` |
| 暖色协作 | `notion`, `airbnb` |
| 暗色终端 | `ollama`, `warp`, `voltagent` |
| 企业蓝 | `ibm`, `hashicorp` |

完整列表（73+）见上游 README。

## 与其他 skill 的关系

| Skill | 分工 |
|-------|------|
| **awesome-design-md** | 品牌级 **DESIGN.md** token → 页面/组件视觉 |
| **html-deck-layout** | 1920×1080 口播幻灯；可 **吸收** DESIGN.md 色板写 CSS |
| **html-slides** | 全视口 HTML 演示；可贯彻 DESIGN.md 理念 |
| **cover-design** | 短视频封面 CONFIG；可与 DESIGN.md 主色对齐 |
| **fireworks-tech-graph** | 架构/流程 **SVG 图**；配色可对齐 DESIGN.md |

## 反模式

- 不要在没有 Read DESIGN.md 的情况下「凭印象」仿品牌  
- 不要把整个 `design-md/` 目录提交进业务仓库——只复制需要的单份 `DESIGN.md`  
- 不要修改 `repos/reference/awesome-design-md/` 内上游文件（该目录由 `sync` 维护）
---
description: "Skill: script-deck-audit — 校验口播讲稿（01-script.md）与 presentation.html 的一致性：讲稿关键术语须在对应 slide 可见文字中出现；幻灯可更深更细；输出句↔页映射。触发：口播幻灯一致性、讲稿校对幻灯、 script deck audit、口播稿覆盖检查。"
globs:
alwaysApply: true
---

<!-- GENERATED by onecxt adapt — DO NOT EDIT. Changes will be overwritten. Edit the source in meta/ or knowledge/ instead. -->

# Skill: script-deck-audit

# script-deck-audit — 口播稿 ↔ 幻灯一致性

## 适用场景

- 已有 `production/content/01-script.md`（或 `讲稿.md`）和 `production/slides/presentation.html`
- 成片 / TTS / 录口播 **之前**，确认「口播说的要点画面上都有」
- 需要维护 **哪句话在哪一页**（映射表，供 `flip-boundaries.md` / 改稿对照）

## 三条规则（与用户约定一致）

| # | 规则 | CLI | Agent 复审 |
|---|------|-----|------------|
| 1 | **口播稿出现的关键信息，对应 slide 上必须有**（专名、数字、英文标识等） | `audit` 机械提取术语并匹配可见文字 | 近义/缩写误判时人工改幻灯或收窄讲稿 |
| 2 | **幻灯可以有额外、更深、更专业的内容** | 不检查「幻灯多出来的字」；允许多页幻灯、少块讲稿时用 `--allow-page-mismatch` | 确认多出的页不误导观众即可 |
| 3 | **哪句话对应哪一页要确定** | `--write-map` 生成 `timing/script-deck-map.md`；报告含句#↔slide | 页数须与 `# 【页题】` 块一致（或与 TTS 分页一致） |

> 本 skill **不**替代 `timing-check`（翻页时刻 vs SRT）或 `srt-proofread`（字幕错字）。推荐顺序：**script-deck-audit → 录口播/TTS → srt-proofread → flip-boundaries → timing-check → wav**。

## 快速命令

在 **one-context 仓库根** 或 skill 目录执行：

```bash
node skills/script-deck-audit/cli.js audit --project features/content-pipeline/<feature-id>/production
```

```bash
node skills/script-deck-audit/cli.js audit --project <dir> --write-map timing/script-deck-map.md
node skills/script-deck-audit/cli.js audit --project <dir> --json
node skills/script-deck-audit/cli.js audit --project <dir> --allow-page-mismatch
node skills/script-deck-audit/cli.js audit --project <dir> --pair-by-index   # 旧行为：sN↔块N
```

默认 **按页题/关键词语义配对**（讲稿 12 块、幻灯 15 页时仍可对齐）；`1-2 层` 等拆成两页时会合并 s4+s5 的可见文字再验术语。

| 退出码 | 含义 |
|--------|------|
| 0 | 通过（机械规则） |
| 1 | 缺少讲稿或幻灯文件 |
| 2 | 存在 error（缺术语、或页数不一致且未 `--allow-page-mismatch`） |

## 输入路径（与 content-production 布局一致）

| 文件 | 解析顺序 |
|------|----------|
| 讲稿 | `content/01-script.md` → `讲稿.md`；`--script` 覆盖 |
| 幻灯 | `slides/presentation.html` → `presentation.html`；`--presentation` 覆盖 |

讲稿分页契约与 `html-video-from-slides` **tts** 相同：块首 `# 【页题】`，块尾单独一行 `---`。

## 工作流（Agent 必按序）

### Step 1：运行机械审计

```bash
node skills/script-deck-audit/cli.js audit --project <production> --write-map timing/script-deck-map.md
```

### Step 2：处理 FAIL

- **PAGE_COUNT_MISMATCH**：增删讲稿 `# 【】` 块或 slide，使页数一致；或确认有意多页幻灯后加 `--allow-page-mismatch`。
- **SCRIPT_TERM_NOT_ON_SLIDE**：在 **对应 slide** 补上缺失专名/数字（优先标题、chip、图示标注）；或改口播去掉该页未展示的断言。
- 幻灯 **允许** 比口播更密；**禁止** 口播有而画面无的关键专名/数字。

### Step 3：语义复审（仅 FAIL 或灰区）

CLI 只做子串匹配。下列情况由 Agent 读报告逐条判定，**不要** 为了通过而删口播：

- 口播「Agent 式搜索」、幻灯「Agent 搜索」→ 可判通过
- 口播展开比喻、幻灯只保留结论 → 结论句中的术语仍须在 slide 上
- 中英文大小写 / `CLAUDE.md` vs `Claude.md`

复审后更新 `presentation.html` 或 `01-script.md`，再跑 Step 1 至退出码 0。

### Step 4：与下游衔接

| 下游 | 用法 |
|------|------|
| `html-video-from-slides` tts | 讲稿块数 = slide 数 |
| `timing/flip-boundaries.md` | 映射表 + SRT 锚点写翻页语义 |
| `html-deck-layout` / 手改 HTML | 先 audit 再改幻灯，避免漏要点 |

## 产出物

| 文件 | 说明 |
|------|------|
| 终端 / `--json` | 审计报告 |
| `timing/script-deck-map.md`（可选） | `--write-map` 生成，句↔页留档 |

## 反模式

- 不要要求口播与幻灯 **逐字** 一致——口播可展开，幻灯是骨架
- 不要用本 skill 代替 Whisper 时间轴或 `timing-check`
- 页数不一致时不要强行 `wav-auto`（易均分时长）

## 与其他 skill 的关系

```
01-script.md + presentation.html
        ↓ script-deck-audit（本 skill）
        ↓ TTS / 录口播 / volc-podcast-tts
        ↓ srt-proofread
        ↓ flip-boundaries + timing-check
        ↓ html-video-from-slides wav
```

| Skill | 分工 |
|-------|------|
| **script-deck-audit** | 讲稿要点 ⊆ 幻灯可见字；句↔页 |
| **html-deck-layout** | 生成/改版式幻灯 |
| **srt-to-deck** | 从 SRT 反推幻灯（SRT 为真源） |
| **srt-proofread** | 字幕错字 |
| **html-video-from-slides** | 成片 |
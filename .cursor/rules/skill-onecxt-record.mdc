---
description: "Skill: onecxt-record — 录制 cc 跑 skill 的全过程，落 onecxt eval 可回放的 scenario。触发词：录制、record、录一下、开始录、录评测、录制一下。注意只有用户**显式说\"录制\"**才进，不要因为用户说\"评测 / 跑一下 X\"就抢戏。**录 ≠ 跑**：录是给用户当前 cc 装监听器，由用户自己接下来手动用 skill；cc 自己绝对不能 spawn 子 cc / 调 `Bash("
globs:
alwaysApply: true
---

<!-- GENERATED by onecxt adapt — DO NOT EDIT. Changes will be overwritten. Edit the source in meta/ or knowledge/ instead. -->

# Skill: onecxt-record

# onecxt-record · 录一份 skill 评测

> 客户：所有 skill 作者 / 使用者。
> 做的事：把"用户在 cc 里完整跑一次 skill"过程中的外部 tool IO + 产物快照 + 判定维度，落到 `skills/<skill>/evals/<scenario>/`，将来 `onecxt eval` 可回放。
> 不做的事：不替你跑 skill；不录跨多 cc 会话；不录本地 tool（Read / Write / Edit / Grep）。

---

## 触发词

只在用户**明确说"录制"**时进。命中以下任一即激活：

- "录一下 <skill>" / "录制一下 <skill>"
- "使用录制模式跑 <skill>"
- "开始录制 <skill> 的评测"
- "录评测 <skill>"
- "record <skill>"
- "录 <skill> 的 <scenario>"（自带 scenario 名）

`<skill>` 可以是 skill 名（如 `info-radar`）或绝对路径（如 `/abs/skills/info-radar`），按最后一段目录名取 skill_name。

**不要抢戏**：用户只说"跑一下 info-radar / 用 info-radar 查 XX" → **不要进 onecxt-record**，让 info-radar 自己接管。

---

## 完成信号（识别 + 反问）

录制中收到以下任一短语 → 进入 finalize：

- "录完了" / "录制完成" / "录制成功"
- "可以了" / "停" / "结束录制" / "好了"
- "stop recording" / "done"

**歧义反问规则**：
- 用户当前正在跑 skill 的某一步（assistant 上一条还在等 tool 结果）→ **不**立即 finalize，反问一句："你是要结束 `<skill>` 的录制吗？还是这一步要继续？"
- 用户在多 skill 切换语境下（同会话内先后说过两个 skill 名）→ 反问："这次结束的是 `<最近被录的 skill>` 的录制吧？"
- 用户用了模糊词如"好了 / 行了"，且没有明确指向录制 → 反问："你是想结束录制（保存 scenario），还是只是这一步完成了？"

反问得到肯定答复后立即进 finalize；否定则继续录。

---

## 工作流（三段式）

### Step 1: start_recording

用户触发 trigger 后：

1. 解析出 `skill_name`（必填）和 `scenario_name`（可选）。
   - skill_name 未给 → 反问："要录哪个 skill？"
   - scenario_name 未给 → 用 `AskUserQuestion` 让用户填，给个默认建议（如 `recording-<YYYY-MM-DD-HHMM>`）
2. 校验 `skills/<skill_name>/SKILL.md` 存在。不存在 → 报错退出。
3. 调 `mcp__onecxt_recorder__start_recording(skill_name, scenario_name)` → 拿到 `session_id`。
4. 给用户一句确认："已开始录制 `<skill>/<scenario>`。请正常使用该 skill，完成后告诉我『录完了』。"
5. **立刻退场，结束本回合输出**：
   - ❌ DO NOT spawn 子 cc / 调 `Bash(claude -p ...)` / 调 `Bash(claude ...)` 启子会话
   - ❌ DO NOT 主动 Read / Explore / Grep skill 内部文件（Step 1.2 校验 SKILL.md 存在已足够，无需深入）
   - ❌ DO NOT 在 start_recording 同一回合内继续输出任何新的 tool_use
   - ✅ 把对话主导权交回用户，等用户下一次发消息

### Step 2: 等待用户跑 skill

录制进行中，本 skill **不再插话**，让用户和原 skill 正常对话。
hook 在后台自动落每一次外部 tool 的 (input, result) 到录制 session 文件。本 skill 此时不调任何 MCP tool。

**强制 yield 规则**：用户没有再发消息前，整个 turn **必须以 start_recording 的确认文案结束**。不要尾随任何 Bash / Task / Explore / Read。

**最常见的违反模式（必须避免）**：assistant 收到 start_recording 成功后，自作主张在同一回合「为了帮用户开始」spawn 一个子 cc 跑 skill。这是错的——录的是用户**自己**接下来在主 cc 里跑 skill 的过程，不是 cc 代跑。如果你发现自己已经在准备 `Bash(claude -p ...)`，立刻停手。

**支持模式**：用户在**同一个 cc**（也就是 start_recording 所在的 parent cc）里继续跑被录的 skill 是支持的——这是默认且最常见的形态。hook 通过 tool 名识别自己的回声（`mcp__onecxt_recorder__*`），其他外部 tool（WebFetch / WebSearch / Bash / 其他 mcp__*）一律录入，不再按 cc_session_id 区分。

唯一例外：检测到完成信号 → 进 Step 3。

### Step 3: finalize（两阶段）

**阶段 A — 起草**

1. 调 `mcp__onecxt_recorder__finalize(session_id)` → 拿到 `finalize_draft_md`（AI 生成的「判定维度候选清单」）。
   - **可选 `workspace_mirror_from` 参数**：如果用户跑的 skill 把产物写到了项目树（如 `production/<skill>/`）而不是 session 自带 workspace，要在调用时显式传入：
     ```
     mcp__onecxt_recorder__finalize(
         session_id=<id>,
         workspace_mirror_from="/abs/path/to/production/<skill>"
     )
     ```
     server 会在 baseline 快照前把该目录内容镜像进 session workspace，确保 BASELINE artifacts 完整。
   - 何时需要：录制内容里有 `Write` / `Edit` 写到 cwd（不是 sandbox）；典型场景是 info-radar 把图写到 `production/info-radar/`。
   - 何时不需要：skill 只写到 session workspace（默认行为），或只产文本无文件副作用。
2. 把 `finalize_draft_md` **原样**贴到 cc 对话给用户看，附上引导语：
   > "这是我根据这次录制起草的判定维度。请你过一遍：哪些保留 / 删掉 / 改判定标准 / 调权重？还有哪些虚假通过场景没覆盖到？回我一段反馈即可，不用改 yaml。"
3. **贴完 draft 后立刻停止本回合**，等待用户在下一回合给反馈：
   - ❌ DO NOT 在同一回合内继续调 `commit_finalize`（哪怕你已经能猜到用户大概会说什么）
   - ❌ DO NOT 自己回答自己起草的问题
   - ❌ DO NOT 用 `AskUserQuestion` 把 draft 拆成多选项；draft 是给用户看完用自己的话回的，不要替他结构化
   - ✅ 文案尾部就是引导语，turn 在此结束

**阶段 B — 收口**

4. 等用户回反馈（自由 markdown 文本即可）。
5. 调 `mcp__onecxt_recorder__commit_finalize(session_id, user_feedback_md)` → 拿到 `{scenario_dir, files_written}`。
6. 给用户最终回执：
   > "已落到 `<scenario_dir>`，包含 `<files_written 数量>` 个文件。运行 `onecxt eval <skill>/<scenario>` 即可回放。"

---

## 边界

- **不录本地 tool**（Read / Write / Edit / Grep / Glob / NotebookEdit / TodoWrite）。这些 tool 回放时会真跑。
- **不录隐式 tool**（如 cc 内部的 SystemReminder）。只录 user / assistant 显式触发的 tool_use。
- **一次 cc 会话内开始 + 结束**。不支持跨多会话恢复 session。
- **一次只录一个 scenario**。已有 active session 时再触发 start_recording → 报错并提示先 finalize 或 abort。
- **不修改 `MockRound` 模型**。如果发现需要新字段 → 退出录制，让水猿先升级 `session_inject.py`，再重录。

---

## 异常处理

| 情况 | 动作 |
|---|---|
| 用户中途说"取消录制" / "别录了" | 调 `mcp__onecxt_recorder__abort(session_id)`，清理临时目录，回执"已取消" |
| finalize 阶段 LLM 报错 | 把错误原样贴给用户，session 保留为 `finalizing` 状态，等用户决定 retry 还是 abort |
| commit_finalize 写文件冲突（scenario 目录已存在且非空）| 反问："已存在 `<scenario_dir>`，是覆盖还是改名？"，按用户选择走 |
| hook 落 rounds.jsonl 失败（磁盘满 / 权限）| MCP server 应在 start_recording 时预检，失败立刻报错，不要等到 finalize 才发现 |

---

## Gotchas

1. **Trigger 必须含"录制"字面**：避免和 info-radar / cover-prompt 等业务 skill 冲突。
2. **完成信号优先级低于反问**：宁可多问一句，不要错杀正在进行的步骤。
3. **artifacts 快照在 finalize 时拍**：不是用户每改一次文件就快照。所以 finalize 前用户最好别再动文件。
4. **本 skill 不应被回放**：onecxt-record 自身不进入 evals，它是个工作流 skill，不是数据加工 skill。
5. **录的是用户的过程，不是 cc 的代跑**：
   - 三个 yield point 必须严格停下：start_recording 后 / finalize 阶段 A 贴完 draft 后 / commit_finalize 完成后
   - 任何"我先帮用户跑一遍"的冲动都是 bug，会污染 mock_rounds（录到的全是 cc 自己 spawn 子 cc 的 round，不是 skill 真实工具调用）
   - 如果用户给的是 skill 路径而不是 skill 名（如 `录一下 /abs/skills/info-radar`），按目录最后一段取 skill_name，不要 Read / Explore 整个目录
---
description: "Skill: slide-subtitle-sync-review — 幻灯片-字幕同步审查技能。以 SRT 字幕为基准，逐页校验 presentation.html 内容 是否与口播音频对齐：检测幻灯片 DOM 顺序与口播顺序错位、字幕提及但幻灯片缺失 的信息、幻灯片冗余但字幕未覆盖的内容、数据/结论矛盾等。输出 sync_review.md。 "
globs:
alwaysApply: true
---

<!-- GENERATED by onecxt adapt — DO NOT EDIT. Changes will be overwritten. Edit the source in meta/ or knowledge/ instead. -->

# Skill: slide-subtitle-sync-review

# Skill: Slide-Subtitle Sync Review — 幻灯片-字幕同步审查

> 适用：`features/content-pipeline/**/production/`
> 核心输入：`subtitles/sub.srt`（字幕） + `slides/presentation.html`（幻灯片）
> 辅助输入：`timing/flip-boundaries.md`（翻页时刻）、`timing/script-deck-map.md`（映射表）
> 产出：`sync_review.md`

---

## 1. 适用场景

- 视频成片前，校验幻灯片内容与口播字幕的同步性
- 检测幻灯片 DOM 顺序与实际口播顺序是否错位
- 发现字幕提及但幻灯片缺失的关键信息
- 发现幻灯片展示但口播未提及的冗余内容
- 校验翻页时刻是否与口播节奏匹配

---

## 2. 触发方式

在 content-pipeline feature 目录下，或给出路径后触发：

```
/slide-subtitle-sync-review              # 扫描当前 feature 的字幕 + 幻灯片 + timing
/slide-subtitle-sync-review <feature路径> # 指定 feature 路径
```

---

## 3. Review 总原则

1. **字幕为准** — SRT 字幕是口播音频的逐字记录，是内容真实性的基准。当幻灯片与字幕不一致时，以字幕为准。
2. **先对顺序，再对内容** — DOM 顺序错位是最隐蔽也最严重的问题，必须优先检查。
3. **可量化可定位** — 每个问题必须精确到「幻灯片页码 + SRT 序号 + 时间码」，不可模糊描述。
4. **区分轻重** — 顺序错位、核心信息缺失、数据矛盾为 Blocking；表达差异、冗余内容为 Suggestion。

---

## 4. 检查清单（Checklist）

### §A. 幻灯片顺序与口播顺序对齐

- [ ] **DOM 顺序 = 口播顺序**：逐页检查 presentation.html 中 section 的 DOM 排列顺序是否与 flip-boundaries.md 中的翻页时刻顺序完全一致。若不一致，标记为 **Blocking**。
- [ ] **页码标注正确**：每页 `.page-num` 的标注与实际翻页顺序一致（如 DOM 交换后页码需同步更新）。
- [ ] **script-deck-map 映射准确**：映射表中「讲稿 #N → sN」的对应关系与实际口播顺序一致。

### §B. 字幕信息 → 幻灯片覆盖

逐页提取该页时段内（从 flip-boundaries 读取进入时刻到下一页进入时刻）的字幕文本，检查：

- [ ] **关键数据有视觉承载**：字幕中出现的具体数字（评分、时间、百分比、代码量等）在对应幻灯片中是否有展示？
- [ ] **核心概念有视觉承载**：字幕中解释的核心概念（如"五维评分"、"交叉审核"）在幻灯片中是否有对应的图表或文字？
- [ ] **流程/架构有图示**：字幕中描述的流程或架构在幻灯片中是否有可视化（流程图、架构图、时间线）而非纯文字？
- [ ] **名称/地址可识别**：字幕提及的项目名、GitHub 地址、工具名在幻灯片中是否有清晰展示？

### §C. 幻灯片内容 → 字幕覆盖

逐页检查幻灯片中的信息是否在对应时段的字幕中有所提及：

- [ ] **幻灯片数据有口播支撑**：幻灯片中展示的数据在口播中是否被提及或至少被引用？完全未提及的数据项标记为 Suggestion（非 Blocking，因为幻灯片允许比口播更深入）。
- [ ] **无孤立视觉元素**：幻灯片中有但口播完全没提到的图表/模块，是否需要补充口播讲解或从幻灯片移除？
- [ ] **幻灯片增量合理**：幻灯片展示比口播更深入的内容（架构图细节、代码片段）是允许的，但不应出现口播完全不相关的新主题。

### §D. 内容一致性

- [ ] **数据无矛盾**：同一数据（评分、迭代轮次、耗时、百分比）在字幕和幻灯片中是否完全一致？小数点差异也算矛盾。
- [ ] **结论无矛盾**：字幕中的定性结论与幻灯片中的文字描述是否一致（如字幕说"交叉审核优于自审"，幻灯片不能暗示相反结论）。
- [ ] **术语一致**：同一概念在字幕和幻灯片中使用相同的术语名称，不出现字幕说"五维评分"而幻灯片写"多维评估"的情况。
- [ ] **时序一致**：字幕中的时间线（如"2026 年 3 月"）与幻灯片中的时间标注一致。

### §E. 翻页时刻与口播节奏

- [ ] **翻页锚点合理**：flip-boundaries 中的翻页时刻是否落在口播自然停顿或话题切换处？翻页不应打断句子中间。
- [ ] **时长分布合理**：每页停留时长是否在 10s-120s 之间？过短（<10s）观众来不及阅读，过长（>120s）画面停滞。
- [ ] **无字幕溢出**：某页时段内的字幕内容是否过多（导致字幕与幻灯片对比信息过载）？过多则建议拆页。

---

## 5. Review 输出格式

对每次 review，输出结构化的 markdown 报告，写入 feature 根目录的 `sync_review.md`（如不存在则新建）。

```markdown
# Slide-Subtitle Sync Review — `<feature-id>`

**Review Date**: YYYY-MM-DD
**Subtitle Path**: `production/subtitles/sub.srt`（N 条）
**Slides Path**: `production/slides/presentation.html`（N 页）
**Flip Boundaries**: `production/timing/flip-boundaries.md`（若存在）
**Script-Deck Map**: `production/timing/script-deck-map.md`（若存在）

---

## 严重问题（Blocking）

| # | 检查项 | 页码 | SRT# | 时间码 | 问题描述 | 建议修改 |
|---|--------|------|------|--------|---------|---------|
| 1 | §A-DOM顺序 | s8/s9 | #68-86 | 04:05-04:48 | 交叉审核(s8)与五维评分(s9)DOM顺序与口播顺序相反 | 交换 s8/s9 两页 section 的 DOM 位置，更新页码 |

---

## 建议优化（Suggestion）

| # | 检查项 | 页码 | SRT# | 时间码 | 问题描述 | 建议修改 |
|---|--------|------|------|--------|---------|---------|
| 1 | §B-信息缺失 | s12 | #119-121 | 06:27-06:31 | 口播提及 .autoresearch 配置覆盖，幻灯片无对应内容 | 在 s12 底部追加 .autoresearch 目录说明 |

---

## 加分项（Highlight）

- 所有数据（600 行、12 exp/h、10 min、9.0 评分）在字幕和幻灯片中完全一致。
- Issue 15/21/6 案例对比表格与口播逐一对应，信息密度合理。

---

## 逐页同步矩阵

| 页码 | 幻灯标题 | SRT# | 进入时间 | 时长 | 字幕摘要 | 对齐状态 |
|------|---------|------|---------|------|---------|---------|
| s0 | 封面 | #1-6 | 00:00 | 31s | AI 实现软件开发自动化 | ✅ |
| s1 | 01项目介绍 | #7-14 | 00:31 | 40s | autoresearch 项目介绍 | ✅ |
| s8 | 04交叉审核 | #68-76 | 04:05 | 19s | Codex/Claude 轮流审核 | ⚠️ DOM 顺序需交换 |

---

## 翻页时刻审查（若 flip-boundaries 存在）

| 页码 | 进入时间 | 时长 | 锚点 SRT | 时长评估 | 备注 |
|------|---------|------|----------|---------|------|
| s0 | 00:00 | 31s | #1 | 正常 | |
| s12 | 05:35 | 56s | #102 | 偏长 | 口播含 .autoresearch 配置说明，幻灯片缺少对应视觉 |
```

---

## 6. 执行流程（Agent 执行步骤）

当用户触发 `/slide-subtitle-sync-review` 时，按以下步骤执行：

### Step 1: 定位文件

- 必须读取：`production/subtitles/sub.srt`
- 必须读取：`production/slides/presentation.html`
- 辅助读取：`production/timing/flip-boundaries.md`（若存在）
- 辅助读取：`production/timing/script-deck-map.md`（若存在）
- 辅助读取：`production/timing/wav-durations.json`（若存在）

若 srt 或 html 文件不存在，报错退出。

### Step 2: 建立时间-页面映射

1. 从 flip-boundaries.md 读取每页的进入时刻和锚点 SRT 序号
2. 若 flip-boundaries 不存在，则从 script-deck-map.md 的映射关系推断
3. 若两者都不存在，按幻灯片 DOM 顺序与 SRT 时间轴等分映射（并标注"低置信度"）
4. 建立 `页码 → [SRT起, SRT止, 起时间, 止时间]` 的完整映射表

### Step 3: 顺序检查（最高优先级）

1. 提取 presentation.html 中所有 section.slide 的 DOM 顺序和标题
2. 提取 flip-boundaries / script-deck-map 中的口播翻页顺序
3. 逐位对比，发现错位立即标记为 **Blocking**
4. 特别注意：script-deck-map 中可能标注了非顺序映射（如 `讲稿#8 → s9`、`讲稿#9 → s8`），这意味着 DOM 顺序与口播顺序不一致

### Step 4: 逐页内容对齐

对每一页：

1. 提取该页时段内的所有 SRT 字幕文本
2. 提取该页幻灯片的全部文本内容（忽略样式代码，只取可见文字）
3. 从字幕文本中提取关键信息点：数字、概念、流程、名称
4. 检查每个关键信息点在幻灯片中是否有视觉承载
5. 从幻灯片文本中提取关键信息点
6. 检查字幕是否遗漏了幻灯片展示的核心内容
7. 比对数据值、结论、术语是否一致

### Step 5: 翻页节奏审查

1. 计算每页停留时长
2. 标记过短（<10s）或过长（>120s）的页面
3. 检查翻页锚点是否在句子中间（检查 SRT 条目的时间跨度）

### Step 6: 生成 sync_review.md

将完整报告写入 `features/content-pipeline/<feature-id>/sync_review.md`。

⚠️ **注意**：
- 若 `sync_review.md` 已存在，**追加**本次 review 到文件末尾（加 `---` 分隔线），不要覆盖已有的历史 review。
- 除非用户明确说"重新 review"，否则保留历史记录。

### Step 7: 输出摘要

向用户返回极简摘要：

```
Sync Review 完成。<feature-id>
- Blocking：N 条
- Suggestion：N 条
- 逐页状态：M 页 ✅  K 页 ⚠️
- 报告路径：sync_review.md

最优先处理：`<最严重的一条问题简述>`
```

---

## 7. 与其他 Skill 的关系

| Skill | 关系 |
|-------|------|
| `html-video-from-slides` | 本 skill 审查其输入的对齐性，在其合成视频前执行 |
| `srt-proofread` | 本 skill 在字幕校对完成后执行，假设 SRT 已定稿 |
| `script-review`（已废弃） | 本 skill 替代 script-review 中 §E（HTML 对齐）部分，并扩展为完整的字幕-幻灯片同步审查 |

---

## 8. 反模式清单（禁止出现）

以下问题在审查时必须标记：

1. **顺序错位**：幻灯片 DOM 顺序与口播顺序不一致，导致观众看到的画面与听到的解说不匹配。
2. **信息黑洞**：口播花了 10 秒以上讲解某个概念/流程/数据，幻灯片上完全没有对应视觉。
3. **数据打架**：同一数据在字幕和幻灯片中数值不同（即使只是小数点差异）。
4. **术语分裂**：同一概念在字幕和幻灯片中用不同名称。
5. **幻灯片幽灵**：幻灯片中展示了重要信息但口播完全没提到且无上下文衔接。
6. **翻页切割**：翻页时刻落在句子中间，导致一句话跨两页，观众感知断裂。

---

## 9. 可选：workflow 加速

当一篇视频 §4 checklist 要逐页人工走 §A-§E 五遍时，可调用 `workflows/` 下的脚本，
让 5 个 agent 并行扫一遍、context 不爆主线。

**触发条件**（不会自动跑）：
- 用户在请求里出现 `workflow` / `workflows` 关键字
- 或用户明确说"用 workflow"、"多 agent 并行"

| 脚本 | 用途 | 何时跑 |
|------|------|--------|
| `sync-review-audit.workflow.mjs` | 5 维度并行扫：order-align / subtitle-to-slide / slide-to-subtitle / consistency / flip-rhythm | Step 5 之前，作为 sync_review.md 草稿 |

workflow **不写盘 productionDir、不改字幕/幻灯片、不写入 sync_review.md**。决策（接受 / 改 html / 改 srt）留给主线 agent。调用契约见脚本顶部注释。
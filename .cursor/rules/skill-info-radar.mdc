---
description: "Skill: info-radar — 信息雷达：多源技术文章追踪 + AI 评估 + content-pipeline 自动转化。 零配置消费 Anthropic/OpenAI/Google/DeepSeek Blog、GitHub Trending、Hacker News、RSSHub 中文源； 可选拉取 wewe-rss 等用户源；Claude 四维评估；高分文章自动创建 mid-video feature。 触发关键词：信息雷达"
globs:
alwaysApply: true
---

<!-- GENERATED by onecxt adapt — DO NOT EDIT. Changes will be overwritten. Edit the source in meta/ or knowledge/ instead. -->

# Skill: info-radar

# 信息雷达 — 多源技术文章追踪 + AI 评估 + content-pipeline 自动转化

零配置消费公开技术源（官方博客、GitHub、HN），可选拉取自建 RSS 源（微信公众号等），Claude 多维度评估文章价值，高分文章自动转化为 content-pipeline feature。

## 行为约束（先读）

- **命中 trigger 即直接执行 Step 1，不要先反问用户「想做什么」**。本 skill 的所有 trigger（`信息雷达` / `选题扫榜` / `扫文章` / `info-radar` / `追踪技术文章` / `AI 选题` / `扫博客` / `HN 排行`）一旦在用户消息中出现，**无论是单独一个词还是完整一句**，都视为「立即执行一次扫描」的明确指令；不要列菜单、不要追问"是想了解技能还是执行"。仅当用户接着补一句明确反对（"先别跑/只看下文档"）时才停。原因：这些词都不是日常闲聊词，触发本身就承载了"动手做"的意图。
- **主线串行执行，不要派 Agent**。Step 2 拉每个源后，**主线自己用结构化 prompt 调 WebFetch 并解析返回结果**；不要派 Agent / Task 去做单源解析（沙盒里行为不稳定，且增加上下文跳跃成本）。可选的 `workflows/multi-source-fetch.workflow.mjs` 是显式 workflow 路径，只在用户主动声明时走。
- **任一源 fetch 失败即跳过 + 记录 errors**，不做二次降级、不调 webSearch 兜底。fallback 由源配置自身承担（见 sources.yaml 的 `fallback_url`），主线只重试一次：原 URL → fallback_url，二者都失败即标记该源 failed 并继续。
- **不要读取 sandbox / 工作目录之外的任何路径**。特别是 `~/.codefuse/`、`~/.claude/`、`/Users/<人>/Library/` 这类宿主路径，以及 `/private/tmp/claude-*/`、`/tmp/claude-*/` 这种 cc 自己的 transcript/task-output 目录——评测沙盒会让真实路径以 `/private/tmp/onecxt-eval-<id>/...` 暴露，所有 Read/Bash/Grep 操作的 path 必须在该前缀内（或当前 cwd 下）。**不要 cat 自己的 stream-json / transcript，不要 Read 其他后台任务的 .output 文件**——上下文恢复请用 TaskCreate 已有的子任务列表。

## Output Contract（每次扫描必须落盘）

每次扫描完整流程结束时，必须把每步结构化结果**显式写到文件**（不只是在对话里输出）：

| 路径 | 来源 | 内容 |
|------|------|------|
| `production/info-radar/01-raw-fetched.json` | Step 2/3 拉取后合并 | JSON 数组，全部原始文章。每条至少含 `title` / `url` / `source` / `summary` / `date`（缺失字段填空字符串或 null）。**附加 `errors` 字段记录失败源列表**：`[{source: "rsshub_zhihu", url: "...", reason: "timeout"}]` |
| `production/info-radar/02-deduped.json` | Step 4 四层过滤后 | JSON 数组，保留下来的文章，结构同 01；附加 `dropped_counts: {by_age: N, by_url: N, by_topic: N, by_existing: N}` 便于审计 |
| `production/info-radar/03-evaluated.json` | Step 5 四维评估后 | JSON 数组。每条文章额外含 `content_quality` / `topic_match` / `trend_relevance` / `video_fitness`（0-100 整数）+ `total_score`（按权重加权和）+ 简短 `reason` |
| `production/info-radar/04-report.md` | Step 5 最终报告 | Markdown 报告，含 "扫描概览" / "评估结果" / "推荐选题" 三章节；评估结果表格行数 == 03-evaluated.json 长度 |

**写文件路径**：`production/info-radar/` 默认相对**当前工作目录**。若用户在主仓根目录触发扫描，写到 `<repo_root>/production/info-radar/`；若在某 feature 目录触发，写到 `<feature_dir>/production/info-radar/`。评测模式下，sandbox 已经准备好了 `production/info-radar/` 容器目录。

## 工作流

### Step 1: 读取配置

读取 `skills/info-radar/references/sources.yaml`，获取：

1. **内置源开关** — 哪些源启用/禁用
2. **评估权重与阈值** — content_quality / topic_match / trend_relevance / video_fitness 权重 + auto_convert_threshold
3. **用户关注主题** — topics 列表（用于 topic_match 维度评估）
4. **用户自建 RSS 源** — user_sources 列表（可选）
5. **转化模式** — confirm（手动确认）或 auto（全自动）

✓ Gate: 配置文件存在且可解析。若 user_sources 中有 URL 不可达，发出警告但继续。

### Step 2: 拉取内置源

**主线串行**逐一拉取 `sources.yaml` 中 `enabled: true` 的内置源。**使用 WebFetch 工具**获取并解析；**不要派 Agent / Task 去做单源解析**——WebFetch 的 prompt 已经要求直接返回 JSON 数组，主线读结果即可。

每个源的拉取策略：
1. 用主 URL 调一次 WebFetch；prompt 要求"返回 JSON 数组（**只输出 JSON，不要 markdown 围栏**）"
2. WebFetch 失败 → 若有 `fallback_url` 再调一次；仍失败即把该源记入 `errors` 数组（`{source, url, reason}`），**继续下一个源**
3. **不要**调 `webSearch`、`Bash curl`、`mcp__codefusesearchmcp__*` 兜底；不要派 Agent 二次解析
4. 解析 WebFetch 返回时，若结果被裹在 markdown ```json``` 围栏里，主线自己 strip 围栏后 `JSON.parse`；解析失败也记入 errors

拉取结果统一为标准文章条目：

```
标准文章条目：
- title: 文章标题（必填）
- url: 原文链接（必填）
- source: 来源标识（如 anthropic_blog, github_trending, hacker_news）
- summary: 摘要/描述（尽可能提取，缺失时为空）
- date: 发布日期（YYYY-MM-DD）
- meta: 来源特定元数据（如 HN score, GitHub stars, 语言等）
```

#### 各源拉取策略

**Anthropic Blog**:
- URL: `https://www.anthropic.com/blog`
- WebFetch prompt: `提取此页面上所有博客文章的标题、URL、发布日期和摘要描述，返回 JSON 数组，每个元素包含 title/url/date/summary 字段。只输出 JSON，不要解释、不要 markdown 围栏。`
- 降级: `https://www.anthropic.com/news`
- 趋势分默认: 60

**OpenAI Blog**:
- URL: `https://openai.com/blog`
- WebFetch prompt: 同 Anthropic Blog
- 降级: `https://openai.com/blog/rss.xml`（HTML 被 Cloudflare 拦截时使用 RSS）
- 趋势分默认: 60

**Google Blog**:
- URL: `https://blog.google/rss/`（RSS feed，更稳定）
- WebFetch prompt: `解析此 RSS feed，提取所有条目的标题、链接、发布日期和摘要描述，返回 JSON 数组`
- 降级: `https://blog.google/technology/ai/`（HTML 页面，含 AI 分类文章列表）
- 趋势分默认: 60

**DeepSeek Blog**:
- URL: `https://api-docs.deepseek.com/updates`
- WebFetch prompt: `提取此页面上所有新闻/更新条目的标题、URL、日期和摘要，返回 JSON 数组`
- 趋势分默认: 60

**GitHub Trending**:
- URL: `https://github.com/trending`
- WebFetch prompt: `提取此页面上所有趋势仓库的名称（owner/repo格式）、URL、描述、编程语言、总stars数和今日新增stars数，返回 JSON 数组`
- 提取: title (repo), url, summary (desc), language, stars, today_stars
- 趋势分映射: Trending 排名 → 65-85 分

**Hacker News**:
- Top Stories: `https://hacker-news.firebaseio.com/v0/topstories.json`
- Item Detail: `https://hacker-news.firebaseio.com/v0/item/{id}.json`
- 步骤: 1) WebFetch 获取 topstories.json → ID 列表; 2) 取前 30 个 ID; 3) 逐个 WebFetch item 详情
- 提取: title, url, score, by, time, descendants
- 过滤: score < `min_score`（默认 100）的跳过
- 趋势分映射: score < 100 → ≤50; 100-200 → 50-69; 200-500 → 70-89; >500 → 90-100

**RSSHub 公共实例**（36kr / 虎嗅 / 知乎热榜）:
- 36kr: `https://rsshub.app/36kr/hot-list`
- 虎嗅: `https://rsshub.app/huxiu/article`
- 知乎: `https://rsshub.app/zhihu/hotlist`
- WebFetch prompt: `解析此 RSS feed，提取所有条目的标题、链接、发布日期和摘要描述，返回 JSON 数组`
- 降级: `https://rsshub.rssforever.com/`

✓ Gate: 至少 2 个内置源成功返回非空数据。每个失败的源把 `{source, url, reason}` 追加到合并结果的 `errors` 数组（最终写入 01-raw-fetched.json）。**若启用源全部失败**，仍然落盘空数组 + errors（让 04-report 能说明原因），然后跳过 Step 4/5 直接进入 Step 6 之前的 "无文章" 分支并终止。

### Step 3: 拉取用户源（可选）

遍历 `sources.yaml` 中 `user_sources` 列表，对每个 RSS URL 使用 WebFetch 拉取并解析 XML。

- wewe-rss 类源（`type: wechat`）：标准 RSS 2.0 格式
- 任意 RSS/Atom 源：兼容 `<entry>` 格式
- 不可达时警告并跳过

✓ Gate: 此步骤可选增强，失败不影响主流程。

### Step 4: 去重

四层过滤：

1. **时间过滤** — 读取 `sources.yaml` 中 `max_age_days`（默认 1，即 24 小时），跳过发布日期超出时间窗口的文章。无日期字段的文章（如 GitHub Trending、HN 热帖）不做时间过滤。

2. **URL 精确去重** — 清理 tracking 参数（utm_*、ref、source）后精确匹配 URL。同一文章在不同源出现时保留元数据更丰富的版本。

3. **主题语义去重** — Claude 判定标题+摘要语义相似度。同一事件不同报道保留热度更高的版本，标记另一篇为"主题相似"。

4. **已有 feature 去重** — 读取 `features/INDEX.md` 获取已有 content-pipeline 选题列表。URL 精确匹配则跳过；标题高度相似则标记"已有相似选题"。

✓ Gate: 去重后文章列表不为空。若为空，报告"无新文章"并结束。

### Step 5: Claude 四维评估

基于 `references/eval-criteria.md` 中的评估标准对每篇文章打分。

**执行方式 — 单次批量打分，不要逐篇 LLM 调用**：

主线（你自己，不是 spawn agent）**用一个 prompt 一次性对全部 02-deduped.json 文章打分**——把整个数组喂给你自己的推理，要求返回结构化 JSON 数组。**不要**：
- 写 Python/JS 脚本机械打分（脚本只会做"标题关键词命中数"之类的伪评估，不是真 LLM 判断）
- 派 Agent / Task 逐篇评估（沙盒里行为不稳定 + 上下文跳跃）
- 对每篇文章单独调一次 LLM（N 篇文章 → N 次 round-trip，慢且费 token）

批量打分 prompt 模板（直接放进你自己的推理上下文，不需要调任何工具）：

```
对下面的 N 篇文章，按四维（content_quality / topic_match / trend_relevance / video_fitness）
逐一打分。每维 0-100 整数。topics 列表为 <来自 sources.yaml>。

返回严格的 JSON 数组（不要 markdown 围栏、不要解释），每条包含：
  - url, title, source
  - content_quality, topic_match, trend_relevance, video_fitness (int 0-100)
  - total_score (float, 按权重 0.30/0.25/0.20/0.25 加权后保留 1 位小数)
  - reason (≤ 30 字)

文章列表如下：
<贴入 02-deduped.json 全部条目，每条至少含 title/source/summary/meta>
```

直接把推理结果 `Write` 到 03-evaluated.json。

**四维评估（详细标准见 eval-criteria.md）**：

| 维度 | 权重 | 评估方式 |
|------|------|---------|
| 内容质量 | 30% | 深度、原创性、信息密度、技术含量 |
| 主题匹配 | 25% | 与 sources.yaml 中 topics 列表的语义匹配 |
| 热点趋势 | 20% | 来源元数据（HN score、GitHub stars、排行位置） |
| 视频适配度 | 25% | 叙事性、可视化潜力、话题性、时长预估 |

总分 = Σ(维度得分 × 权重)，范围 [0, 100]。

**输出格式（Markdown 报告）**：

```markdown
## 信息雷达报告 — YYYY-MM-DD

### 扫描概览
- 扫描时间：YYYY-MM-DD HH:MM
- 启用源：anthropic_blog, github_trending, hacker_news, ... (N 个)
- 拉取文章：M 篇
- 去重后：K 篇
- 已有 feature 去重：L 篇

### 评估结果

| # | 标题 | 来源 | 质量(30%) | 匹配(25%) | 趋势(20%) | 适配(25%) | **总分** |
|---|------|------|-----------|-----------|-----------|-----------|---------|
| 1 | ... | HN | 85 | 92 | 90 | 80 | **86.5** |

### 推荐选题（总分 >= 80）

#### 1. [文章标题] (86.5分)
- **来源**: Hacker News
- **原文**: URL
- **评估理由**: ...
- **建议选题方向**: ...
- **预估时长**: 8-12min (mid-video)

### 主题相似（已去重）
- [文章标题]：与 #1 为同一事件不同报道

### 已有相似选题
- [文章标题]：与 `features/content-pipeline/xxx-mid-video/` 主题重叠
```

✓ Gate: 评估报告已输出。

### Step 6: 转化为 content-pipeline feature

根据 `sources.yaml` 中 `convert_mode` 设置：

- **confirm 模式**（默认）：列出推荐文章，逐篇询问用户"是否为这篇创建 mid-video feature？"
- **auto 模式**：总分 >= `auto_convert_threshold`（默认 80）的文章自动创建 feature

#### 创建流程

1. **生成 feature-id**：基于文章标题生成英文短 ID，格式 `{keyword-slug}-{video_type}`。小写、连字符分隔。检查 `features/INDEX.md` 确保不重复。

2. **创建目录**：`features/content-pipeline/{feature-id}/`

3. **复制模板**：从 `features/_template/content-production/production/` 复制完整目录结构（content/、media/、slides/、subtitles/、timing/、tmp/ 及 .gitkeep 文件）。

4. **生成 spec.md**：基于 `features/_template/spec-content-pipeline.md` 模板填充：

```yaml
---
id: "{feature-id}"
title: "{文章标题}（{视频类型}）"
status: draft
category: content-pipeline
primary_repo_id: one-context
owner: ""
updated: "{today}"
tts:
  engine: volc-podcast-tts
  action: 0
  authority: wav_srt
  override_reason: ""
render:
  stack: remotion-pipelines
---
```

正文填充：
- 概述：原文 URL + 摘要 + 评估理由 + 目标受众
- 目标：标准 content-pipeline 目标清单
- 验收标准：标准 content-pipeline 验收清单
- 实现落点：`features/content-pipeline/{feature-id}/`

5. **生成 00-podcast-source.md**：

```markdown
# 播客合成输入（火山 action=0 · 默认）

> **给 `volc-podcast-tts --action 0` 的长文输入。** ...

## 素材来源
- 原文 URL：{url}
- 本地存档：待补充

## 节目定位
{基于评估的 suggested_angle}

## 钩子与关注引导
- **钩子**：[待人工补充 — 参考 knowledge/standards/video-voiceover-script-conventions.md]
- **固定关注句**（须逐字）：
  「如果你想听更多的大厂和业界AI最新资讯，欢迎关注大厂吾师兄，点关注不迷路。」

## 章节要点
[基于文章摘要自动生成 2-4 个章节，需人工审核修订]

### 1 [章节标题]
[要点提取]

## 专名与链接（口播 + 简介须覆盖）
| 类型 | 内容 |
|------|------|
| 版本/命令 | [从文章提取] |
| GitHub / YouTube | [从文章提取] |
| 须标「第三方/待核」的断言 | [从文章提取] |
```

6. **更新 `features/INDEX.md`**：在表格末尾追加新行。

✓ Gate: 每个创建操作完成后验证文件存在。若 INDEX.md 中已有同 ID，跳过并警告。

---

## Resources

### references/sources.yaml
内置源开关 + 评估权重 + 关注主题 + 用户源配置 + 去重配置。**每次扫描前必读。**

### references/eval-criteria.md
评估维度定义、打分标准、Claude 评估 prompt 模板。**每次评估前必读。**

### features/_template/spec-content-pipeline.md
content-pipeline feature 的 spec 模板。Step 6 创建 feature 时必读，用于填充 frontmatter。

### features/_template/content-production/production/content/00-podcast-source.md
播客合成输入模板。Step 6 生成 00-podcast-source.md 时参考。

### knowledge/standards/content-pipeline-tts-routing.md
TTS 路由规范。确保创建的 feature 默认 `tts.action: 0`。

---

## 可选：workflow 加速

当 `sources.yaml` 启用源 ≥4 个，Step 2 的多路 WebFetch 串行排队会拉长扫榜耗时（每源 IO 等待 5-15s），可调 `workflows/` 下脚本让 N 个 agent 并行拉一轮。

**触发条件**（不会自动跑）：
- 用户请求里出现 `workflow` / `workflows` 关键字，或明确说"用 workflow"、"多源并行"

| 脚本 | 用途 | 何时跑 |
|------|------|--------|
| `multi-source-fetch.workflow.mjs` | Scout 读 sources.yaml → parallel 每源 1 agent 按 SKILL.md L37-99 策略 WebFetch | Step 1 读完配置后、Step 3 去重前；**只覆盖 Step 2** |

workflow **只拉取**：不去重、不评估、不写盘、不读 `features/INDEX.md`。返回的 `articles` 数组交还主线继续走 **Step 3 去重 → Step 4 四层过滤 → Step 5 Claude 评估 → Step 6 confirm gate**（用户人审锚点，workflow 不能跳过）。调用契约见脚本顶部注释。
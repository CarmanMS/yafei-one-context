---
description: "Skill: cover-design — cover-design"
globs:
alwaysApply: true
---

<!-- GENERATED by onecxt adapt — DO NOT EDIT. Changes will be overwritten. Edit the source in meta/ or knowledge/ instead. -->

# Skill: cover-design

# Cover Design Skill

> CONFIG 驱动的独立封面模板。v3 全幅氛围场式：装饰 SVG 铺满整画布当氛围场 → 旋转光晕核 + 光环 → 遮罩 → **标题暗场** → 标题。

## 适用场景

- 视频封面（短视频平台：抖音/小红书/B站）
- 文章头图
- 演示文稿首页
- 任何需要视觉冲击力的单页设计

## 画布规格

| 版式 | 尺寸 | 用途 |
|------|------|------|
| 竖版 | **1080 × 1920** | 短视频封面（抖音/小红书/B站） |

## 核心构图（v3 全幅氛围场式）

```
┌──────────────────────────┐
│  品牌栏（可选）            │  tagline + 品牌名 + 头像
│ ░░░░░░░░░░░░░░░░░░░░░░░░░ │  ← 全幅氛围场 SVG 铺满整画布
│ ░░░  网格 + 坐标线  ░░░░░ │     (网格/坐标/轨道/中心隐喻/节点)
│ ░▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓░ │  ← 标题暗场（径向羽化，保可读性）
│ ░▓   标签            ▓░ │
│ ░▓   ✦ 主标题 ✦      ▓░ │     标题坐在确定暗场上
│ ░▓   ── 发光分割线 ── ▓░ │
│ ░▓   副标题（.hl）    ▓░ │
│ ░▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓░ │
│ ░░░  ◌ 光晕核 + 光环 ░░░░ │  ← 旋转 conic 光晕 + 边框光环（沉在下方做氛围）
│ ░░░░░░░░░░░░░░░░░░░░░░░░░ │
│  底部信息（可选）          │  关键词 + 水印
└──────────────────────────┘

Z 轴层级：
  bg / bg-fallback
   → deco-layer (z:2，全幅氛围场)
   → glow-core (z:3，旋转光晕核)
   → halo-ring (z:4，光环边框)
   → overlay (z:10) → noise (z:11)
   → title-scrim (z:12，标题暗场 ★ 可读性兜底)
   → content-glass + 标题 (z:25，卡片默认关)
```

### 关键进化（v3 vs v2）

| 维度 | v2（中央贴图） | v3（全幅氛围场） |
|------|---------------|-----------------|
| 装饰图形态 | 中央 820×920 贴图 | **全幅 SVG 铺满 1080×1920**（默认），氛围场而非贴纸 |
| 氛围层 | 无 | **`#glow-core` 旋转光晕核 + `#halo-ring` 光环**（主题色参数化） |
| 标题可读性 | 裸标题靠 padding 撑 | **`#title-scrim` 标题暗场**（径向羽化暗底）架构兜底，玻璃卡片改可选 |
| 标题字体 | 固定黑体 900 | **`headlineFont` 预设**：sans / brush（书法）/ cyber（科技） |
| 遮罩 | 单层 | **`center-dual` 双层**（radial 聚焦 + linear 压暗） |
| 字号档位 | 跳档（缺 len9/len11） | **完整 5/6/7/8/9/10/11/12/long** 连续档 |

### 关键约束

1. **全幅氛围场**——decoSVG 默认铺满整画布（`decoFullBleed: true`），是一整个有网格/坐标/轨道/隐喻的视觉场，不是中央一个图标
2. **标题暗场是可读性的第一保证**——`#title-scrim`（z:12）在氛围场/光晕之上、标题之下注入一层径向羽化暗底。**无论氛围场多亮、光晕落在哪，标题背后永远是暗的**。可读性靠这层架构兜底，不靠去调光晕位置或卡片透明度（`titleScrim: true`，建议常开）
3. **玻璃卡片改可选**——`#content-glass` 默认**关**（`glassCard: false`）：暗场已负责把标题从背景剥离，裸标题更通透、不挡 motif。只在明确要卡片美学时才开
4. **氛围层加纵深**——`#glow-core`（旋转 conic 光晕，默认下移 66% 避开标题）+ `#halo-ring`（边框光环）做背景氛围，不与标题抢位
5. **重点词撞色高亮**——`**词**` 渲染为主题色渐变；**重点字渐变须与标题区背景撞色**（如青冷背景用暖金橙重点字），同色相会被背景吃掉；拉丁词自动套 `Audiowide`
6. **整行霓虹**——`accentLine: 1` 使第一行整行发光，配色用主题 `accentGrad` + `accentGlow`
7. **分割线**——headline 和 subline 间的发光渐变条，颜色跟随 `accentGrad`

---

## 图形按选题设计（最高优先级，必读）

封面装饰图不是「换个颜色的通用氛围场」，而是**每一期都要为当期选题专门设计的视觉隐喻**。这是本 skill 历史上最容易翻车的点，务必先读本节再动手画 SVG。

❌ **反面教训**：直接拿 `template.html` 的 `DEFAULT_FULLBLEED_SVG` 或 ELEMENTS.md「图案 6」改个 `rgba` 颜色 → 每期都是「网格 + 同心圆轨道 + 中心圆核 + 节点连线」，配色换了几何没换，肉眼看就是「每期同一个圆」。

✅ **正面范例（装饰图）**：`skills/cover-design/examples/example-cyber-portrait-deco.html` —— 选题是「AI 超级助手」，于是逐 `path` 手绘了一张赛博人像（发丝/眼睛/AR 眼镜/轮廓光），**图形本身就在讲这期选题**。这才是合格的封面装饰图。

✅ **正面范例（标题层级 + 可选分字）**：`skills/cover-design/examples/example-hero-chars-split-domain.html` —— **中文钩子「置身钉内」是视觉主角**（`heroChars` 逐字实色），英文产品名 `ONE` 降为第二行辅助字；背景分裂域 SVG 是**该期专属氛围**，不是通用皮肤。学的是**层级与深底可读手法**，不要每期复制同一套四色 hex 或同一骨架。

> **落盘约定**：正面范例放在 **skill 的 `examples/`**，不要只写在 `features/**/production/`——后者常被清理或 gitignore，Agent 会找不到参考。

### 强制流程（每期必走，禁止跳步）

**Step A · 提炼视觉隐喻** — 先问自己：这期选题的核心意象是什么？一句话能不能画出来？

| 选题 | 视觉隐喻举例 |
|------|------|
| AI 超级助手 | 一个人物 / 虚拟人 |
| 工程化的 Agent 循环 | 一个闭环 / 莫比乌斯环 / 齿轮链 |
| 钉钉 One 内嵌 | 对话气泡 / 嵌套容器 / 入口 / 分裂域（见 dingtalk 封面范例） |
| 数据库选型 | 表格 / 层级树 / 数据管道 |

**Step B · 选几何骨架（必须与隐喻匹配，禁止默认同心圆）** — 骨架决定整张图的「形」，每期都该不同：

| 选题意象 | 候选骨架（举例，非穷举） |
|---|---|
| 人 / 角色 / 助手 | 人像轮廓 + 轮廓光 |
| 循环 / 迭代 / 闭环 | 莫比乌斯环、首尾相接箭头、齿轮链 |
| 流程 / 管道 / 链路 | 横/纵向管道流、泳道、有向节点图 |
| 层级 / 结构 / 数据库 | 树状层级、堆叠卡片、表格栅格 |
| 对话 / 协作 / IM | 气泡云、消息流、嵌套窗口 |
| 网络 / 生态 / 连接 | 力导向图、星座网、蜂巢六边形 |
| 增长 / 突破 | 折线攀升、火箭、阶梯 |
| 抽象 / 概念 | 拓扑面、波形、粒子场 |

> ⚠️ **同心圆 + 网格只是「抽象/数据场」这一类里的一种选择，不是默认骨架。** 除非选题本身就是「轨道 / 雷达 / 同心扩散」类意象，否则不要用它。

**Step C · 不确定就问用户** — 如果你提炼不出贴切的视觉隐喻，或拿不准该用哪种骨架，**停下来问用户的建议**（用 AskUserQuestion 给几个骨架方案让用户选），不要默认套同心圆糊弄过去。

**Step D · 逐元素为选题手绘** — 骨架定了之后，用 SVG `path`/`shape` 把隐喻画出来。可以保留网格底纹 / monospace 角标这类「场」元素做氛围，但**主视觉骨架必须是当期独有的、能一眼看出选题的**。

### 与氛围层正圆的关系

`#glow-core`（旋转光晕核）和 `#halo-ring`（光环边框）是两个**居中正圆** CSS 图层，默认开启。当你的 motif 已自带视觉焦点（如人像、闭环），这两个圆会和 motif 抢焦点、又叠出「圆套圆」观感 —— 此时应在 CONFIG 里设 `glowCore:false` / `haloRing:false` 关掉，让 motif 独占焦点。只有当 motif 偏弱/偏满、需要中心再聚焦时才保留。

### motif 与居中标题卡的避让（实操血泪）

`#content` 玻璃卡片**垂直居中**（`top:50%`）。如果你把 motif 的主元素也堆在画布正中（cy≈900），**卡片会把 motif 核心整块盖掉**——渲染出来只剩边角漏光，骨架等于白画。这是 full-bleed motif 最常见的翻车点。

✅ **正确做法：motif 放大、焦点外推，环绕标题卡，中心留空**：
- **环形/闭环骨架**（莫比乌斯环、跑道、轨道）→ 把环放大到顶部 cy≈360、底部 cy≈1440、左右 x≈170/910，让环带从卡片四周经过，中心镂空给标题。
- **辐射/枢纽骨架**（入口辐射、星座网）→ 枢纽锚点可压在标题中心（被卡片半盖、做「焦点在此」的脉冲即可），但**外围图标/端点必须推到卡片上方一排、下方一排或四角**，别落在卡片覆盖的中段。
- **人像/单体骨架** → 干脆 `glassCard:false` 裸标题（如 openhuman 那期），让 motif 整张铺开，标题靠 textShadow/径向暗底压可读。

> 经验数值：标题卡大约占据 `cy 600~1200` 的中段。motif 主元素要么在这段**之外**，要么细到能透过卡片缝隙——否则一律被盖。改完务必 headless 渲染一张实图确认，不要凭代码想象。

### 标题暗场（`#title-scrim`）优先于玻璃门板（`glassCard`）

`glassCard:true` 会在标题下垫一块**圆角矩形毛玻璃**。在 full-bleed motif 上它有个致命副作用：那块**方形门板会把背景氛围场和 motif 整块遮死**，封面看上去像「背景图上盖了一块磨砂玻璃板」，motif 全废。

✅ **改用 `#title-scrim` 标题暗场兜底可读性**：一层**径向羽化暗底**（中心浓、向四周渐隐到全透明，**无硬边**），压在氛围场/光晕之上、标题之下。它把标题区压暗到足够对比，又因为没有边界，背景与 motif 自然透出、不会被「切一块方的」。

- `titleScrim:true`（建议常开）+ `glassCard:false` —— full-bleed motif 封面的推荐组合。
- 仅当背景极简、确实需要一块实体卡片承托时才用 `glassCard:true`。
- 两者可叠加，但 full-bleed 下基本不需要 glassCard。

> z 轴：`overlay(10) → noise(11) → title-scrim(12) → 标题(25)`。scrim 在噪点之上、标题之下，所以只压背景不压字。

---

## CONFIG 配置

```javascript
const CONFIG = {
    // ── 内容 ──
    headline:    "AI正在**重塑**\n软件开发",  // **词** = 重点高亮，\n = 换行
    accentLine:  0,              // 整行霓虹发光：1=第一行，2=第二行，0=关闭
    subline:     "一句话副标题，**关键词**高亮", // 可选，空则隐藏；**词** = .hl 高亮
    tag:         "2026 · 标签",               // 可选，空则隐藏

    // ── 装饰氛围场 ──
    decoSVG:     "",             // 内联 SVG，空则用 tech 默认全幅氛围场
    decoFullBleed: true,         // true=全幅铺满（默认）；false=中央贴图（旧式单图标）
    decoRotate:  0,              // 旋转角度（full-bleed 建议 0）
    decoOpacity: 1,              // 透明度
    decoScale:   1,              // 缩放
    decoTop:     "52%",          // 仅 decoFullBleed:false：垂直位置
    decoWidth:   "820px",        // 仅 decoFullBleed:false：宽
    decoHeight:  "920px",        // 仅 decoFullBleed:false：高

    // ── 氛围层 / 标题可读性（默认值已调好，一般无需改） ──
    titleScrim:  true,           // 标题暗场：保证可读性（建议常开）
    glowCore:    true,           // 旋转光晕核（氛围，默认下移 66% 避开标题）
    haloRing:    true,           // 光环边框
    glassCard:   false,          // 玻璃卡片：默认关（暗场已剥离）；想要卡片美学时开

    // ── 标题字体 ──
    headlineFont: "sans",        // sans(黑体900) | brush(书法) | cyber(Audiowide)

    // ── 可选：分字主标题（默认关，见下文「分字主标题」节） ──
    heroChars:     false,        // true=第一行逐字实色+光晕；第二行降为 sub-head
    heroCharColors: [],          // 可选 hex[]；空则从主题 accent/accent2 推导

    // ── 品牌（可选） ──
    avatarImage: "",
    avatarEmoji: "🐶",
    brandName:   "品牌名",
    tagline:     "Slogan",

    // ── 底部（可选） ──
    footerItems: ["关键词1", "关键词2"],
    watermark:   "水印文字",

    // ── 主题 ──
    theme:       "tech",         // danger|growth|tech|finance|blueprint|openhuman|deck|postgres
    bgImage:     "",             // 背景图路径，空则用主题渐变

    // ── 背景控制 ──
    overlayDirection: "center-dual",  // center-dual | to-bottom | to-top | center-out | to-left | to-right
    noiseEnabled: true,
    noiseOpacity: 0.05,
};
```

### CONFIG 字段详解

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `headline` | string | 是 | 主标题，`\n` 换行，`**词**` 高亮重点词 |
| `accentLine` | number | 否 | 整行霓虹高亮：1=第一行，2=第二行，0=关闭（默认） |
| `subline` | string | 否 | 副标题，`**词**` 渲染为 `.hl` 高亮词，空则隐藏 |
| `tag` | string | 否 | 小标签，空则隐藏 |
| `decoSVG` | string | 否 | 装饰 SVG，空则用对应模式默认图（full-bleed→氛围场 / 贴图→手机） |
| `decoFullBleed` | boolean | 否 | **默认 true**=全幅铺满氛围场；false=中央贴图（旧式单图标） |
| `decoRotate` | number | 否 | 旋转角度，full-bleed 默认 0，贴图默认 -11 |
| `decoOpacity` | number | 否 | 装饰图透明度，默认 1 |
| `decoScale` | number | 否 | 装饰图缩放，默认 1 |
| `decoTop` | string | 否 | 仅贴图模式：垂直位置，默认 "52%" |
| `decoWidth` | string | 否 | 仅贴图模式：宽，默认 "820px" |
| `decoHeight` | string | 否 | 仅贴图模式：高，默认 "920px" |
| `titleScrim` | boolean | 否 | **默认 true**：标题暗场（径向羽化暗底），可读性架构兜底，建议常开 |
| `glowCore` | boolean | 否 | **默认 true**：旋转 conic 光晕核（默认下移 66% 避开标题） |
| `haloRing` | boolean | 否 | **默认 true**：边框光环 |
| `glassCard` | boolean | 否 | **默认 false**：玻璃卡片；暗场已剥离标题，仅在要卡片美学时设 true |
| `headlineFont` | string | 否 | 标题字体预设：`sans`（默认黑体900）/ `brush`（书法）/ `cyber`（Audiowide） |
| `heroChars` | boolean | 否 | **默认 false**。`true` 时第一行逐字实色+光晕；第二行起为 `sub-head` 辅线。见「分字主标题」节 |
| `heroCharColors` | string[] | 否 | 逐字 hex；**空**则从本期 `theme` 推导，勿写死某一期配色 |
| `avatarImage` | string | 否 | 头像图片路径 |
| `avatarEmoji` | string | 否 | 无头像时的 emoji，默认 🐶 |
| `brandName` | string | 否 | 品牌名 |
| `tagline` | string | 否 | 频道定位/标语 |
| `footerItems` | string[] | 否 | 底部关键词列表 |
| `watermark` | string | 否 | 右下角水印 |
| `theme` | string | 否 | 主题色系，默认 tech |
| `bgImage` | string | 否 | 背景图路径 |
| `overlayDirection` | string | 否 | 遮罩渐变方向，默认 center-dual |
| `noiseEnabled` | boolean | 否 | 噪点纹理开关 |
| `noiseOpacity` | number | 否 | 噪点强度 0~1 |

---

## 装饰图两种形态

| 形态 | `decoFullBleed` | viewBox | 适用 | 说明 |
|------|-----------------|---------|------|------|
| **全幅氛围场**（默认） | `true` | `0 0 1080 1920` | 绝大多数封面 | SVG 铺满整画布，是有网格/坐标/轨道/中心隐喻/数据节点的视觉场，配玻璃卡片 |
| **中央贴图**（旧式） | `false` | 任意比例 | 单一图标符号（手机/火箭/芯片） | 居中偏下摆放一个图标，靠 `decoTop/Width/Height` 定位 |

全幅氛围场 SVG 的设计方法见上文「图形按选题设计」节——**先定当期专属的视觉隐喻与几何骨架**，再用下面这些「场」元素把空间填出纵深：
```
网格底纹（粗+细两层 pattern）   ← 通用氛围，可复用
坐标基准线（实线 + 虚线）        ← 通用氛围，可复用
monospace 角标（低透明度英文）   ← 通用氛围，可复用
─────────────────────────────
主视觉骨架                       ← 每期不同，按选题设计，禁止默认同心圆
```
颜色用主题色系的半透明 `rgba`，渐变 `id` 用唯一前缀避免冲突。

> ELEMENTS.md「图案 6」给的同心圆+轨道范式，只是「抽象/数据场」选题的一种骨架，**不是默认骨架**，不要无脑套用。

---

## 标题可读性、氛围层与玻璃卡片

可读性的分层职责（这是 skill 的核心架构，务必理解）：

- **`#title-scrim` 标题暗场（`titleScrim`，默认开，z:12）★ 第一保证**：径向羽化暗底锁定标题区中心，压在氛围场/光晕之上、标题之下。**它的存在使「标题可读」与「氛围场多花哨」彻底解耦**——无论背景多亮、光晕落在哪，标题背后永远是暗场。**不要靠关光晕、调卡片透明度去救可读性，那是治标；暗场才是治本。** 一般保持常开。
- **`#glow-core` 旋转光晕核（`glowCore`，默认开，z:3）**：980×980 旋转 conic 光晕 + 径向核，`mix-blend-mode: screen`，默认中心在 `top:66%`（已避开标题区）做背景氛围。motif 自带强焦点（人像/闭环）时设 `false` 避免抢焦。
- **`#halo-ring` 光环（`haloRing`，默认开，z:4）**：720×720 边框光环 + box-shadow，加纵深。同上，motif 强时可关。
- **`#content-glass` 玻璃卡片（`glassCard`，默认关，z:25）**：毛玻璃卡片，`backdrop-filter: blur` + 主题半透底（`glassBg`）+ 边框光晕。**默认关**——暗场已负责剥离标题，裸标题更通透、不挡 motif。**只在明确想要「卡片美学」（如发布会风、信息卡风）时才设 `true`**；此时暗场 + 卡片双层叠加，最稳但最「框」。

> 经验法则：**先靠暗场 + 撞色重点字保证可读**（默认配置就是），不够再考虑开卡片。openhuman / loop 两期都是 `glassCard:false` 纯暗场，最浑然一体。

---

## 标题字体预设（headlineFont）

| 预设 | 字体 | 字重 | 适用 |
|------|------|------|------|
| `sans`（默认） | PingFang/YaHei 黑体 | 900 | 科技/通用/力量感 |
| `brush` | ZCOOL QingKe HuangYou 书法 | 400 | 国风/人文/标题党金句 |
| `cyber` | Audiowide 科技字 | 400 | 纯英文/赛博/产品名 |

拉丁重点字（`**Postgres**`、`accentLine` 命中纯英文行）**自动**套 `Audiowide`（`.en-accent`），无需手动指定。

---

## 遮罩方向（overlayDirection）

| 方向 | 适合场景 |
|------|----------|
| `center-dual`（默认） | **双层**：中心通透 + 上下压暗，full-bleed 高密度氛围场首选 |
| `to-bottom` | 顶部亮底部暗，标准竖版 |
| `to-top` | 顶部暗底部亮，人物在上方 |
| `center-out` | 中央亮四周暗，人像/中心图案突出 |
| `to-left` / `to-right` | 文字偏一侧的构图 |

---

## 字号自适应

CJK 字符计 1 宽度，ASCII 计 0.55 宽度 → 取最长行视觉宽度 → 连续分档映射字号：

| 最长行宽度 | class | 字号 |
|-----------|-------|------|
| ≤4（且 `heroChars:true`） | len-hero4 | 220px |
| ≤4 | len5 | 200px |
| ≤5 | len6 | 180px |
| ≤6 | len7 | 155px |
| ≤7 | len8 | 130px |
| ≤8 | len9 | 118px |
| ≤9 | len10 | 108px |
| ≤10 | len11 | 98px |
| ≤11 | len12 | 92px |
| >11 | long | 82px |

**注意**：计算宽度时自动忽略 `**` 标记；letter-spacing 由 `headlineFont` 预设决定（sans -2px / brush 2px / cyber 2px）。

---

## 标题渲染规则

1. **`accentLine: N`** — 第 N 行整行渲染为 `<em>`，应用 `accentGrad` 渐变 + `accentGlow` drop-shadow 霓虹光晕
2. **`**词**`** — 行内 `**词**` 渲染为 `<em>`，应用 `accentGrad` 渐变；若主题有 `accentGlow`，追加 drop-shadow
3. 普通行渲染为 `<span>`，应用 `textShadow` 阴影
4. `accentLine` 与 `**词**` 可共存；同一行内 `accentLine` 优先
5. **`heroChars: true`** — 第一行逐字 `<span class="hero-char">` 实色 + 多层 `text-shadow` 光晕；**关闭** `accentLine` 与第一行 `**词**`（与分字互斥）。第二行起用 `.sub-head`（约 0.4em），英文 `ONE` 等可套 `.one-lite` 作辅助标识

> `heroChars` 与默认 `accentLine` / `**词**` 是**两套标题美学**，每期只选一种主标题手法，不要叠用。

---

## 可选模式：分字主标题（`heroChars`）

> **默认关。** 这不是「每期封面都要四色分字」——只是一条在**深底 + 短中文钩子**场景下验证过的**可选**排版路径。成功沉淀的是**原则**，不是 dingtalk 那一期的具体 hex 或分裂域 SVG。

### 适用（可考虑开 `heroChars`）

- 第一行是 **3–6 字中文金句/钩子**，需要在缩略图上一眼读出「本期在讲什么」
- 背景偏暗（近黑 / 高对比氛围场），**渐变裁字**或整行霓虹容易发闷、抢不过英文品牌名
- 第二行是**产品名 / 英文标识 / 追问句**，语义上应弱于第一行（如「置身钉内」> 「ONE 服务谁？」）

### 不适用（保持默认渲染）

- 第一行以**英文**为主，或依赖 `accentLine` / `**词**` 撞色渐变做重点
- 第一行超过 **6 字**（分字会挤、光晕糊成一团）——改用默认字号分档或缩短钩子
- 用户明确要「整行霓虹」「书法单行」「发布会卡片风」——走 `accentLine` / `brush` / `glassCard`

### 每期必须变的轴（防「每期一模一样」）

| 轴 | 本期怎么定 | 禁止 |
|----|-----------|------|
| **钩子文案** | 从 `spec` / 口播钩子提炼，字数 3–6 | 固定抄「置身钉内」等往期金句 |
| **字数 → 字号** | ≤4 字用 `len-hero4`（220px），更长走默认分档 | 每期都强行 4 字 |
| **逐字配色** | 优先 `heroCharColors` 从**本期** `theme` + 氛围场 SVG 主色推导（冷底配暖字、分裂域可左右两色各取一字） | 写死 `#FF5A6E/#FFC14D/...` 当 skill 默认值 |
| **第二行** | 按选题写副问句 / 英文名；`ONE` 仅当本期口播真有该专名 | 每期都加 `ONE` + `.one-lite` |
| **氛围场** | 仍走「图形按选题设计」——分裂域、人像、环等**每期重画** | 把 A-split 红蓝裂缝当通用皮肤 |

### 配色推导清单（开 `heroChars` 前 30 秒）

1. 定 `theme`（或本期自定义 `THEMES` 条目），读出 `accent` / `accent2`
2. 看 `decoSVG` 里**最抢眼的两块色**（如左域暖色、右域冷色）——可各映射 1–2 个字
3. 凑满 N 色（N = 第一行字数）：不足则从 `defaultHeroColors()` 池（主题色 + 辅助撞色）循环，**不要**从别的 feature 复制数组
4. headless 截图确认：缩略图 100px 宽仍能分清四字；英文辅线不能比中文更亮

### 与 `cover-prompt`（AI 生图）的分工

- **`cover-prompt`**：Midjourney/Flux 等**位图**封面，另一套范式（极简海报 / 信息密度）
- **`cover-design` + `heroChars`**：**HTML CONFIG → 精确叠字**，适合品牌名、专名、双行层级要锁死的场景
- 同一期可以只做其一；不要要求两路封面长得一样

### 反模式（skill 硬约束）

- ❌ 把 `heroChars: true` 设为模板默认 —— 必须显式开启
- ❌ 每期封面都四色分字 —— 多数期用 `accentLine` / `**词**` / 单行书法即可
- ❌ 照抄 dingtalk 的 `heroCharColors` 或分裂域 SVG —— 只复用**层级**（中文主、英文辅、实色+光晕）
- ❌ 第一行开 `heroChars` 同时又设 `accentLine: 1` 或 `**词**` —— 渲染路径互斥
- ❌ 用 `heroChars` 逃避「为选题画专属 motif」—— 分字只解决**字**，不替代装饰图设计

---

## 重点字「炸裂高亮」避坑指南

`accentGlow` 非空（tech / postgres / blueprint / openhuman / deck 等辉光主题）时，渐变重点字最容易翻车。四条必守规则，已固化进 `template.html`：

### 1. 渐变裁字与辉光必须分两层 —— 否则字内透黑（模板级 bug）

`background-clip:text` + `-webkit-text-fill-color:transparent` 与 `filter:drop-shadow(...)` **加在同一元素**会触发 Chromium 渲染 bug：渐变填充失效，字心透出黑背景，只剩外圈一圈彩色辉光（肉眼像「字是空心黑的」）。

✅ 正确做法（模板已实现 `wrapAccent`）：外层 `<em>` 只挂 `filter` 辉光，内层 `<i>` 只挂渐变 `clip-text`。

```html
<em style="filter: <accentGlow>"><i style="background:<grad>; background-clip:text; color:transparent;">榜单</i></em>
```

### 2. 渐变裁字必须配粗体 —— 细笔画填不满会「发闷」

中文艺术字（如 `ZCOOL QingKe HuangYou`）笔画细，渐变裁切后笔画填不满、对比不足，重点字反而比白字还暗。

✅ 重点字单独覆盖为**粗黑体 900**（`PingFang SC` / `Microsoft YaHei`），并适当放大（1.1~1.2em）让撞色渐变铺得开。拉丁重点字用 `Audiowide` 等科技字即可，无需此处理。

### 3. 重点字背后需要暗场 —— 亮背景会冲淡辉光

若装饰 SVG 的高光（聚光灯锥、光晕）正好落在标题区，辉光会被亮背景冲淡，怎么调都「不够炸」。两个解法任选或并用：

- **压暗背景**：让 SVG 的高光顶点避开标题行（如聚光锥顶点下移到主视觉附近）
- **给标题加径向羽化暗底**：`#headline` 加 `radial-gradient(...rgba(4,8,20,0.82)...至透明)` 背景，无硬边把文字从背景剥离

### 4. 撞色比同色系更「炸」

单色渐变（青→紫）在同色调背景上跳脱感弱。要冲击力就用**撞色**：渐变从白心起步 → 冷色 → 暖色跨色相（如 `白→青→品红→金黄`），在冷背景上对比最强。

### 5. 不要在单期 cover.html 里硬编码覆盖 `accentGrad` / `.accent-line`（高频翻车）

模板里整行霓虹（`accentLine`）和内联高亮（`**词**`）**都走 `wrapAccent` → 用主题的 `t.accentGrad` / `t.accentGlow`**，而每个主题的默认 `accentGrad` 已是「与该主题背景撞色」调好的（如 tech 青紫背景配暖金橙）。

❌ **反面教训**：想要「更炸的霓虹」时，直接拷贝另一主题（如 openhuman 的青→品红→粉）的渐变，在单期 cover.html 里塞一段 `#headline em.accent-line { background: ...!important }` 覆盖掉默认值。结果：openhuman 的渐变是为**粉调背景**调的，照搬到 **tech 青紫氛围场**上就成了「青紫字压青紫背景」，同色系自溶、发闷——正是第 4 条说的坑。**换了渐变却没换背景，等于把调好的撞色改回了同色系。**

✅ **正确做法**：
- 想换重点色 → 优先**换主题**（`theme` 字段），主题的 `accentGrad`/背景是配套的。
- 确需新配色 → 在 `template.html` 的 `THEMES` 里**新增一个主题**，把 `accentGrad`、`fallbackBg`、`decoGlow` 一起按撞色原则定好，而不是在单期文件里 `!important` 覆盖单个属性。
- 真要在单期临时覆盖 → **先确认新渐变与本期氛围场 SVG / 背景是撞色**（冷背景配暖字，暖背景配冷字），并 headless 渲染确认，不要凭「另一期好看」照搬。

> 判断口诀：**重点色好不好看，取决于它和背景是不是撞色，而不是渐变本身好不好看。** 同一套青紫渐变，在青紫背景上糊、在粉调背景上炸。

---

## 装饰 SVG 规范

- 全幅氛围场（默认）：`viewBox="0 0 1080 1920"`，`#deco-layer` 铺满 `inset:0`
- 中央贴图（`decoFullBleed:false`）：任意比例 viewBox，居中偏下定位（`decoTop`）
- 颜色建议使用半透明 `rgba`，让底层主题色微微透出
- 渐变 ID 使用唯一前缀避免冲突
- `decoRotate` / `decoScale` / `decoOpacity` 独立控制旋转/缩放/透明

### 装饰图设计原则

1. **寓意优先**——图案要表达内容主题的核心概念，不是纯装饰
2. **铺满成场**——full-bleed 模式下 SVG 是覆盖全屏的氛围场，用网格/坐标/轨道把空间填出纵深
3. **与主题色协调**——使用主题色系中的颜色（见 PRESETS.md）
4. **留出中心**——中心标题区域低饱和/通透，靠玻璃卡片承载文字，避免和标题冲突

---

## 工作流程

### Step 1: 确定内容 + 标题手法
```
主标题几行？第一行是中文钩子还是英文品牌？
→ 默认：accentLine / **词** 撞色
→ 可选：heroChars（3–6 字中文钩子 + 弱化的第二行），见「分字主标题」节
副标题（含 **关键词**）+ 标签
```

### Step 2: 选主题 + 字体
```
内容类型 → 匹配主题色系（见 PRESETS.md）→ 选 headlineFont（sans/brush/cyber）
```

### Step 3: 为选题设计装饰图（核心，按「图形按选题设计」节走）
```
提炼视觉隐喻 → 选匹配的几何骨架（禁止默认同心圆）→ 逐元素手绘当期独有的主视觉
拿不准就 AskUserQuestion 让用户选骨架，不要套通用同心圆氛围场
网格/角标等「场」元素可保留做氛围，但主骨架必须能一眼看出本期选题
```
> ⚠️ 不要直接拿 DEFAULT_FULLBLEED_SVG / ELEMENTS「图案 6」改色就用——那会导致每期都是同一个同心圆。

### Step 4: 填写 CONFIG
```
复制 template.html → 重命名为 cover.html → 修改 CONFIG
（氛围层/玻璃卡片默认全开，背景越满越要保留 glassCard）
```

### Step 5: 截图生成
```
封面截图工序在 content-pipeline 主流程中统一执行（见 features 模板）
```

---

## 文件结构

```
production/
├── cover.html          # 从 template.html 复制，填好 CONFIG
└── videos/
    └── cover.png       # 竖版输出 (1080×1920)
```

---

## 相关文件

- `template.html` — CONFIG 驱动封面模板（v3 全幅氛围场式）
- `PRESETS.md` — 8 套主题色系说明
- `ELEMENTS.md` — 装饰 SVG 图库（含全幅氛围场范式）
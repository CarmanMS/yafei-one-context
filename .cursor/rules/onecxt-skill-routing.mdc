---
description: 用户一句话触发任务时，必须走 skills/ 下对应 SKILL.md 与脚本，禁止只用零散系统命令代替
alwaysApply: true
---

# Skill 路由（强制）

本仓库在 **`skills/`** 下维护**唯一权威**的可执行流程。当用户意图匹配下方任一情形时，代理 **必须**：

1. **先**用 `read_file`（或等价方式）**完整阅读**对应的 **`SKILL.md`**；
2. **再**按该文档步骤执行（含运行文档中的脚本路径），**不得**仅用「自己编的」通用命令替代整条流程。

## C 盘 / 磁盘空间（Windows）

**触发示例**（含同义说法）：清理 C 盘、腾空间、C 盘满了、磁盘空间不足、Docker/npm 占 C 盘、WSL 太大、我要清理c盘。

**权威文档**：`skills/windows-c-drive-cleanup/SKILL.md`

**阶段 1（只读，无需用户额外授权跑脚本）**：在仓库根下用终端执行（**默认**产品化报告）：

`.\skills\windows-c-drive-cleanup\survey-c-drive-report.ps1`

可选：`-Verbose`（逐项统计进度）、`-IncludeProgramFilesBreakdown`（Program Files 下谁大）、`-IncludeWindowsFull`（整棵 Windows，很慢）。可辅以 `survey-disk-hints.ps1`（或 `-Quick`）。

用户在对话中**逐条明确同意**具体清理项后，在仓库根运行 **`invoke-c-drive-cleanup.ps1`**：必填 **`-ChatAuthorizationNote`**（用户同意原文），先 **`-DryRun`** 再实跑；开关表见 `SKILL.md`。

若当前 shell 不在仓库根，先用工作区绝对路径调用上述 `.ps1`（路径以用户克隆位置为准）。

**禁止**：在已匹配上述意图时，**只**给出 `Get-PSDrive` / `Get-Volume` 等零散命令而不读 `SKILL.md`、不运行 `survey-c-drive-report.ps1`（可与脚本输出一并展示系统命令，但不能以此代替 skill）。**禁止**未获用户对具体开关的同意就执行无 `-DryRun` 的 `invoke-c-drive-cleanup.ps1`。

## HTML 幻灯 + 口播 → MP4

**触发示例**：生成视频、幻灯片转视频、presentation 导出 MP4、口播合成视频、wav-auto / Edge TTS 视频。

**权威文档**：`skills/html-video-from-slides/SKILL.md`

按该文件中的 `cli.js` 与子命令执行；选题目录只放素材，不复制 skill 实现。

## HTML 全屏幻灯版式（防空白 / 字号 / 示意图）

**触发示例**：幻灯空白多、字太小、图太小、只有网格底、`fill-deck`、改 `presentation.html` 版式、卡片里很空。

**权威文档**：`skills/html-deck-layout/SKILL.md`

先读该文件再改 HTML/CSS；与 `html-video-from-slides` 分工：成片流程前者，**单页视觉与信息密度**后者。

## AI 生图封面 / cover-prompt

**触发示例**：封面提示词、cover-prompt、生图封面、Midjourney/Flux 封面、写 `production/cover-prompt.md`、封面 callout/tag。

**权威文档**：`skills/cover-prompt/SKILL.md`

**硬门禁**：先完整阅读 SKILL → 完成 **Step 0**（默认 **极简艺术海报**，2 行标题）→ 复制 `assets/template-minimal-cover-prompt.md` 写产出；**禁止**因 00-structure 密、Remotion Scene 多而自动选信息密度型。

**禁止**：未读 SKILL 就写带 callout/tag 的 cover-prompt；把 SceneCover/ETCLOVG 七层栈搬进平台缩略图 prompt。

## 其他

新增 skill 时，在本文件与 `AGENTS.md` 的「Skill 路由」中补充一行映射。

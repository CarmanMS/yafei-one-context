---
description: one-context「跨平台/多端」= 多 AI 工具适配（adapt），非操作系统安装包；默认走 onecxt adapt
alwaysApply: true
---

# one-context：多端 / 跨工具配置（adapt）

在本仓库与 README 中，**「一次配置，多端共享」「跨工具」** 指：**同一套** `meta/workspaces.yaml`、`meta/profiles.yaml`、`meta/agents.yaml`、`knowledge/` **经适配器生成** Cursor / Claude Code / Hermes / OpenClaw 等**各工具原生配置文件**。

- **禁止**把用户说的「跨平台版本」「多端同步」默认理解成 **Python wheel / 操作系统二进制 / `python -m build`**，除非用户明确在谈打包发行。
- **权威能力**：CLI **`onecxt adapt`**（实现于 `packages/one-context/`），便携调用 **`python -m one_context adapt`**。

## 用户意图映射

| 用户说法（示例） | 应执行 / 说明 |
|---|---|
| 多端、跨平台配置、同步各工具、生成 Cursor/Claude/OpenClaw、adapt | 在仓库根：`onecxt adapt --all` 或 `python -m one_context adapt --all` |
| 只更新某一工具 | `onecxt adapt --all --only cursor`（或 `claude_code`、`hermes`、`openclaw`） |
| 单一 workspace | `onecxt adapt <WORKSPACE_ID>`（见 `meta/workspaces.yaml` 的 `id`） |

## 与 `skills/` 的关系

- **`skills/`** 下是可执行流程（HTML 视频、C 盘清理等），**不是**「adapt 多端」的占位；**没有**名为「跨平台生成」的独立 SKILL 对应整条 adapt 流水线。
- **不要把**「同步各端 AI 配置」误判为必须找某个 `skills/*/SKILL.md`；**先**想到 **`adapt`**。

## 参考

- 根目录 `README.md` 中 **适配器**、**`onecxt adapt`** 说明
- `packages/one-context/README.md` 中 `adapt`、`adapt-install` 等子命令

---
description: "Skill: arch-html — Use when the user wants to create an architecture diagram from a TEXT or Markdown description. Do NOT use when the input is an existing IMAGE — use img2svg instead. Triggers on arch-html, text-to-diag"
globs:
alwaysApply: true
---

<!-- GENERATED by onecxt adapt — DO NOT EDIT. Changes will be overwritten. Edit the source in meta/ or knowledge/ instead. -->

# Skill: arch-html

# /arch-html - 文本生成交互式架构图 HTML（GoJS + 迭代精修）

将自然语言架构描述自动生成为基于 GoJS 的交互式分层架构图 HTML，支持三种高亮模式（直连/全链路/分层），通过双模式迭代精修确保与描述/参考图高度一致。

## 使用方式

```
/arch-html "系统包含用户层、服务层和数据层..."
/arch-html "微服务架构：API Gateway → 多个微服务 → 数据库集群" output=architecture.html
/arch-html docs/arch-description.md                          # 从 Markdown 文件读取描述
/arch-html "参考此图生成" ref=docs/reference-arch.png         # 有参考图（模式 A）
/arch-html "简单架构图" mode=single                            # 仅生成一次
/arch-html "复杂系统" max_rounds=5 threshold=90                # 自定义精修参数
```

## 核心流程

```
Phase 0: 前置检查（GoJS CDN + Playwright MCP）
    ↓
Phase 1: 输入解析 + AI 推断结构大纲 + ⚠️用户确认门控 + 生成初始 HTML
    ↓
Phase 2: 迭代精修循环（loop 模式，双模式，模式B最少2轮）
    模式 A（有参考图）：HTML 截图 → 视觉比对 → 差异报告 → HTML 修改
    模式 B（纯文本）：HTML 代码 review → 一致性报告 → HTML 修改
    ↓
Phase 3: HTML 优化 + 文件输出
```

---

## 深色主题设计系统（Design System）

从 `architecture-diagram-generator` 融合的设计规范，用于统一 GoJS 架构图的视觉风格。

### 配色语义（Semantic Color Palette）

每层/每类组件必须使用语义化颜色，确保架构图颜色含义一致：

| 语义 | 描边色 (stroke) | 填充色 (semi-transparent) | 适用层 (category) |
|------|----------------|--------------------------|-------------------|
| **Frontend / 前端** | `#22d3ee` (cyan-400) | `rgba(8, 51, 68, 0.4)` | user（用户接入层） |
| **Backend / 后端** | `#34d399` (emerald-400) | `rgba(6, 78, 59, 0.4)` | service（服务层） |
| **Database / 数据** | `#a78bfa` (violet-400) | `rgba(76, 29, 149, 0.4)` | model / tool（AI层 / 工具中间件层） |
| **Cloud / 基础设施** | `#fbbf24` (amber-400) | `rgba(120, 53, 15, 0.3)` | infra（基础设施层） |
| **Security / 安全** | `#fb7185` (rose-400) | `rgba(136, 19, 55, 0.4)` | govern（安全/治理层） |
| **Message Bus** | `#fb923c` (orange-400) | `rgba(251, 146, 60, 0.3)` | 消息总线节点（特殊） |
| **External** | `#94a3b8` (slate-400) | `rgba(30, 41, 59, 0.5)` | 外部系统/用户 |

### 深色主题画布

- **Diagram 背景**：`#020617` (slate-950)
- **网格线**：`#1e293b`，40px 间距，SVG pattern 或 GoJS GridPattern
- **Group 背景**：半透明深色填充，虚线边框（dash: [8, 4]），rx=12
- **文本颜色**：`white` 为主标签，`#94a3b8` 为辅标签/连线标签
- **连线（Link）**：默认 `#64748b`（slate-500），实线 1.5px

### 字体栈

优先使用 JetBrains Mono 保持技术感，中文回退到系统字体：
```
font-family: 'JetBrains Mono', -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', 'Noto Sans SC', sans-serif
```

### 组件盒样式

- 圆角矩形：`rx=6, ry=6`
- 描边宽度：`strokeWidth=1.5`
- 填充：半透明 rgba（上表），保持一致
- 标签字号：主标签 12px 加粗，辅标签 9px
- **箭头遮挡处理**：GoJS 中通过 `layerName` 控制层级，link 放在 "Background" 层，node 放在默认层，确保连线在节点下方

---

## Phase 0: 前置检查

### 0.1 检查 GoJS CDN 可达性 + 版本锁定

```bash
# 第一步：检查可达性
curl -sI https://unpkg.com/gojs@3/release/go.js 2>&1 | head -20

# 第二步：从重定向 URL 提取锁定版本号
# 重定向 URL 格式：https://unpkg.com/gojs@3.1.9/release/go.js
# 提取版本号：3.1.9
GOJS_VERSION=$(curl -sI https://unpkg.com/gojs@3/release/go.js 2>&1 | grep -i "^location:" | sed 's|.*/gojs@||' | sed 's|/release.*||')
```

**版本锁定规则（强制）**：

| 步骤 | 操作 | 说明 |
|------|------|------|
| 1 | 获取 `GOJS_VERSION` | 从 CDN 重定向 URL 中提取实际版本号 |
| 2 | 存储为变量 | `GOJS_VERSION=3.1.9`（示例） |
| 3 | 写入 HTML | `<script src="https://unpkg.com/gojs@{GOJS_VERSION}/release/go.js">` |
| 4 | **禁止**使用 `@3` 浮动版本 | 必须锁定为具体版本号，防止 API 变更导致白屏 |

**⚠️ 强制规则：HTML 中的 GoJS CDN URL 必须使用锁定版本号（如 `gojs@3.1.9`），严禁使用浮动版本号（如 `gojs@3`）。**

**降级策略**：

| 依赖状态 | 降级行为 |
|---------|---------|
| CDN 可达 + 版本提取成功 | 正常，HTML 使用锁定版本号 |
| CDN 可达 + 版本提取失败 | 使用默认 `gojs@3.1.9`，HTML 中写死 |
| CDN 不可达 | 警告用户：HTML 需联网环境打开，不影响生成 |

### 0.2 检查 Playwright MCP 可用性

检查 `mcp__playwright__browser_navigate` 工具是否可用：

**降级策略**：

| Playwright 状态 | 降级行为 |
|----------------|---------|
| 可用 | 支持视觉比对模式和代码审查模式 |
| 不可用 + 无参考图 | 仅代码审查模式，不受影响 |
| 不可用 + 有参考图 | 强制降级为代码审查模式，**使用 AskUserQuestion 询问用户是否继续** |

### 0.2.1 组合降级（重要）

| GoJS CDN | Playwright | 降级行为 |
|----------|-----------|---------|
| 可达 | 可用 | 正常（双模式可用） |
| 不可达 | 可用 | 生成 HTML 需联网打开，精修正常 |
| 可达 | 不可用 | 降级为代码审查模式 |
| **不可达** | **不可用** | **强制 mode=single，明确警告用户："HTML 需联网环境才能渲染，且无法进行任何视觉验证。若需迭代精修，请确保 Playwright MCP 可用。"** 使用 AskUserQuestion 询问用户是否继续 |

### 0.3 解析参数

**参数解析**：
- `input`：用户输入（文本描述或 Markdown 文件路径）
- `ref`：参考图路径（可选）
- `mode`：默认 `loop`，Playwright 不可用且 ref 参数存在时不影响（降级为模式 B）
- `max_rounds`：默认 `10`
- `threshold`：默认 `95`
- `output`：默认 `{title}_arch.html`

**输入类型判断**：

| 输入 | 处理方式 |
|------|---------|
| 文本描述 | 直接使用 |
| `.md` 文件路径 | 读取 Markdown 提取正文作为描述 |
| `ref=` 图片路径 | 用于模式 A 截图比对 |

---

## Phase 1: 初始 HTML 生成

### 1.1 AI 推断结构大纲（两步法）

#### 第一步：生成结构大纲

使用 AI 根据自然语言描述推断架构结构，输出结构化 JSON。

**结构大纲提示词**：

```
你是架构图结构分析专家。请根据以下自然语言描述，推断架构图的分层结构和组件关系。

## 分析要求（强制）

1. **推断分层**：从描述中推断架构分为几层，每层名称（中英文双语），如"用户接入层 Access Layer"。分层名称需要准确反映该层的职责。
2. **列举节点**：每层包含哪些组件/节点，每个节点用简短中英文标签表示
3. **定义连线**：组件之间的调用/依赖关系，标注连线标签和样式（实线 solid / 虚线 dashed）
4. **跨层关系**：横切层（如安全/治理/监控层）与各层的关系，用虚线表示
5. **配色方案**：为每层选择配色（背景色 gBg、边框色 gBd、节点填充色 bg、节点文字色 text）

## 布局规则（强制）

- 节点统一尺寸：minWidth=160, maxWidth=220, height=48
- 水平间距：HGAP=28（节点间水平距离）
- 垂直间距：VGAP=28（同组内节点行间距）
- 层间距：每组间垂直距离 >= 130
- 横切层：放在右侧独立列，GOV_X = 5 * (NW + HGAP) + 80
- pos 格式："{x} {y}"（GoJS Point.parse 格式）
- 同行节点：baseX + i * (NW + HGAP), baseY
- 多行节点：第二行 baseY + NH + VGAP

## 输出格式（严格 JSON）

```json
{
  "title": "架构图标题",
  "groups": [
    {
      "id": "g-user",
      "text": "用户接入层  Access Layer",
      "category": "user",
      "nodes": [
        {"key": "Web UI", "pos": "0 0"},
        {"key": "API Gateway", "pos": "172 0"},
        {"key": "SDK / CLI", "pos": "344 0"}
      ]
    }
  ],
  "links": [
    {"from": "Web UI", "to": "API Gateway", "label": "请求", "dash": null},
    {"from": "API Gateway", "to": "Auth Service", "label": "auth", "dash": [6, 4]}
  ]
}
```

**category 取值与配色映射**（深色主题）：
- "user": 用户接入层 / 前端（Frontend - cyan `#22d3ee`）
- "service": 服务/应用层 / 后端（Backend - emerald `#34d399`）
- "model": 模型/AI层（Database - violet `#a78bfa`）
- "tool": 工具/中间件层（Database - violet `#a78bfa`）
- "infra": 基础设施层（Cloud - amber `#fbbf24`）
- "govern": 安全/治理层（Security - rose `#fb7185`）

**重点**：
- 描述中提到的所有组件都必须出现在节点列表中
- 描述中提到的所有关系都必须出现在连线列表中
- 横切关系（安全、认证、监控等）用虚线 dash: [6, 4]
- pos 坐标必须精确计算，确保节点不重叠
```

#### 1.5 用户确认门控（钻石门条件）

结构大纲生成后，**必须暂停并等待用户确认**，确保 AI 对架构的理解正确后再进入 HTML 生成阶段。

使用 `AskUserQuestion` 展示结构大纲摘要并请求确认：

```
📋 AI 推断的架构结构大纲：

标题：{title}
层数：{N} 层
  - {第1层名称}：{节点列表}
  - {第2层名称}：{节点列表}
  - ...
连线：{M} 条

请确认结构大纲是否正确：
  [1] ✅ 正确，继续生成 HTML
  [2] ✏️ 需要调整（请说明需要修改的内容）
  [3] ❌ 重新推断（请补充描述信息）
```

**门控规则**：
- 用户选择 `[1]`：继续进入第二步生成 HTML
- 用户选择 `[2]`：根据用户反馈修改大纲（修改节点/层/连线），修改后重新确认
- 用户选择 `[3]`：回到第一步，结合用户补充的描述重新推断大纲
- **禁止跳过确认**：即使 mode=single，也必须经过确认门控
- **大纲校验**：确认前检查 JSON 格式合法性、必填字段完整性（title、groups、links）

#### 第二步：根据大纲生成完整 GoJS HTML

将第一步输出的结构大纲（经用户确认后）作为上下文，结合原始描述，生成完整 HTML。HTML 必须遵循 GoJS 架构图模板规范（见下文）。

**生成提示词**（关键约束）：

```
你是一个 GoJS 架构图生成专家。请根据以下结构大纲和原始描述，生成完整的 GoJS HTML 文件。

## 结构大纲
{上一步输出的 JSON 大纲}

## 原始描述
{用户输入的文本描述}

## 生成规则（强制）

### 结构完整性（最高优先级）
1. 大纲中列出的每个节点必须出现在 nodeDataArray 中，不得遗漏
2. 大纲中列出的每个 Group 必须创建，且分配正确的配色
3. 大纲中列出的每条连线必须绘制，包括连线上标注文字和虚线样式
4. 验证：生成后自检 nodeDataArray 中的节点数量是否与大纲一致

### GoJS 模板规范
5. 必须包含完整的 HTML 骨架（DOCTYPE、head、body）
6. 必须包含工具栏：放大/缩小/适应画布/重置/直连/全链路/分层/清除高亮
7. 必须包含 GoJS Group 模板（isSubGraphExpanded: true, movable: true）
8. 必须包含 GoJS Node 模板（含 Tooltip、isHighlighted 高亮效果）
9. 必须包含 GoJS Link 模板（含 label、dash 虚线、isHighlighted 高亮效果）
10. GoJS 从 CDN 加载：https://unpkg.com/gojs@{GOJS_VERSION}/release/go.js（**必须使用 Phase 0 提取的锁定版本号**，严禁 `@3` 浮动版本）
11. 所有 CSS/JS 必须内联，不依赖外部资源（除 GoJS CDN）

### 高亮模式（三种）
12. 直连模式（direct）：点击节点高亮相邻上下游节点和连线
13. 全链路模式（chain）：BFS 追踪整条调用链上下游
14. 分层高亮模式（layer）：点击节点高亮同 Group 所有节点和组内连线
15. 点击空白区域清除高亮
16. 初始加载后自动 zoomToFit

### 坐标与布局
17. 节点坐标使用 Manual Layout（pos 属性），确保不重叠
18. 节点尺寸：minWidth: 160, maxWidth: 220, height: 48（中文标签宽度自适应）
19. 配色使用大纲中的 category 映射

### 字体与主题
20. **深色主题**：GoJS Diagram 必须使用深色背景 `#020617`，配合 40px 间距的网格线 `#1e293b`
21. font-family: 'JetBrains Mono', -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', 'Noto Sans SC', sans-serif
22. 节点主标签 font-size: 12px 加粗，辅标签/连线标签 font-size: 9px，颜色 `#94a3b8`
23. 连线颜色默认 `#64748b`（slate-500），描边宽度 1.5px

请直接输出完整 HTML 代码，不要解释。
```

### 1.2 写入初始 HTML

将 AI 生成的 HTML 代码写入临时文件：

```
/tmp/claude/arch-html_{basename}_current.html
```

### 1.3 渲染健康检查（强制，不可跳过）

HTML 写入后，**必须**立即验证渲染是否正常，防止白屏输出。

**检查步骤**：

```
1. 启动本地 HTTP 服务器：cd /tmp/claude && python3 -m http.server 18765 &
2. 使用 Playwright/Puppeteer 打开 http://localhost:18765/arch-html_{basename}_current.html
3. 等待 GoJS 渲染：browser_wait_for → selector: ".diagramcanvas", timeout: 5000
   （若 .diagramcanvas 不存在，等待 3 秒后截图检查）
4. 截图：browser_take_screenshot → /tmp/claude/arch-html_health_check.png
5. 检查截图是否白屏/空白（使用 Read 工具读取截图，判断是否大面积白色）
6. 关闭 HTTP 服务器
```

**白屏判定规则**：
- 截图中 GoJS 图表区域（div#myDiagramDiv）完全空白 → 白屏
- 只有标题 badge 和工具栏，图表区域无内容 → 白屏

**白屏修复流程**（自动，不中断用户）：
```
1. 检查浏览器控制台错误（browser_console_messages）
2. 版本锁定检查：确认 <script src> 中使用的是锁定版本号（如 gojs@3.1.9），而非浮动版本（如 gojs@3）
   - 若使用浮动版本 → 替换为 Phase 0 提取的锁定版本号
3. 常见错误模式匹配：
   - "CurveType is not defined" → 替换 go.CurveType.JumpOver 为 go.Curve.JumpOver
   - "Orientation.Auto is not defined" → 替换 go.Orientation.Auto 为 go.Orientation.Along
   - "AutoScale is not defined" → 替换 go.AutoScale 为 go.AutoScale.Uniform
   - 其他 JS 错误 → 根据错误信息修复 HTML 代码
4. 修复后重新写入 HTML 文件
5. 重新执行渲染健康检查（最多重试 3 次）
6. 3 次重试后仍白屏 → 输出当前版本并警告用户：
   ⚠️ 渲染健康检查失败，HTML 可能无法正常显示。请检查浏览器控制台错误。
   错误信息：{console_error}
```

**⚠️ 强制规则：渲染健康检查通过后才能进入 Phase 2。白屏 HTML 进入精修循环是浪费 Token。**

---

## Phase 2: 迭代精修循环（仅 mode=loop）

### ⚠️ 强制执行规则（DO NOT SKIP）

Phase 2 是**强制迭代流程**，不是可选建议。以下规则必须严格遵守：

1. **必须执行比对/检查步骤**：每轮必须启动 Task subagent 执行模式 A 或模式 B 的比对/检查，获取 JSON 报告
2. **必须执行修改步骤**：每轮必须根据差异/一致性报告启动 Task subagent 修改 HTML
3. **必须执行渲染健康检查**：每轮修改后必须执行渲染健康检查（Phase 1.3），确认 HTML 非白屏
4. **必须追踪并输出每轮状态**：每轮结束时必须输出状态行，格式如下
5. **禁止跳过任何步骤**：不得因"看起来差不多"或"分数已经很高"而跳过后续轮次（除非达到终止条件）
6. **禁止在终止条件不满足时提前结束**：无正当理由不得在 `score < threshold` 或 `round < min_rounds` 时终止

**每轮必输出状态行**（替换思维链输出，作为可追踪的执行日志）：

```
🔄 Phase 2 | Round {round}/{max_rounds} | 模式: {A/B} | 一致性: {score}% | 目标: {threshold}% | 终止检查: {结果}
```

**完整执行流程**（每轮必须按此顺序执行，不得省略任何步骤）：

```
round = 1, best_score = 0, best_html_path = current_html_path, prev_score = 0, prev2_score = 0, prev_ma = 0
oscillation_tracker = {}

### 🔴 不可跳过：循环入口检查
if mode != 'loop':
    跳过 Phase 2，进入 Phase 3

while round <= max_rounds:
    ### Step 1: 比对/检查（必须执行）
    if 模式 A:
        截图 → Task subagent 比对 → diff_report (JSON)
        score = diff_report.similarity
    else:
        Task subagent 代码 review → consistency_report (JSON)
        score = consistency_report.consistency
        # G2 防护：模式 B 坐标重叠额外扣分
        if 检测到 pos 坐标重叠: score = max(0, score - 10)

    ### Step 2: 更新最佳版本
    if score > best_score:
        cp /tmp/claude/arch-html_{basename}_current.html /tmp/claude/arch-html_{basename}_best.html
        best_score = score

    ### Step 3: 终止条件检查（必须逐一判断）
    terminated = false
    terminate_reason = ""

    # 3a: 一致性/相似度达标（需满足 min_rounds）
    if score >= threshold and round >= min_rounds:
        terminated = true, terminate_reason = "成功终止"

    # 3b: 移动平均无提升（需 >= 3 轮数据）
    if round >= 3 and not terminated:
        current_ma = (score + prev_score + prev2_score) / 3
        if round > 3 and current_ma <= prev_ma:
            terminated = true, terminate_reason = "连续3轮移动平均无提升"
        prev_ma = current_ma

    # 3c: 超过最大轮次
    if round > max_rounds and not terminated:
        terminated = true, terminate_reason = "轮次用尽"

    # 3d: 振荡检测
    update oscillation_tracker with current round differences
    if oscillating_diffs found and not terminated:
        terminated = true, terminate_reason = "振荡检测"

    # 3e: 一致性回退
    if score < best_score - 5 and not terminated:
        # 回退到 best 版本
        cp /tmp/claude/arch-html_{basename}_best.html /tmp/claude/arch-html_{basename}_current.html
        terminated = true, terminate_reason = "一致性回退（{score}% < {best_score}% - 5%）"

    # 🔴 输出状态行（不可省略）
    🔄 Phase 2 | Round {round}/{max_rounds} | 模式: {A/B} | 一致性: {score}% | 目标: {threshold}% | 终止检查: {terminated ? terminate_reason : "继续"}

    if terminated:
        break

    ### Step 4: 修改 HTML（必须执行）
    启动 Task subagent（修改专家）→ 根据 diff_report/consistency_report 修改 HTML
    修改范围：仅修复报告中的差异，不重构已有正确部分

    ### Step 5: 渲染健康检查（必须执行）
    执行 Phase 1.3 渲染健康检查
    if 白屏:
        自动修复（见 Phase 1.3 白屏修复流程）
        修复后仍白屏 → 回退到 best 版本，终止循环

    ### Step 6: 更新状态变量
    prev2_score = prev_score
    prev_score = score
    round++

### 循环结束后
使用 best_html_path 作为最终 HTML（即使非最后一轮的输出）
输出 Phase 2 循环结束摘要（见 2.7）
```

### 2.1 双模式选择

```
if ref 参数提供了参考图 AND Playwright MCP 可用:
    模式 = A（截图比对）
else:
    模式 = B（代码 review 一致性检查）
```

### 2.2 模式 A：截图比对

#### Step 1: HTML → 渲染截图

使用 Playwright MCP 工具：

```
1. mcp__playwright__browser_navigate → 打开 file:///tmp/claude/arch-html_{basename}_current.html
2. mcp__playwright__browser_take_screenshot → 截图保存到 /tmp/claude/arch-html_iter_{round}.png
```

#### Step 2: 比对 Subagent

使用 `Task` 工具启动 general-purpose subagent 执行视觉比对：

**Subagent Prompt**：

```
你是架构图比对专家。请比对以下两张图片，找出渲染结果与参考图之间的差异。

## 参考图（目标）
使用 Read 工具读取：{参考图路径}

## 渲染结果
使用 Read 工具读取：/tmp/claude/arch-html_iter_{round}.png

## 比对方法

### 第一步：结构完整性检查
1. 识别参考图中所有层/区域，逐一与渲染结果对比是否遗漏
2. 在每个层内，数出参考图的节点数量，再数渲染结果的节点数量，记录差值
3. 识别参考图中每条连线，检查渲染结果中是否都有对应连线

### 第二步：细节逐项比对
4. 逐层比对节点文字标签是否与参考图一致
5. 比对层级配色是否正确
6. 比对连线样式（实线/虚线）和箭头
7. 比对连线上的标注文字

### 第三步：综合评估
8. 根据结构完整性和细节差异综合评分

## 输出格式（严格 JSON）

```json
{
  "similarity": <0-100的整数>,
  "structural_integrity": {
    "参考图层/区域数": N,
    "渲染结果层/区域数": M,
    "参考图节点总数": X,
    "渲染结果节点总数": Y,
    "参考图连线总数": A,
    "渲染结果连线总数": B,
    "缺失层": ["层名1"],
    "缺失节点清单": [{"层": "层名", "缺失节点": ["节点1", "节点2"]}]
  },
  "differences": [
    {
      "type": "missing|wrong|extra",
      "description": "<具体差异描述>",
      "location": "<层名+位置>",
      "severity": "high|medium|low"
    }
  ],
  "diff_count": <差异总数>,
  "suggestion": "<优先修复建议>"
}
```

## 评分标准
- 95-100: 节点数一致，连线一致，仅有极微小样式差异
- 85-94: 节点数差异<5%，少量样式偏差
- 70-84: 有少量节点缺失，但整体结构完整
- 50-69: 有层缺失或大面积节点缺失（>20%）
- 0-49: 主要层缺失或节点数差异>40%

## severity 标准
- high: 层缺失、≥3个关联节点缺失、文字标签错误、连线方向错误
- medium: 1-2个节点缺失、布局偏差、多余元素、颜色明显不匹配
- low: 颜色微差、边框粗细、字体大小差异

请仔细比对后输出 JSON。
```

### 2.3 模式 B：代码 Review 一致性检查

使用 `Task` 工具启动 general-purpose subagent 执行一致性检查：

**Review Subagent Prompt**：

```
你是架构图一致性检查专家。请对比以下自然语言描述和生成的 GoJS HTML 代码，检查一致性。

## 原始描述
{用户输入的文本描述}

## 当前 HTML 代码
使用 Read 工具读取：/tmp/claude/arch-html_{basename}_current.html

## 检查维度（强制逐一检查）

### 1. 节点完整性
从原始描述中提取所有提到的组件/服务/模块名称，逐一检查是否出现在 HTML 的 nodeDataArray 中。

### 2. 连线完整性
从原始描述中提取所有提到的调用/依赖关系，逐一检查是否出现在 HTML 的 linkDataArray 中。

### 3. 分层正确性
检查每个节点是否被分配到正确的 Group（层）中。例如，"MySQL 属于数据层"但 HTML 中 MySQL 在了服务层，则标记为错误。

### 4. 标签准确性
检查 nodeDataArray 中的 text 字段是否与原始描述一致（不翻译、不简写、不拼写错误）。

### 5. 连线标签
检查 linkDataArray 中的 label 字段是否与原始描述中的关系描述一致。

### 6. 虚线样式
检查横切关系（安全、认证、监控等跨层关系）是否使用了 dash: [6, 4] 虚线。

## 输出格式（严格 JSON）

```json
{
  "consistency": <0-100的整数，整体一致性>,
  "node_check": {
    "描述中节点总数": N,
    "HTML中节点总数": M,
    "missing_nodes": ["描述中有但HTML中缺少的节点名"],
    "extra_nodes": ["HTML中多余但描述未提及的节点名"]
  },
  "link_check": {
    "描述中关系总数": N,
    "HTML中连线总数": M,
    "missing_links": ["描述中有但HTML中缺少的连线描述"],
    "extra_links": ["HTML中多余但描述未提及的连线"]
  },
  "layer_check": {
    "wrong_layer": [{"node": "节点名", "current": "当前层", "expected": "期望层"}]
  },
  "label_check": {
    "wrong_labels": [{"current": "当前文字", "expected": "期望文字"}]
  },
  "dash_check": {
    "wrong_dash": ["应使用虚线但用了实线的连线"]
  },
  "differences": [
    {
      "type": "missing_node|missing_link|wrong_layer|wrong_label|wrong_dash|extra",
      "description": "<具体差异描述>",
      "severity": "high|medium|low"
    }
  ],
  "suggestion": "<优先修复建议>"
}
```

## 评分标准
- 95-100: 所有节点和连线完整，分层和标签完全正确
- 85-94: 缺少1-2个非关键节点或连线
- 70-84: 缺少少量节点/连线，或有分层错误
- 50-69: 缺少>20%节点/连线，或多个分层错误
- 0-49: 主要节点/连线缺失，结构严重不一致

## severity 标准
- high: 节点缺失、连线缺失、分层错误
- medium: 标签不准确、虚线样式错误、多余元素
- low: 布局微调、配色轻微差异

请仔细检查后输出 JSON。
```

### 2.4 修改 Subagent（两种模式共用）

使用 `Task` 工具启动 general-purpose subagent 执行 HTML 修改：

**修改 Subagent Prompt**：

```
你是 GoJS 架构图修改专家。请根据差异/一致性报告，修改指定的 HTML 文件。

## 待修改 HTML 文件
使用 Read 工具读取：/tmp/claude/arch-html_{basename}_current.html

## 差异/一致性报告
{差异报告JSON}

## 修改约束（强制）
1. 使用 Edit 工具增量修改，禁止 Write 全量覆写
2. 只修复报告中的差异，不重构已有正确部分
3. 优先修复 severity=high 的差异
4. 修改 nodeDataArray 时保持 pos 坐标格式一致
5. 新增节点的 pos 坐标必须与其他节点不重叠
6. 修改完成后，结果写入原文件

## 修改原则
- 缺少节点（missing_node）：在对应 Group 的 nodeDataArray 中添加
- 缺少连线（missing_link）：在 linkDataArray 中添加
- 分层错误（wrong_layer）：将节点移到正确的 Group
- 标签错误（wrong_label）：修改节点的 text 字段
- 虚线错误（wrong_dash）：修改 linkDataArray 中的 dash 字段
- 多余元素（extra）：从 nodeDataArray 或 linkDataArray 中删除

## 坐标计算规则
新增节点的 pos 坐标计算方法：
- 找到同 Group 内最后一个节点的 pos
- 如果同行有空间：x += NW + HGAP（NW=160, HGAP=28）
- 如果同行已满（>=4个节点）：换行 y += NH + VGAP（NH=48, VGAP=28），x 重置为 Group 起始 x
```

### 2.5 终止条件

| 条件 | 判断 | 模式 |
|------|------|------|
| 一致性/相似度达标 | `score >= threshold` 且 `round >= min_rounds` | A+B |
| 连续 3 轮移动平均无提升 | `(score + prev + prev2) / 3 <= prev_ma` 且 `round >= min_rounds` | A+B |
| 迭代超过 max_rounds | `round > max_rounds` | A+B |
| 振荡检测 | 同一差异 ≥3 轮交替出现 | A+B |
| 一致性/相似度回退 | `score < best_score - 5` | A+B |

**最少迭代轮次保障**（模式 B）：
- 模式 A：`min_rounds = 1`（视觉比对可靠，首轮即可信）
- 模式 B：`min_rounds = 2`（代码审查可能虚高，至少迭代 2 轮确保布局问题被发现）
- 当 `round < min_rounds` 时，即使 `score >= threshold` 也不终止，继续迭代

**结构大纲 schema 校验**（Phase 1 门控前执行）：
```
校验项：
1. JSON 格式合法性：能否成功解析
2. 必填字段：title, groups（非空数组）, links（数组）
3. groups 中每个元素：id, text, category, nodes（非空数组）
4. nodes 中每个元素：key, pos
5. pos 格式校验："{数字} {数字}"
6. links 中每个元素：from, to
7. category 取值校验：必须是 user/service/model/tool/infra/govern 之一
8. 坐标重叠检测：同一 Group 内节点 pos 坐标不得重叠

校验失败 → 提示 AI 修正大纲 → 重新校验 → 最多重试 2 次 → 仍失败则提示用户
```

### 2.6 振荡检测

```
oscillation_tracker = {}  # {差异描述: [出现的轮次列表]}

# 每轮更新
current_diffs_set = set(d["description"] for d in differences)
for desc in current_diffs_set:
    if desc not in oscillation_tracker:
        oscillation_tracker[desc] = []
    oscillation_tracker[desc].append(round)

# 检测振荡
oscillating_diffs = []
for desc, rounds in oscillation_tracker.items():
    if len(rounds) >= 3:
        gaps = [rounds[i+1] - rounds[i] for i in range(len(rounds)-1)]
        if any(g > 1 for g in gaps):
            oscillating_diffs.append(desc)

# 过滤振荡项
filtered_differences = [d for d in differences if d["description"] not in oscillating_diffs]
```

### 2.7 循环结束输出

```
📊 架构图 HTML 精修完成

  输出: {HTML路径}
  最终一致性/相似度: {score}%（目标: {threshold}%）
  迭代轮次: {round}/{max_rounds}
  剩余差异: {diff_count} 个（high: {h}, medium: {m}, low: {l}）
  精修模式: {模式A/模式B}
  终止原因: {成功/轮次用尽/移动平均无提升/振荡/一致性回退}
  高亮模式: 直连 / 全链路 / 分层
```

---

## Phase 3: 输出

### 3.1 HTML 优化

写入文件前进行优化：
- 移除调试用的 console.log 语句
- 确保 GoJS CDN 引用正确
- 验证 HTML 语法完整性

### 3.2 写入 HTML 文件

```
输出路径: {output 参数} 或 {当前目录}/{title}_arch.html
```

### 3.3 最终输出摘要

```
✅ 架构图 HTML 生成完成

  📄 HTML: ./architecture_arch.html
  🌐 浏览器打开: open ./architecture_arch.html
  📊 一致性/相似度: 96%（{round}轮迭代）
  🔄 高亮模式: 直连 / 全链路 / 分层
  🎯 终止原因: 成功终止
```

---

## single 模式

当 `mode=single` 时：
1. 跳过 Phase 2（迭代精修循环）
2. 直接输出 Phase 1 生成的初始 HTML
3. 可打开浏览器预览

---

## 临时文件管理

所有临时文件存放在 `/tmp/claude/` 目录：

```
/tmp/claude/
├── arch-html_{basename}_current.html    # 当前版本 HTML
├── arch-html_iter_{N}.png              # 第 N 轮渲染截图（模式 A）
└── arch-html_{basename}_best.html      # 最佳版本 HTML（回退用）
```

**清理**：处理完成后，保留最终 HTML，删除中间迭代产物。

---

## GoJS 模板规范（AI 生成时必须遵循）

### 配色方案（深色主题语义化）

```javascript
const C = {
  // user    → Frontend (cyan)
  user:   { stroke: '#22d3ee', fill: 'rgba(8, 51, 68, 0.4)',  gBg: 'rgba(8, 51, 68, 0.15)', gBd: '#22d3ee', text: '#fff', sub: '#94a3b8', icon: '👤' },
  // service → Backend (emerald)
  service:{ stroke: '#34d399', fill: 'rgba(6, 78, 59, 0.4)',   gBg: 'rgba(6, 78, 59, 0.15)',  gBd: '#34d399', text: '#fff', sub: '#94a3b8', icon: '⚙️' },
  // model   → Database (violet)
  model:  { stroke: '#a78bfa', fill: 'rgba(76, 29, 149, 0.4)', gBg: 'rgba(76, 29, 149, 0.15)', gBd: '#a78bfa', text: '#fff', sub: '#94a3b8', icon: '🧠' },
  // tool    → Database (violet)
  tool:   { stroke: '#a78bfa', fill: 'rgba(76, 29, 149, 0.4)', gBg: 'rgba(76, 29, 149, 0.15)', gBd: '#a78bfa', text: '#fff', sub: '#94a3b8', icon: '🔧' },
  // infra   → Cloud (amber)
  infra:  { stroke: '#fbbf24', fill: 'rgba(120, 53, 15, 0.3)', gBg: 'rgba(120, 53, 15, 0.1)',  gBd: '#fbbf24', text: '#fff', sub: '#94a3b8', icon: '🖥️' },
  // govern  → Security (rose)
  govern: { stroke: '#fb7185', fill: 'rgba(136, 19, 55, 0.4)', gBg: 'rgba(136, 19, 55, 0.1)',  gBd: '#fb7185', text: '#fff', sub: '#94a3b8', icon: '🛡️' },
};
```

**Diagram 深色主题初始化（强制）**：
```javascript
$(go.Diagram, "myDiagramDiv", {
  "grid.visible": true,
  "grid.gridCellSize": new go.Size(40, 40),
  "grid.origin": new go.Point(0, 0),
})
// 背景色 CSS 控制：body/diagram container 背景 `#020617`
// GridPattern 颜色 `#1e293b`
```

### 关键约束

| 规则 | 说明 |
|------|------|
| GoJS CDN | **必须锁定版本号**：`https://unpkg.com/gojs@{GOJS_VERSION}/release/go.js`（Phase 0 提取的具体版本，如 `gojs@3.1.9`），**严禁** `gojs@3` 浮动版本 |
| CSS/JS 内联 | 除 GoJS 外不依赖外部资源 |
| Manual Layout | AI 生成 pos 坐标，确保布局不重叠 |
| 节点尺寸 | `minWidth: 160, maxWidth: 220, height: 48`（中文字符宽度自适应） |
| 间距 | 水平 HGAP=28, 垂直 VGAP=28 |
| 字体栈 | `'JetBrains Mono', -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', 'Noto Sans SC', sans-serif` |
| 三种高亮 | 直接邻居 / BFS全链路 / 同Group分层 |
| 虚线连线 | 横切关系使用 `dash: [6, 4]` |
| 节点 Tooltip | 悬停显示 `节点名 (所属层 layer)` |
| 高亮样式 | isHighlighted 时边框加粗(strokeWidth=3)变为该节点语义色(stroke) + 节点添加发光阴影(glow, color=语义色, blur=12) + 连线加粗(strokeWidth=2.5)变为 `#94a3b8`；**非高亮节点 opacity: 0.15，非高亮连线 opacity: 0.08**；transition: 200ms ease。所有颜色在深色背景 `#020617` 下必须保持可读性 |
| 高亮交互 | 点击节点触发高亮，点击画布空白区清除所有高亮（重置 opacity 为 1） |
| 初始缩放 | `initialAutoScale: go.AutoScale.Uniform` + `InitialLayoutCompleted → zoomToFit` |

---

## 错误处理

| 错误 | 处理 |
|------|------|
| 输入为空 | 提示用户提供架构描述文本 |
| Markdown 文件不存在 | 提示文件路径错误，列出可能路径 |
| GoJS CDN 不可达 | 警告但继续，提示 HTML 需联网打开 |
| Playwright 不可用 | 自动降级为模式 B（code review），如果用户提供了参考图则提示无法截图比对 |
| 结构大纲生成失败 | 提示用户简化描述或提供更清晰的架构说明 |
| HTML 渲染异常 | 检查 GoJS 语法，尝试修复；若无法修复则输出当前版本并提示 |
| 迭代一致性持续下降 | 回退到 best_html 并终止 |
| 临时文件写入失败 | 检查 /tmp/claude/ 目录权限 |

---

## Gotchas（常见失败模式）

> 以下记录了运行此 Command 时容易踩的坑和应对措施，帮助模型在执行时主动规避。

### G1: 截图时序问题（模式 A）

GoJS 在浏览器中异步渲染，`browser_navigate` 后立即 `take_screenshot` 可能捕获到半成品（空白/部分渲染）。

**应对**：在 `navigate` 和 `take_screenshot` 之间插入 `browser_wait_for`，等待 GoJS 图表元素完全出现：
```
mcp__playwright__browser_navigate → file:///tmp/claude/arch-html_xxx.html
mcp__playwright__browser_wait_for → selector: ".diagramcanvas", timeout: 5000
mcp__playwright__browser_take_screenshot → /tmp/claude/arch-html_iter_N.png
```

### G2: 模式 B 一致性分数虚高

纯代码审查（模式 B）无法检测布局问题（节点重叠、间距不均、层级视觉混乱），但节点和连线文本完整性可能得到 90+ 分，导致提前终止。

**应对**：模式 B 增加结构性质检维度（坐标系合理性检测），判定前检查 pos 坐标是否有重叠，若检测到重叠则额外扣减 10 分。且模式 B 至少执行 2 轮迭代，不因首轮分数达标即停止。

### G3: pos 坐标重叠

AI 生成的 pos 坐标可能因计算错误导致节点重叠，尤其在多行节点和横切层布局中。

**应对**：结构大纲生成阶段强制要求坐标系计算（baseX + i*(NW+HGAP), baseY + row*(NH+VGAP)），并在用户确认门控（1.5）前自动校验 pos 不重叠。

### G4: GoJS 版本锁定（⚠️ 强制）

`https://unpkg.com/gojs@3/release/go.js` 中 `@3` 指向 v3.x 最新版，API 可能随版本变化导致 HTML 不可用。

**历史上已发生的 API 变更**：
- `go.CurveType.JumpOver` → `go.Curve.JumpOver`（v3.x 中 CurveType 被移除）
- `go.Orientation.Auto` → `go.Orientation.Along`（v3.x 中 Orientation.Auto 不存在）
- 未来可能有更多 API 变更

**⚠️ 强制规则**：
1. Phase 0.1 中**必须**从 CDN 重定向 URL 提取具体版本号（如 `3.1.9`）
2. HTML 中 `<script src>` **必须**使用锁定版本号（如 `gojs@3.1.9`），**严禁**使用浮动版本号（如 `gojs@3`）
3. 提取失败时使用默认版本 `gojs@3.1.9`
4. 生成 HTML 后**必须**检查 `<script src>` 中的版本号是否已锁定

### G5: 大架构图 Token 消耗

复杂架构（>30 节点、>20 连线）的 HTML 代码量大（通常 8-15K tokens），加上迭代精修（每轮比对 + 修改约 10-20K tokens），总消耗可能超过 75K tokens。

**应对**：在 Phase 1 生成后输出预估 Token 消耗（节点数 × 连线数 × 轮次 × 系数），超过 50K 时提示用户。用户可通过 `max_rounds=3` 或 `mode=single` 控制。

### G6: 结构大纲幻觉

AI 可能"发明"描述中未提及的组件或关系（幻觉），导致生成的架构图与用户意图不符。

**应对**：用户确认门控（1.5）是主要防线。此外，结构大纲提示词中已包含强制性约束（"描述中提到的所有组件都必须出现在节点列表中"），但 AI 可能过度推断。门控环节展示完整大纲让用户校验。
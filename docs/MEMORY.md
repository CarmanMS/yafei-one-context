# MEMORY.md — optional long-term notes template

This file is **not** wired into one-context core. Replace with your own structure if your tooling expects `MEMORY.md`.

Suggested sections for personal / team use:

- Project facts and repo pointers
- Standing preferences (language, tone, review rules)
- Tooling notes (CLI paths, API endpoints — **no secrets**)

Remove this placeholder content when you start filling it in.
